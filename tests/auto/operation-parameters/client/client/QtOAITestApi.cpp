/**
 * Operation Parameters Test
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "QtOAITestApi.h"
#include "QtOAIServerConfiguration.h"

#include <QtCore/qanystringview.h>
#include <QtCore/qjsonarray.h>
#include <QtCore/qjsondocument.h>
#include <QtNetwork/qhttpmultipart.h>
#include <QtNetwork/qnetworkaccessmanager.h>
#include <QtNetwork/qnetworkreply.h>
#include <QtNetwork/qnetworkrequestfactory.h>
#include <QtNetwork/qrestaccessmanager.h>

using namespace Qt::StringLiterals;

namespace QtOpenAPI {

QtOAITestApi::QtOAITestApi(QObject *parent)
    : QtOAIBaseApi(parent)
{
    initializeServerConfigs();
}

QtOAITestApi::~QtOAITestApi()
{
}

void QtOAITestApi::initializeServerConfigs()
{
    //Default server
    QList<QtOAIServerConfiguration> defaultConf = QList<QtOAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(QtOAIServerConfiguration(
    QUrl("http://127.0.0.1:10203/v2"),
    "No description provided",
    QMap<QString, QtOAIServerVariable>()));
    m_serverConfigs.insert("deepObjectExplodeModelMap", defaultConf);
    m_serverIndices.insert("deepObjectExplodeModelMap", 0);
    m_serverConfigs.insert("deepObjectExplodeObject", defaultConf);
    m_serverIndices.insert("deepObjectExplodeObject", 0);
    m_serverConfigs.insert("deepObjectExplodeStringMap", defaultConf);
    m_serverIndices.insert("deepObjectExplodeStringMap", 0);
    m_serverConfigs.insert("deepObjectNotExplodeObject", defaultConf);
    m_serverIndices.insert("deepObjectNotExplodeObject", 0);
    m_serverConfigs.insert("formExplodeAnytype", defaultConf);
    m_serverIndices.insert("formExplodeAnytype", 0);
    m_serverConfigs.insert("formExplodeArray", defaultConf);
    m_serverIndices.insert("formExplodeArray", 0);
    m_serverConfigs.insert("formExplodeDifferentOptions", defaultConf);
    m_serverIndices.insert("formExplodeDifferentOptions", 0);
    m_serverConfigs.insert("formExplodeDouble", defaultConf);
    m_serverIndices.insert("formExplodeDouble", 0);
    m_serverConfigs.insert("formExplodeFloat", defaultConf);
    m_serverIndices.insert("formExplodeFloat", 0);
    m_serverConfigs.insert("formExplodeInt", defaultConf);
    m_serverIndices.insert("formExplodeInt", 0);
    m_serverConfigs.insert("formExplodeModelMap", defaultConf);
    m_serverIndices.insert("formExplodeModelMap", 0);
    m_serverConfigs.insert("formExplodeObject", defaultConf);
    m_serverIndices.insert("formExplodeObject", 0);
    m_serverConfigs.insert("formExplodeString", defaultConf);
    m_serverIndices.insert("formExplodeString", 0);
    m_serverConfigs.insert("formExplodeStringMap", defaultConf);
    m_serverIndices.insert("formExplodeStringMap", 0);
    m_serverConfigs.insert("formExplodeStringOptions", defaultConf);
    m_serverIndices.insert("formExplodeStringOptions", 0);
    m_serverConfigs.insert("formNotExplodeAnytype", defaultConf);
    m_serverIndices.insert("formNotExplodeAnytype", 0);
    m_serverConfigs.insert("formNotExplodeArray", defaultConf);
    m_serverIndices.insert("formNotExplodeArray", 0);
    m_serverConfigs.insert("formNotExplodeDouble", defaultConf);
    m_serverIndices.insert("formNotExplodeDouble", 0);
    m_serverConfigs.insert("formNotExplodeFloat", defaultConf);
    m_serverIndices.insert("formNotExplodeFloat", 0);
    m_serverConfigs.insert("formNotExplodeInt", defaultConf);
    m_serverIndices.insert("formNotExplodeInt", 0);
    m_serverConfigs.insert("formNotExplodeModelMap", defaultConf);
    m_serverIndices.insert("formNotExplodeModelMap", 0);
    m_serverConfigs.insert("formNotExplodeObject", defaultConf);
    m_serverIndices.insert("formNotExplodeObject", 0);
    m_serverConfigs.insert("formNotExplodeString", defaultConf);
    m_serverIndices.insert("formNotExplodeString", 0);
    m_serverConfigs.insert("formNotExplodeStringMap", defaultConf);
    m_serverIndices.insert("formNotExplodeStringMap", 0);
    m_serverConfigs.insert("labelExplodeAnytype", defaultConf);
    m_serverIndices.insert("labelExplodeAnytype", 0);
    m_serverConfigs.insert("labelExplodeArray", defaultConf);
    m_serverIndices.insert("labelExplodeArray", 0);
    m_serverConfigs.insert("labelExplodeDouble", defaultConf);
    m_serverIndices.insert("labelExplodeDouble", 0);
    m_serverConfigs.insert("labelExplodeFloat", defaultConf);
    m_serverIndices.insert("labelExplodeFloat", 0);
    m_serverConfigs.insert("labelExplodeInt", defaultConf);
    m_serverIndices.insert("labelExplodeInt", 0);
    m_serverConfigs.insert("labelExplodeModelMap", defaultConf);
    m_serverIndices.insert("labelExplodeModelMap", 0);
    m_serverConfigs.insert("labelExplodeObject", defaultConf);
    m_serverIndices.insert("labelExplodeObject", 0);
    m_serverConfigs.insert("labelExplodeString", defaultConf);
    m_serverIndices.insert("labelExplodeString", 0);
    m_serverConfigs.insert("labelExplodeStringMap", defaultConf);
    m_serverIndices.insert("labelExplodeStringMap", 0);
    m_serverConfigs.insert("labelNotExplodeAnytype", defaultConf);
    m_serverIndices.insert("labelNotExplodeAnytype", 0);
    m_serverConfigs.insert("labelNotExplodeArray", defaultConf);
    m_serverIndices.insert("labelNotExplodeArray", 0);
    m_serverConfigs.insert("labelNotExplodeDouble", defaultConf);
    m_serverIndices.insert("labelNotExplodeDouble", 0);
    m_serverConfigs.insert("labelNotExplodeFloat", defaultConf);
    m_serverIndices.insert("labelNotExplodeFloat", 0);
    m_serverConfigs.insert("labelNotExplodeInt", defaultConf);
    m_serverIndices.insert("labelNotExplodeInt", 0);
    m_serverConfigs.insert("labelNotExplodeModelMap", defaultConf);
    m_serverIndices.insert("labelNotExplodeModelMap", 0);
    m_serverConfigs.insert("labelNotExplodeObject", defaultConf);
    m_serverIndices.insert("labelNotExplodeObject", 0);
    m_serverConfigs.insert("labelNotExplodeString", defaultConf);
    m_serverIndices.insert("labelNotExplodeString", 0);
    m_serverConfigs.insert("labelNotExplodeStringMap", defaultConf);
    m_serverIndices.insert("labelNotExplodeStringMap", 0);
    m_serverConfigs.insert("labelStringMatrixArrayNotExplode", defaultConf);
    m_serverIndices.insert("labelStringMatrixArrayNotExplode", 0);
    m_serverConfigs.insert("matrixExplodeAnytype", defaultConf);
    m_serverIndices.insert("matrixExplodeAnytype", 0);
    m_serverConfigs.insert("matrixExplodeArray", defaultConf);
    m_serverIndices.insert("matrixExplodeArray", 0);
    m_serverConfigs.insert("matrixExplodeDouble", defaultConf);
    m_serverIndices.insert("matrixExplodeDouble", 0);
    m_serverConfigs.insert("matrixExplodeFloat", defaultConf);
    m_serverIndices.insert("matrixExplodeFloat", 0);
    m_serverConfigs.insert("matrixExplodeInt", defaultConf);
    m_serverIndices.insert("matrixExplodeInt", 0);
    m_serverConfigs.insert("matrixExplodeModelMap", defaultConf);
    m_serverIndices.insert("matrixExplodeModelMap", 0);
    m_serverConfigs.insert("matrixExplodeObject", defaultConf);
    m_serverIndices.insert("matrixExplodeObject", 0);
    m_serverConfigs.insert("matrixExplodeString", defaultConf);
    m_serverIndices.insert("matrixExplodeString", 0);
    m_serverConfigs.insert("matrixExplodeStringMap", defaultConf);
    m_serverIndices.insert("matrixExplodeStringMap", 0);
    m_serverConfigs.insert("matrixNotExplodeAnytype", defaultConf);
    m_serverIndices.insert("matrixNotExplodeAnytype", 0);
    m_serverConfigs.insert("matrixNotExplodeArray", defaultConf);
    m_serverIndices.insert("matrixNotExplodeArray", 0);
    m_serverConfigs.insert("matrixNotExplodeDouble", defaultConf);
    m_serverIndices.insert("matrixNotExplodeDouble", 0);
    m_serverConfigs.insert("matrixNotExplodeFloat", defaultConf);
    m_serverIndices.insert("matrixNotExplodeFloat", 0);
    m_serverConfigs.insert("matrixNotExplodeInt", defaultConf);
    m_serverIndices.insert("matrixNotExplodeInt", 0);
    m_serverConfigs.insert("matrixNotExplodeModelMap", defaultConf);
    m_serverIndices.insert("matrixNotExplodeModelMap", 0);
    m_serverConfigs.insert("matrixNotExplodeObject", defaultConf);
    m_serverIndices.insert("matrixNotExplodeObject", 0);
    m_serverConfigs.insert("matrixNotExplodeString", defaultConf);
    m_serverIndices.insert("matrixNotExplodeString", 0);
    m_serverConfigs.insert("matrixNotExplodeStringMap", defaultConf);
    m_serverIndices.insert("matrixNotExplodeStringMap", 0);
    m_serverConfigs.insert("pipeDelimitedExplodeAnytype", defaultConf);
    m_serverIndices.insert("pipeDelimitedExplodeAnytype", 0);
    m_serverConfigs.insert("pipeDelimitedExplodeArray", defaultConf);
    m_serverIndices.insert("pipeDelimitedExplodeArray", 0);
    m_serverConfigs.insert("pipeDelimitedExplodeObject", defaultConf);
    m_serverIndices.insert("pipeDelimitedExplodeObject", 0);
    m_serverConfigs.insert("pipeDelimitedNotExplodeAnytype", defaultConf);
    m_serverIndices.insert("pipeDelimitedNotExplodeAnytype", 0);
    m_serverConfigs.insert("pipeDelimitedNotExplodeArray", defaultConf);
    m_serverIndices.insert("pipeDelimitedNotExplodeArray", 0);
    m_serverConfigs.insert("pipeDelimitedNotExplodeModelMap", defaultConf);
    m_serverIndices.insert("pipeDelimitedNotExplodeModelMap", 0);
    m_serverConfigs.insert("pipeDelimitedNotExplodeObject", defaultConf);
    m_serverIndices.insert("pipeDelimitedNotExplodeObject", 0);
    m_serverConfigs.insert("pipeDelimitedNotExplodeStringMap", defaultConf);
    m_serverIndices.insert("pipeDelimitedNotExplodeStringMap", 0);
    m_serverConfigs.insert("simpleExplodeAnytype", defaultConf);
    m_serverIndices.insert("simpleExplodeAnytype", 0);
    m_serverConfigs.insert("simpleExplodeArray", defaultConf);
    m_serverIndices.insert("simpleExplodeArray", 0);
    m_serverConfigs.insert("simpleExplodeDouble", defaultConf);
    m_serverIndices.insert("simpleExplodeDouble", 0);
    m_serverConfigs.insert("simpleExplodeFloat", defaultConf);
    m_serverIndices.insert("simpleExplodeFloat", 0);
    m_serverConfigs.insert("simpleExplodeInt", defaultConf);
    m_serverIndices.insert("simpleExplodeInt", 0);
    m_serverConfigs.insert("simpleExplodeModelMap", defaultConf);
    m_serverIndices.insert("simpleExplodeModelMap", 0);
    m_serverConfigs.insert("simpleExplodeObject", defaultConf);
    m_serverIndices.insert("simpleExplodeObject", 0);
    m_serverConfigs.insert("simpleExplodeString", defaultConf);
    m_serverIndices.insert("simpleExplodeString", 0);
    m_serverConfigs.insert("simpleExplodeStringMap", defaultConf);
    m_serverIndices.insert("simpleExplodeStringMap", 0);
    m_serverConfigs.insert("simpleExplodeStrings", defaultConf);
    m_serverIndices.insert("simpleExplodeStrings", 0);
    m_serverConfigs.insert("simpleNotExplodeAnytype", defaultConf);
    m_serverIndices.insert("simpleNotExplodeAnytype", 0);
    m_serverConfigs.insert("simpleNotExplodeArray", defaultConf);
    m_serverIndices.insert("simpleNotExplodeArray", 0);
    m_serverConfigs.insert("simpleNotExplodeDouble", defaultConf);
    m_serverIndices.insert("simpleNotExplodeDouble", 0);
    m_serverConfigs.insert("simpleNotExplodeFloat", defaultConf);
    m_serverIndices.insert("simpleNotExplodeFloat", 0);
    m_serverConfigs.insert("simpleNotExplodeInt", defaultConf);
    m_serverIndices.insert("simpleNotExplodeInt", 0);
    m_serverConfigs.insert("simpleNotExplodeModelMap", defaultConf);
    m_serverIndices.insert("simpleNotExplodeModelMap", 0);
    m_serverConfigs.insert("simpleNotExplodeObject", defaultConf);
    m_serverIndices.insert("simpleNotExplodeObject", 0);
    m_serverConfigs.insert("simpleNotExplodeString", defaultConf);
    m_serverIndices.insert("simpleNotExplodeString", 0);
    m_serverConfigs.insert("simpleNotExplodeStringMap", defaultConf);
    m_serverIndices.insert("simpleNotExplodeStringMap", 0);
    m_serverConfigs.insert("spaceDelimitedExplodeAnytype", defaultConf);
    m_serverIndices.insert("spaceDelimitedExplodeAnytype", 0);
    m_serverConfigs.insert("spaceDelimitedExplodeArray", defaultConf);
    m_serverIndices.insert("spaceDelimitedExplodeArray", 0);
    m_serverConfigs.insert("spaceDelimitedExplodeObject", defaultConf);
    m_serverIndices.insert("spaceDelimitedExplodeObject", 0);
    m_serverConfigs.insert("spaceDelimitedNotExplodeAnytype", defaultConf);
    m_serverIndices.insert("spaceDelimitedNotExplodeAnytype", 0);
    m_serverConfigs.insert("spaceDelimitedNotExplodeArray", defaultConf);
    m_serverIndices.insert("spaceDelimitedNotExplodeArray", 0);
    m_serverConfigs.insert("spaceDelimitedNotExplodeModelMap", defaultConf);
    m_serverIndices.insert("spaceDelimitedNotExplodeModelMap", 0);
    m_serverConfigs.insert("spaceDelimitedNotExplodeObject", defaultConf);
    m_serverIndices.insert("spaceDelimitedNotExplodeObject", 0);
    m_serverConfigs.insert("spaceDelimitedNotExplodeStringMap", defaultConf);
    m_serverIndices.insert("spaceDelimitedNotExplodeStringMap", 0);
}

/**
* \fn virtual void QtOAITestApi::deepObjectExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter)
* 'deepObjectExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::deepObjectExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'deepObjectExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    deepObjectExplodeModelMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual deepObjectExplodeModelMapWithDataImpl() in derived class.
* The virtual deepObjectExplodeModelMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::deepObjectExplodeModelMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by deepObjectExplodeModelMapFinished() or
* being returned as a callback parameter of deepObjectExplodeModelMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::deepObjectExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the deepObjectExplodeModelMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking deepObjectExplodeModelMap() operation calls.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::deepObjectExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["deepObjectExplodeModelMap"][m_serverIndices.value("deepObjectExplodeModelMap")].serverUrl();
    QString fullPath = "/query/map/model-mapping/deepObject-explode/deepObjectExplodeMap";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "deepObject";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"mapParameter"_s, true, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            qWarning() << "Serialization of complex array or object properties in path or query "
                          "parameters is undefined. The generated result will not conform to the"
                          " OpenAPI standard.";
            if (queryStyle == "deepObject"_L1) {
                qsizetype index = 0;
                if (mapParameter.isEmpty())
                    qWarning() << "Map is empty!";
                for (const auto &[key, value] : mapParameter.asKeyValueRange()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"mapParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(value)));
                    index++;
                }
            } else {
                paramString.append(serializeMapValue(mapParameter, queryAssignOperator, queryDelimiter));
            }
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            deepObjectExplodeModelMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit deepObjectExplodeModelMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::deepObjectExplodeModelMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit deepObjectExplodeModelMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::deepObjectExplodeObject(const QtOAITestObject &objectParameter)
* 'deepObjectExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::deepObjectExplodeObject(const QtOAITestObject &objectParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'deepObjectExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    deepObjectExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual deepObjectExplodeObjectWithDataImpl() in derived class.
* The virtual deepObjectExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::deepObjectExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by deepObjectExplodeObjectFinished() or
* being returned as a callback parameter of deepObjectExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::deepObjectExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the deepObjectExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking deepObjectExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::deepObjectExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["deepObjectExplodeObject"][m_serverIndices.value("deepObjectExplodeObject")].serverUrl();
    QString fullPath = "/query/object/deepObject-explode/deepObjectExplodeObject";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "deepObject";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"objectParameter"_s, true, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            const QJsonObject parameter = objectParameter.asJsonObject();
            if (queryStyle == "deepObject") {
                qsizetype index = 0;
                if (parameter.isEmpty())
                    qWarning() << "Serialized parameter objectParameter is empty!";
                for (const QString &key : parameter.keys()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"objectParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(convertJsonValueToString(parameter.value(key))));
                    index++;
                }
            } else {
                paramString = serializeJsonValue(QJsonValue(parameter), queryStyle, true, querySuffix, queryAssignOperator, queryDelimiter);
            }
            // style=form && explode=true && non-object => 'objectParameter' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'objectParameter='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (paramString.isEmpty() && queryStyle == "form")
                paramString = u"objectParameter="_s;
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            deepObjectExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit deepObjectExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::deepObjectExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit deepObjectExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::deepObjectExplodeStringMap(const QMap<QString, QString> &mapParameter)
* 'deepObjectExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QString> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::deepObjectExplodeStringMap(const QMap<QString, QString> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'deepObjectExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    deepObjectExplodeStringMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual deepObjectExplodeStringMapWithDataImpl() in derived class.
* The virtual deepObjectExplodeStringMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::deepObjectExplodeStringMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by deepObjectExplodeStringMapFinished() or
* being returned as a callback parameter of deepObjectExplodeStringMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::deepObjectExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the deepObjectExplodeStringMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking deepObjectExplodeStringMap() operation calls.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::deepObjectExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["deepObjectExplodeStringMap"][m_serverIndices.value("deepObjectExplodeStringMap")].serverUrl();
    QString fullPath = "/query/map/string-mapping/deepObject-explode/deepObjectExplodeMap";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "deepObject";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"mapParameter"_s, true, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            if (queryStyle == "deepObject"_L1) {
                qsizetype index = 0;
                if (mapParameter.isEmpty())
                    qWarning() << "Map is empty!";
                for (const auto &[key, value] : mapParameter.asKeyValueRange()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"mapParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(value)));
                    index++;
                }
            } else {
                paramString.append(serializeMapValue(mapParameter, queryAssignOperator, queryDelimiter));
            }
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            deepObjectExplodeStringMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit deepObjectExplodeStringMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::deepObjectExplodeStringMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit deepObjectExplodeStringMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::deepObjectNotExplodeObject(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter = ::QtOpenAPI::OptionalParam<QtOAITestObject>())
* 'deepObjectNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::deepObjectNotExplodeObject(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter = ::QtOpenAPI::OptionalParam<QtOAITestObject>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'deepObjectNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    deepObjectNotExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual deepObjectNotExplodeObjectWithDataImpl() in derived class.
* The virtual deepObjectNotExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::deepObjectNotExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by deepObjectNotExplodeObjectFinished() or
* being returned as a callback parameter of deepObjectNotExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::deepObjectNotExplodeObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the deepObjectNotExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking deepObjectNotExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::deepObjectNotExplodeObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["deepObjectNotExplodeObject"][m_serverIndices.value("deepObjectNotExplodeObject")].serverUrl();
    QString fullPath = "/query/object/deepObject-not-explode/deepObjectNotExplodeObject";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "deepObject";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"objectParameter"_s, false, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (objectParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            const QJsonObject parameter = objectParameter.value().asJsonObject();
            if (queryStyle == "deepObject") {
                qsizetype index = 0;
                if (parameter.isEmpty())
                    qWarning() << "Serialized parameter objectParameter is empty!";
                for (const QString &key : parameter.keys()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"objectParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(convertJsonValueToString(parameter.value(key))));
                    index++;
                }
            } else {
                paramString = serializeJsonValue(QJsonValue(parameter), queryStyle, false, querySuffix, queryAssignOperator, queryDelimiter);
            }
            // style=form && explode=true && non-object => 'objectParameter' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'objectParameter='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (paramString.isEmpty() && queryStyle == "form")
                paramString = u"objectParameter="_s;
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            deepObjectNotExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit deepObjectNotExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::deepObjectNotExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit deepObjectNotExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formExplodeAnytype(const QJsonValue &anytypeParameter)
* 'formExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formExplodeAnytype(const QJsonValue &anytypeParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formExplodeAnytypeWithDataImpl() in derived class.
* The virtual formExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formExplodeAnytypeFinished() or
* being returned as a callback parameter of formExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formExplodeAnytype"][m_serverIndices.value("formExplodeAnytype")].serverUrl();
    QString fullPath = "/query/anytype/form-explode/formExplodeAnytype";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"anytypeParameter"_s, true, anytypeParameter.type() == QJsonValue::Object);
        const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, anytypeParameter.type() == QJsonValue::Object);
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            paramString = serializeJsonValue(anytypeParameter, queryStyle, true, querySuffix, queryAssignOperator, queryDelimiter);
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formExplodeArray(const QList<qint32> &arrayParameter)
* 'formExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formExplodeArray(const QList<qint32> &arrayParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formExplodeArrayWithDataImpl() in derived class.
* The virtual formExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formExplodeArrayFinished() or
* being returned as a callback parameter of formExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formExplodeArray"][m_serverIndices.value("formExplodeArray")].serverUrl();
    QString fullPath = "/query/array/form-explode/formExplodeArray";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"arrayParameter"_s, true, (!false && !true));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !true));
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(serializeArrayValue(arrayParameter, queryStyle, true, querySuffix, queryDelimiter));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formExplodeDifferentOptions(const qint32 &stringParameterB, const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter = ::QtOpenAPI::OptionalParam<QtOAITestObject>())
* 'formExplodeDifferentOptions' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameterB qint32 [required]
* @param[in] objectParameter QtOAITestObject [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formExplodeDifferentOptions(const qint32 &stringParameterB, const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter = ::QtOpenAPI::OptionalParam<QtOAITestObject>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formExplodeDifferentOptions' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formExplodeDifferentOptions(stringParameterB, objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formExplodeDifferentOptionsWithDataImpl() in derived class.
* The virtual formExplodeDifferentOptionsWithDataImpl() is being called by the template
* function.

* @param[in] stringParameterB qint32 [required]
* @param[in] objectParameter QtOAITestObject [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formExplodeDifferentOptionsCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formExplodeDifferentOptionsFinished() or
* being returned as a callback parameter of formExplodeDifferentOptions() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formExplodeDifferentOptionsWithDataImpl(const qint32 &stringParameterB, const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formExplodeDifferentOptions() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formExplodeDifferentOptions() operation calls.

* @param[in] stringParameterB qint32 [required]
* @param[in] objectParameter QtOAITestObject [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formExplodeDifferentOptionsWithDataImpl(const qint32 &stringParameterB, const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formExplodeDifferentOptions"][m_serverIndices.value("formExplodeDifferentOptions")].serverUrl();
    QString fullPath = "/query/strings/form-explode/formExplodeDifferentOptions";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"objectParameter"_s, true, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (objectParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            const QJsonObject parameter = objectParameter.value().asJsonObject();
            if (queryStyle == "deepObject") {
                qsizetype index = 0;
                if (parameter.isEmpty())
                    qWarning() << "Serialized parameter objectParameter is empty!";
                for (const QString &key : parameter.keys()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"objectParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(convertJsonValueToString(parameter.value(key))));
                    index++;
                }
            } else {
                paramString = serializeJsonValue(QJsonValue(parameter), queryStyle, true, querySuffix, queryAssignOperator, queryDelimiter);
            }
            // style=form && explode=true && non-object => 'objectParameter' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'objectParameter='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (paramString.isEmpty() && queryStyle == "form")
                paramString = u"objectParameter="_s;
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"stringParameterB"_s, true, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameterB)));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formExplodeDifferentOptionsCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formExplodeDifferentOptionsErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formExplodeDifferentOptionsCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formExplodeDifferentOptionsFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formExplodeDouble(const ::QtOpenAPI::OptionalParam<double> &doubleParameter = ::QtOpenAPI::OptionalParam<double>())
* 'formExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] doubleParameter double [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formExplodeDouble(const ::QtOpenAPI::OptionalParam<double> &doubleParameter = ::QtOpenAPI::OptionalParam<double>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formExplodeDouble(doubleParameter, this, [&](const QRestReply &reply, const QtOAIDoubleResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formExplodeDoubleWithDataImpl() in derived class.
* The virtual formExplodeDoubleWithDataImpl() is being called by the template
* function.

* @param[in] doubleParameter double [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formExplodeDoubleCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formExplodeDoubleFinished() or
* being returned as a callback parameter of formExplodeDouble() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formExplodeDoubleWithDataImpl(const ::QtOpenAPI::OptionalParam<double> &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formExplodeDouble() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formExplodeDouble() operation calls.

* @param[in] doubleParameter double [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formExplodeDoubleWithDataImpl(const ::QtOpenAPI::OptionalParam<double> &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formExplodeDouble"][m_serverIndices.value("formExplodeDouble")].serverUrl();
    QString fullPath = "/query/double/form-explode/formExplodeDouble";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"doubleParameter"_s, true, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (doubleParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(doubleParameter.value())));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formExplodeDoubleCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formExplodeDoubleErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIDoubleResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formExplodeDoubleCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIDoubleResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formExplodeDoubleFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formExplodeFloat(const ::QtOpenAPI::OptionalParam<float> &floatParameter = ::QtOpenAPI::OptionalParam<float>())
* 'formExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] floatParameter float [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formExplodeFloat(const ::QtOpenAPI::OptionalParam<float> &floatParameter = ::QtOpenAPI::OptionalParam<float>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formExplodeFloat(floatParameter, this, [&](const QRestReply &reply, const QtOAIFloatResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formExplodeFloatWithDataImpl() in derived class.
* The virtual formExplodeFloatWithDataImpl() is being called by the template
* function.

* @param[in] floatParameter float [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formExplodeFloatCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formExplodeFloatFinished() or
* being returned as a callback parameter of formExplodeFloat() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formExplodeFloatWithDataImpl(const ::QtOpenAPI::OptionalParam<float> &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formExplodeFloat() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formExplodeFloat() operation calls.

* @param[in] floatParameter float [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formExplodeFloatWithDataImpl(const ::QtOpenAPI::OptionalParam<float> &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formExplodeFloat"][m_serverIndices.value("formExplodeFloat")].serverUrl();
    QString fullPath = "/query/float/form-explode/formExplodeFloat";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"floatParameter"_s, true, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (floatParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(floatParameter.value())));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formExplodeFloatCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formExplodeFloatErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIFloatResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formExplodeFloatCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIFloatResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formExplodeFloatFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formExplodeInt(const ::QtOpenAPI::OptionalParam<qint64> &intParameter = ::QtOpenAPI::OptionalParam<qint64>())
* 'formExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] intParameter qint64 [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formExplodeInt(const ::QtOpenAPI::OptionalParam<qint64> &intParameter = ::QtOpenAPI::OptionalParam<qint64>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formExplodeInt(intParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formExplodeIntWithDataImpl() in derived class.
* The virtual formExplodeIntWithDataImpl() is being called by the template
* function.

* @param[in] intParameter qint64 [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formExplodeIntCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formExplodeIntFinished() or
* being returned as a callback parameter of formExplodeInt() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formExplodeIntWithDataImpl(const ::QtOpenAPI::OptionalParam<qint64> &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formExplodeInt() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formExplodeInt() operation calls.

* @param[in] intParameter qint64 [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formExplodeIntWithDataImpl(const ::QtOpenAPI::OptionalParam<qint64> &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formExplodeInt"][m_serverIndices.value("formExplodeInt")].serverUrl();
    QString fullPath = "/query/int/form-explode/formExplodeInt";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"intParameter"_s, true, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (intParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(intParameter.value())));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formExplodeIntCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formExplodeIntErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formExplodeIntCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formExplodeIntFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter)
* 'formExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formExplodeModelMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formExplodeModelMapWithDataImpl() in derived class.
* The virtual formExplodeModelMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formExplodeModelMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formExplodeModelMapFinished() or
* being returned as a callback parameter of formExplodeModelMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formExplodeModelMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formExplodeModelMap() operation calls.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formExplodeModelMap"][m_serverIndices.value("formExplodeModelMap")].serverUrl();
    QString fullPath = "/query/map/model-mapping/form-explode/formExplodeMap";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"mapParameter"_s, true, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            qWarning() << "Serialization of complex array or object properties in path or query "
                          "parameters is undefined. The generated result will not conform to the"
                          " OpenAPI standard.";
            if (queryStyle == "deepObject"_L1) {
                qsizetype index = 0;
                if (mapParameter.isEmpty())
                    qWarning() << "Map is empty!";
                for (const auto &[key, value] : mapParameter.asKeyValueRange()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"mapParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(value)));
                    index++;
                }
            } else {
                paramString.append(serializeMapValue(mapParameter, queryAssignOperator, queryDelimiter));
            }
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formExplodeModelMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formExplodeModelMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formExplodeModelMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formExplodeModelMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formExplodeObject(const QtOAITestObject &objectParameter)
* 'formExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formExplodeObject(const QtOAITestObject &objectParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formExplodeObjectWithDataImpl() in derived class.
* The virtual formExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formExplodeObjectFinished() or
* being returned as a callback parameter of formExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formExplodeObject"][m_serverIndices.value("formExplodeObject")].serverUrl();
    QString fullPath = "/query/object/form-explode/formExplodeObject";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"objectParameter"_s, true, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            const QJsonObject parameter = objectParameter.asJsonObject();
            if (queryStyle == "deepObject") {
                qsizetype index = 0;
                if (parameter.isEmpty())
                    qWarning() << "Serialized parameter objectParameter is empty!";
                for (const QString &key : parameter.keys()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"objectParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(convertJsonValueToString(parameter.value(key))));
                    index++;
                }
            } else {
                paramString = serializeJsonValue(QJsonValue(parameter), queryStyle, true, querySuffix, queryAssignOperator, queryDelimiter);
            }
            // style=form && explode=true && non-object => 'objectParameter' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'objectParameter='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (paramString.isEmpty() && queryStyle == "form")
                paramString = u"objectParameter="_s;
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formExplodeString(const ::QtOpenAPI::OptionalParam<QString> &stringParameter = ::QtOpenAPI::OptionalParam<QString>())
* 'formExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameter QString [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formExplodeString(const ::QtOpenAPI::OptionalParam<QString> &stringParameter = ::QtOpenAPI::OptionalParam<QString>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formExplodeString(stringParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formExplodeStringWithDataImpl() in derived class.
* The virtual formExplodeStringWithDataImpl() is being called by the template
* function.

* @param[in] stringParameter QString [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formExplodeStringCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formExplodeStringFinished() or
* being returned as a callback parameter of formExplodeString() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formExplodeStringWithDataImpl(const ::QtOpenAPI::OptionalParam<QString> &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formExplodeString() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formExplodeString() operation calls.

* @param[in] stringParameter QString [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formExplodeStringWithDataImpl(const ::QtOpenAPI::OptionalParam<QString> &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formExplodeString"][m_serverIndices.value("formExplodeString")].serverUrl();
    QString fullPath = "/query/string/form-explode/formExplodeString";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"stringParameter"_s, true, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (stringParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameter.value())));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formExplodeStringCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formExplodeStringErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formExplodeStringCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formExplodeStringFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formExplodeStringMap(const QMap<QString, QString> &mapParameter)
* 'formExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QString> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formExplodeStringMap(const QMap<QString, QString> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formExplodeStringMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formExplodeStringMapWithDataImpl() in derived class.
* The virtual formExplodeStringMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formExplodeStringMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formExplodeStringMapFinished() or
* being returned as a callback parameter of formExplodeStringMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formExplodeStringMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formExplodeStringMap() operation calls.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formExplodeStringMap"][m_serverIndices.value("formExplodeStringMap")].serverUrl();
    QString fullPath = "/query/map/string-mapping/form-explode/formExplodeMap";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"mapParameter"_s, true, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            if (queryStyle == "deepObject"_L1) {
                qsizetype index = 0;
                if (mapParameter.isEmpty())
                    qWarning() << "Map is empty!";
                for (const auto &[key, value] : mapParameter.asKeyValueRange()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"mapParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(value)));
                    index++;
                }
            } else {
                paramString.append(serializeMapValue(mapParameter, queryAssignOperator, queryDelimiter));
            }
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formExplodeStringMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formExplodeStringMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formExplodeStringMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formExplodeStringMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formExplodeStringOptions(const QString &stringParameterB, const ::QtOpenAPI::OptionalParam<QString> &stringParameterA = ::QtOpenAPI::OptionalParam<QString>(), const ::QtOpenAPI::OptionalParam<qint32> &stringParameterC = ::QtOpenAPI::OptionalParam<qint32>())
* 'formExplodeStringOptions' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameterB QString [required]
* @param[in] stringParameterA QString [optional]
* @param[in] stringParameterC qint32 [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formExplodeStringOptions(const QString &stringParameterB, const ::QtOpenAPI::OptionalParam<QString> &stringParameterA = ::QtOpenAPI::OptionalParam<QString>(), const ::QtOpenAPI::OptionalParam<qint32> &stringParameterC = ::QtOpenAPI::OptionalParam<qint32>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formExplodeStringOptions' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formExplodeStringOptions(stringParameterB, stringParameterA, stringParameterC, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formExplodeStringOptionsWithDataImpl() in derived class.
* The virtual formExplodeStringOptionsWithDataImpl() is being called by the template
* function.

* @param[in] stringParameterB QString [required]
* @param[in] stringParameterA QString [optional]
* @param[in] stringParameterC qint32 [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formExplodeStringOptionsCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formExplodeStringOptionsFinished() or
* being returned as a callback parameter of formExplodeStringOptions() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formExplodeStringOptionsWithDataImpl(const QString &stringParameterB, const ::QtOpenAPI::OptionalParam<QString> &stringParameterA, const ::QtOpenAPI::OptionalParam<qint32> &stringParameterC, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formExplodeStringOptions() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formExplodeStringOptions() operation calls.

* @param[in] stringParameterB QString [required]
* @param[in] stringParameterA QString [optional]
* @param[in] stringParameterC qint32 [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formExplodeStringOptionsWithDataImpl(const QString &stringParameterB, const ::QtOpenAPI::OptionalParam<QString> &stringParameterA, const ::QtOpenAPI::OptionalParam<qint32> &stringParameterC, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formExplodeStringOptions"][m_serverIndices.value("formExplodeStringOptions")].serverUrl();
    QString fullPath = "/query/strings/form-explode/formExplodeStringOptions";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"stringParameterA"_s, true, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (stringParameterA.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameterA.value())));
            queryParamCounter++;
        } else if (stringParameterA.isNull()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            // style=form && explode=true && non-object => 'stringParameterA' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'stringParameterA='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (queryStyle == "form")
                fullPath.append(u"stringParameterA="_s);
            else
                fullPath.append(querySuffix);
            queryParamCounter++;
        }
    }
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"stringParameterB"_s, true, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameterB)));
            queryParamCounter++;
        }
    }
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"stringParameterC"_s, true, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (stringParameterC.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameterC.value())));
            queryParamCounter++;
        } else if (stringParameterC.isNull()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            // style=form && explode=true && non-object => 'stringParameterC' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'stringParameterC='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (queryStyle == "form")
                fullPath.append(u"stringParameterC="_s);
            else
                fullPath.append(querySuffix);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formExplodeStringOptionsCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formExplodeStringOptionsErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formExplodeStringOptionsCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formExplodeStringOptionsFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formNotExplodeAnytype(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter = ::QtOpenAPI::OptionalParam<QJsonValue>())
* 'formNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formNotExplodeAnytype(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter = ::QtOpenAPI::OptionalParam<QJsonValue>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formNotExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formNotExplodeAnytypeWithDataImpl() in derived class.
* The virtual formNotExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formNotExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formNotExplodeAnytypeFinished() or
* being returned as a callback parameter of formNotExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formNotExplodeAnytypeWithDataImpl(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formNotExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formNotExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formNotExplodeAnytypeWithDataImpl(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formNotExplodeAnytype"][m_serverIndices.value("formNotExplodeAnytype")].serverUrl();
    QString fullPath = "/query/anytype/form-not-explode/formNotExplodeAnytype";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"anytypeParameter"_s, false, anytypeParameter.value().type() == QJsonValue::Object);
        const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, anytypeParameter.value().type() == QJsonValue::Object);
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (anytypeParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            paramString = serializeJsonValue(anytypeParameter.value(), queryStyle, false, querySuffix, queryAssignOperator, queryDelimiter);
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formNotExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formNotExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formNotExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formNotExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formNotExplodeArray(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter = ::QtOpenAPI::OptionalParam<QList<qint32>>())
* 'formNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formNotExplodeArray(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter = ::QtOpenAPI::OptionalParam<QList<qint32>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formNotExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formNotExplodeArrayWithDataImpl() in derived class.
* The virtual formNotExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formNotExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formNotExplodeArrayFinished() or
* being returned as a callback parameter of formNotExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formNotExplodeArrayWithDataImpl(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formNotExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formNotExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formNotExplodeArrayWithDataImpl(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formNotExplodeArray"][m_serverIndices.value("formNotExplodeArray")].serverUrl();
    QString fullPath = "/query/array/form-not-explode/formNotExplodeArray";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"arrayParameter"_s, false, (!false && !true));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !true));
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (arrayParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(serializeArrayValue(arrayParameter.value(), queryStyle, false, querySuffix, queryDelimiter));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formNotExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formNotExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formNotExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formNotExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formNotExplodeDouble(const ::QtOpenAPI::OptionalParam<double> &doubleParameter = ::QtOpenAPI::OptionalParam<double>())
* 'formNotExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] doubleParameter double [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formNotExplodeDouble(const ::QtOpenAPI::OptionalParam<double> &doubleParameter = ::QtOpenAPI::OptionalParam<double>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formNotExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formNotExplodeDouble(doubleParameter, this, [&](const QRestReply &reply, const QtOAIDoubleResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formNotExplodeDoubleWithDataImpl() in derived class.
* The virtual formNotExplodeDoubleWithDataImpl() is being called by the template
* function.

* @param[in] doubleParameter double [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formNotExplodeDoubleCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formNotExplodeDoubleFinished() or
* being returned as a callback parameter of formNotExplodeDouble() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formNotExplodeDoubleWithDataImpl(const ::QtOpenAPI::OptionalParam<double> &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formNotExplodeDouble() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formNotExplodeDouble() operation calls.

* @param[in] doubleParameter double [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formNotExplodeDoubleWithDataImpl(const ::QtOpenAPI::OptionalParam<double> &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formNotExplodeDouble"][m_serverIndices.value("formNotExplodeDouble")].serverUrl();
    QString fullPath = "/query/double/form-not-explode/formNotExplodeDouble";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"doubleParameter"_s, false, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (doubleParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(doubleParameter.value())));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formNotExplodeDoubleCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formNotExplodeDoubleErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIDoubleResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formNotExplodeDoubleCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIDoubleResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formNotExplodeDoubleFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formNotExplodeFloat(const ::QtOpenAPI::OptionalParam<float> &floatParameter = ::QtOpenAPI::OptionalParam<float>())
* 'formNotExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] floatParameter float [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formNotExplodeFloat(const ::QtOpenAPI::OptionalParam<float> &floatParameter = ::QtOpenAPI::OptionalParam<float>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formNotExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formNotExplodeFloat(floatParameter, this, [&](const QRestReply &reply, const QtOAIFloatResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formNotExplodeFloatWithDataImpl() in derived class.
* The virtual formNotExplodeFloatWithDataImpl() is being called by the template
* function.

* @param[in] floatParameter float [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formNotExplodeFloatCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formNotExplodeFloatFinished() or
* being returned as a callback parameter of formNotExplodeFloat() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formNotExplodeFloatWithDataImpl(const ::QtOpenAPI::OptionalParam<float> &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formNotExplodeFloat() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formNotExplodeFloat() operation calls.

* @param[in] floatParameter float [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formNotExplodeFloatWithDataImpl(const ::QtOpenAPI::OptionalParam<float> &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formNotExplodeFloat"][m_serverIndices.value("formNotExplodeFloat")].serverUrl();
    QString fullPath = "/query/float/form-not-explode/formNotExplodeFloat";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"floatParameter"_s, false, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (floatParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(floatParameter.value())));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formNotExplodeFloatCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formNotExplodeFloatErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIFloatResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formNotExplodeFloatCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIFloatResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formNotExplodeFloatFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formNotExplodeInt(const ::QtOpenAPI::OptionalParam<qint64> &intParameter = ::QtOpenAPI::OptionalParam<qint64>())
* 'formNotExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] intParameter qint64 [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formNotExplodeInt(const ::QtOpenAPI::OptionalParam<qint64> &intParameter = ::QtOpenAPI::OptionalParam<qint64>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formNotExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formNotExplodeInt(intParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formNotExplodeIntWithDataImpl() in derived class.
* The virtual formNotExplodeIntWithDataImpl() is being called by the template
* function.

* @param[in] intParameter qint64 [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formNotExplodeIntCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formNotExplodeIntFinished() or
* being returned as a callback parameter of formNotExplodeInt() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formNotExplodeIntWithDataImpl(const ::QtOpenAPI::OptionalParam<qint64> &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formNotExplodeInt() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formNotExplodeInt() operation calls.

* @param[in] intParameter qint64 [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formNotExplodeIntWithDataImpl(const ::QtOpenAPI::OptionalParam<qint64> &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formNotExplodeInt"][m_serverIndices.value("formNotExplodeInt")].serverUrl();
    QString fullPath = "/query/int/form-not-explode/formNotExplodeInt";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"intParameter"_s, false, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (intParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(intParameter.value())));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formNotExplodeIntCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formNotExplodeIntErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formNotExplodeIntCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formNotExplodeIntFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formNotExplodeModelMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>>())
* 'formNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formNotExplodeModelMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formNotExplodeModelMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formNotExplodeModelMapWithDataImpl() in derived class.
* The virtual formNotExplodeModelMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formNotExplodeModelMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formNotExplodeModelMapFinished() or
* being returned as a callback parameter of formNotExplodeModelMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formNotExplodeModelMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formNotExplodeModelMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formNotExplodeModelMap() operation calls.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formNotExplodeModelMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formNotExplodeModelMap"][m_serverIndices.value("formNotExplodeModelMap")].serverUrl();
    QString fullPath = "/query/map/model-mapping/form-not-explode/formNotExplodeMap";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"mapParameter"_s, false, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (mapParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            qWarning() << "Serialization of complex array or object properties in path or query "
                          "parameters is undefined. The generated result will not conform to the"
                          " OpenAPI standard.";
            if (queryStyle == "deepObject"_L1) {
                qsizetype index = 0;
                if (mapParameter.value().isEmpty())
                    qWarning() << "Map is empty!";
                for (const auto &[key, value] : mapParameter.value().asKeyValueRange()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"mapParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(value)));
                    index++;
                }
            } else {
                paramString.append(serializeMapValue(mapParameter.value(), queryAssignOperator, queryDelimiter));
            }
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formNotExplodeModelMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formNotExplodeModelMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formNotExplodeModelMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formNotExplodeModelMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formNotExplodeObject(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter = ::QtOpenAPI::OptionalParam<QtOAITestObject>())
* 'formNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formNotExplodeObject(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter = ::QtOpenAPI::OptionalParam<QtOAITestObject>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formNotExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formNotExplodeObjectWithDataImpl() in derived class.
* The virtual formNotExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formNotExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formNotExplodeObjectFinished() or
* being returned as a callback parameter of formNotExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formNotExplodeObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formNotExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formNotExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formNotExplodeObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formNotExplodeObject"][m_serverIndices.value("formNotExplodeObject")].serverUrl();
    QString fullPath = "/query/object/form-not-explode/formNotExplodeObject";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"objectParameter"_s, false, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (objectParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            const QJsonObject parameter = objectParameter.value().asJsonObject();
            if (queryStyle == "deepObject") {
                qsizetype index = 0;
                if (parameter.isEmpty())
                    qWarning() << "Serialized parameter objectParameter is empty!";
                for (const QString &key : parameter.keys()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"objectParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(convertJsonValueToString(parameter.value(key))));
                    index++;
                }
            } else {
                paramString = serializeJsonValue(QJsonValue(parameter), queryStyle, false, querySuffix, queryAssignOperator, queryDelimiter);
            }
            // style=form && explode=true && non-object => 'objectParameter' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'objectParameter='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (paramString.isEmpty() && queryStyle == "form")
                paramString = u"objectParameter="_s;
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formNotExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formNotExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formNotExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formNotExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formNotExplodeString(const ::QtOpenAPI::OptionalParam<QString> &stringParameter = ::QtOpenAPI::OptionalParam<QString>())
* 'formNotExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameter QString [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formNotExplodeString(const ::QtOpenAPI::OptionalParam<QString> &stringParameter = ::QtOpenAPI::OptionalParam<QString>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formNotExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formNotExplodeString(stringParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formNotExplodeStringWithDataImpl() in derived class.
* The virtual formNotExplodeStringWithDataImpl() is being called by the template
* function.

* @param[in] stringParameter QString [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formNotExplodeStringCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formNotExplodeStringFinished() or
* being returned as a callback parameter of formNotExplodeString() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formNotExplodeStringWithDataImpl(const ::QtOpenAPI::OptionalParam<QString> &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formNotExplodeString() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formNotExplodeString() operation calls.

* @param[in] stringParameter QString [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formNotExplodeStringWithDataImpl(const ::QtOpenAPI::OptionalParam<QString> &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formNotExplodeString"][m_serverIndices.value("formNotExplodeString")].serverUrl();
    QString fullPath = "/query/string/form-not-explode/formNotExplodeString";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"stringParameter"_s, false, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (stringParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameter.value())));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formNotExplodeStringCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formNotExplodeStringErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formNotExplodeStringCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formNotExplodeStringFinished(output);
}

/**
* \fn virtual void QtOAITestApi::formNotExplodeStringMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QString>>())
* 'formNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QString> [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::formNotExplodeStringMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QString>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'formNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    formNotExplodeStringMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual formNotExplodeStringMapWithDataImpl() in derived class.
* The virtual formNotExplodeStringMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QString> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::formNotExplodeStringMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by formNotExplodeStringMapFinished() or
* being returned as a callback parameter of formNotExplodeStringMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::formNotExplodeStringMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the formNotExplodeStringMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking formNotExplodeStringMap() operation calls.

* @param[in] mapParameter QMap<QString, QString> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::formNotExplodeStringMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["formNotExplodeStringMap"][m_serverIndices.value("formNotExplodeStringMap")].serverUrl();
    QString fullPath = "/query/map/string-mapping/form-not-explode/formNotExplodeMap";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"mapParameter"_s, false, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (mapParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            if (queryStyle == "deepObject"_L1) {
                qsizetype index = 0;
                if (mapParameter.value().isEmpty())
                    qWarning() << "Map is empty!";
                for (const auto &[key, value] : mapParameter.value().asKeyValueRange()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"mapParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(value)));
                    index++;
                }
            } else {
                paramString.append(serializeMapValue(mapParameter.value(), queryAssignOperator, queryDelimiter));
            }
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            formNotExplodeStringMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit formNotExplodeStringMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::formNotExplodeStringMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit formNotExplodeStringMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelExplodeAnytype(const QJsonValue &anytypeParameter)
* 'labelExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelExplodeAnytype(const QJsonValue &anytypeParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelExplodeAnytypeWithDataImpl() in derived class.
* The virtual labelExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelExplodeAnytypeFinished() or
* being returned as a callback parameter of labelExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelExplodeAnytype"][m_serverIndices.value("labelExplodeAnytype")].serverUrl();
    QString fullPath = "/path/anytype/label-explode/{anytypeParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString anytypeParameterPathParam = QString("{%1}").arg("anytypeParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, anytypeParameter.type() == QJsonValue::Object);
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"anytypeParameter"_s, true, anytypeParameter.type() == QJsonValue::Object);
        QString paramString = pathPrefix + serializeJsonValue(anytypeParameter, pathStyle, true, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(anytypeParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelExplodeArray(const QList<qint32> &arrayParameter)
* 'labelExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelExplodeArray(const QList<qint32> &arrayParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelExplodeArrayWithDataImpl() in derived class.
* The virtual labelExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelExplodeArrayFinished() or
* being returned as a callback parameter of labelExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelExplodeArray"][m_serverIndices.value("labelExplodeArray")].serverUrl();
    QString fullPath = "/path/array/label-explode/{arrayParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString arrayParameterPathParam = QString("{%1}").arg("arrayParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !true));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"arrayParameter"_s, true, (!false && !true));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeArrayValue(arrayParameter, pathStyle, true, pathSuffix, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(arrayParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelExplodeDouble(const double &doubleParameter)
* 'labelExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] doubleParameter double [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelExplodeDouble(const double &doubleParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelExplodeDouble(doubleParameter, this, [&](const QRestReply &reply, const QtOAIDoubleResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelExplodeDoubleWithDataImpl() in derived class.
* The virtual labelExplodeDoubleWithDataImpl() is being called by the template
* function.

* @param[in] doubleParameter double [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelExplodeDoubleCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelExplodeDoubleFinished() or
* being returned as a callback parameter of labelExplodeDouble() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelExplodeDouble() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelExplodeDouble() operation calls.

* @param[in] doubleParameter double [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelExplodeDouble"][m_serverIndices.value("labelExplodeDouble")].serverUrl();
    QString fullPath = "/path/double/label-explode/{doubleParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString doubleParameterPathParam = QString("{%1}").arg("doubleParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"doubleParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(doubleParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(doubleParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelExplodeDoubleCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelExplodeDoubleErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIDoubleResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelExplodeDoubleCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIDoubleResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelExplodeDoubleFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelExplodeFloat(const float &floatParameter)
* 'labelExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] floatParameter float [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelExplodeFloat(const float &floatParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelExplodeFloat(floatParameter, this, [&](const QRestReply &reply, const QtOAIFloatResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelExplodeFloatWithDataImpl() in derived class.
* The virtual labelExplodeFloatWithDataImpl() is being called by the template
* function.

* @param[in] floatParameter float [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelExplodeFloatCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelExplodeFloatFinished() or
* being returned as a callback parameter of labelExplodeFloat() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelExplodeFloat() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelExplodeFloat() operation calls.

* @param[in] floatParameter float [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelExplodeFloat"][m_serverIndices.value("labelExplodeFloat")].serverUrl();
    QString fullPath = "/path/float/label-explode/{floatParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString floatParameterPathParam = QString("{%1}").arg("floatParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"floatParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(floatParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(floatParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelExplodeFloatCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelExplodeFloatErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIFloatResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelExplodeFloatCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIFloatResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelExplodeFloatFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelExplodeInt(const qint64 &intParameter)
* 'labelExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] intParameter qint64 [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelExplodeInt(const qint64 &intParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelExplodeInt(intParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelExplodeIntWithDataImpl() in derived class.
* The virtual labelExplodeIntWithDataImpl() is being called by the template
* function.

* @param[in] intParameter qint64 [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelExplodeIntCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelExplodeIntFinished() or
* being returned as a callback parameter of labelExplodeInt() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelExplodeInt() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelExplodeInt() operation calls.

* @param[in] intParameter qint64 [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelExplodeInt"][m_serverIndices.value("labelExplodeInt")].serverUrl();
    QString fullPath = "/path/int/label-explode/{intParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString intParameterPathParam = QString("{%1}").arg("intParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"intParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(intParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(intParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelExplodeIntCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelExplodeIntErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelExplodeIntCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelExplodeIntFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter)
* 'labelExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelExplodeModelMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelExplodeModelMapWithDataImpl() in derived class.
* The virtual labelExplodeModelMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelExplodeModelMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelExplodeModelMapFinished() or
* being returned as a callback parameter of labelExplodeModelMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelExplodeModelMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelExplodeModelMap() operation calls.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelExplodeModelMap"][m_serverIndices.value("labelExplodeModelMap")].serverUrl();
    QString fullPath = "/path/map/model-mapping/label-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, true, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        qWarning() << "Serialization of complex array or object properties in path or query "
                      "parameters is undefined. The generated result will not conform to the "
                      "OpenAPI standard.";
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelExplodeModelMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelExplodeModelMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelExplodeModelMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelExplodeModelMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelExplodeObject(const QtOAITestObject &objectParameter)
* 'labelExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelExplodeObject(const QtOAITestObject &objectParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelExplodeObjectWithDataImpl() in derived class.
* The virtual labelExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelExplodeObjectFinished() or
* being returned as a callback parameter of labelExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelExplodeObject"][m_serverIndices.value("labelExplodeObject")].serverUrl();
    QString fullPath = "/path/object/label-explode/{objectParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString objectParameterPathParam = QString("{%1}").arg("objectParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"objectParameter"_s, true, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeJsonValue(QJsonValue(objectParameter.asJsonObject()), pathStyle, true, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(objectParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelExplodeString(const QString &stringParameter)
* 'labelExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameter QString [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelExplodeString(const QString &stringParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelExplodeString(stringParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelExplodeStringWithDataImpl() in derived class.
* The virtual labelExplodeStringWithDataImpl() is being called by the template
* function.

* @param[in] stringParameter QString [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelExplodeStringCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelExplodeStringFinished() or
* being returned as a callback parameter of labelExplodeString() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelExplodeString() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelExplodeString() operation calls.

* @param[in] stringParameter QString [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelExplodeString"][m_serverIndices.value("labelExplodeString")].serverUrl();
    QString fullPath = "/path/string/label-explode/{stringParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString stringParameterPathParam = QString("{%1}").arg("stringParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"stringParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(stringParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelExplodeStringCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelExplodeStringErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelExplodeStringCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelExplodeStringFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelExplodeStringMap(const QMap<QString, QString> &mapParameter)
* 'labelExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QString> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelExplodeStringMap(const QMap<QString, QString> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelExplodeStringMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelExplodeStringMapWithDataImpl() in derived class.
* The virtual labelExplodeStringMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelExplodeStringMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelExplodeStringMapFinished() or
* being returned as a callback parameter of labelExplodeStringMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelExplodeStringMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelExplodeStringMap() operation calls.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelExplodeStringMap"][m_serverIndices.value("labelExplodeStringMap")].serverUrl();
    QString fullPath = "/path/map/string-mapping/label-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, true, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelExplodeStringMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelExplodeStringMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelExplodeStringMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelExplodeStringMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelNotExplodeAnytype(const QJsonValue &anytypeParameter)
* 'labelNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelNotExplodeAnytype(const QJsonValue &anytypeParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelNotExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelNotExplodeAnytypeWithDataImpl() in derived class.
* The virtual labelNotExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelNotExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelNotExplodeAnytypeFinished() or
* being returned as a callback parameter of labelNotExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelNotExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelNotExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelNotExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelNotExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelNotExplodeAnytype"][m_serverIndices.value("labelNotExplodeAnytype")].serverUrl();
    QString fullPath = "/path/anytype/label-not-explode/{anytypeParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString anytypeParameterPathParam = QString("{%1}").arg("anytypeParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, anytypeParameter.type() == QJsonValue::Object);
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"anytypeParameter"_s, false, anytypeParameter.type() == QJsonValue::Object);
        QString paramString = pathPrefix + serializeJsonValue(anytypeParameter, pathStyle, false, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(anytypeParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelNotExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelNotExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelNotExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelNotExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelNotExplodeArray(const QList<qint32> &arrayParameter)
* 'labelNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelNotExplodeArray(const QList<qint32> &arrayParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelNotExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelNotExplodeArrayWithDataImpl() in derived class.
* The virtual labelNotExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelNotExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelNotExplodeArrayFinished() or
* being returned as a callback parameter of labelNotExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelNotExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelNotExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelNotExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelNotExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelNotExplodeArray"][m_serverIndices.value("labelNotExplodeArray")].serverUrl();
    QString fullPath = "/path/array/label-not-explode/{arrayParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString arrayParameterPathParam = QString("{%1}").arg("arrayParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !true));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"arrayParameter"_s, false, (!false && !true));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeArrayValue(arrayParameter, pathStyle, false, pathSuffix, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(arrayParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelNotExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelNotExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelNotExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelNotExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelNotExplodeDouble(const double &doubleParameter)
* 'labelNotExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] doubleParameter double [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelNotExplodeDouble(const double &doubleParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelNotExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelNotExplodeDouble(doubleParameter, this, [&](const QRestReply &reply, const QtOAIDoubleResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelNotExplodeDoubleWithDataImpl() in derived class.
* The virtual labelNotExplodeDoubleWithDataImpl() is being called by the template
* function.

* @param[in] doubleParameter double [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelNotExplodeDoubleCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelNotExplodeDoubleFinished() or
* being returned as a callback parameter of labelNotExplodeDouble() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelNotExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelNotExplodeDouble() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelNotExplodeDouble() operation calls.

* @param[in] doubleParameter double [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelNotExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelNotExplodeDouble"][m_serverIndices.value("labelNotExplodeDouble")].serverUrl();
    QString fullPath = "/path/double/label-not-explode/{doubleParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString doubleParameterPathParam = QString("{%1}").arg("doubleParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"doubleParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(doubleParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(doubleParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelNotExplodeDoubleCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelNotExplodeDoubleErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIDoubleResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelNotExplodeDoubleCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIDoubleResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelNotExplodeDoubleFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelNotExplodeFloat(const float &floatParameter)
* 'labelNotExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] floatParameter float [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelNotExplodeFloat(const float &floatParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelNotExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelNotExplodeFloat(floatParameter, this, [&](const QRestReply &reply, const QtOAIFloatResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelNotExplodeFloatWithDataImpl() in derived class.
* The virtual labelNotExplodeFloatWithDataImpl() is being called by the template
* function.

* @param[in] floatParameter float [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelNotExplodeFloatCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelNotExplodeFloatFinished() or
* being returned as a callback parameter of labelNotExplodeFloat() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelNotExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelNotExplodeFloat() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelNotExplodeFloat() operation calls.

* @param[in] floatParameter float [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelNotExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelNotExplodeFloat"][m_serverIndices.value("labelNotExplodeFloat")].serverUrl();
    QString fullPath = "/path/float/label-not-explode/{floatParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString floatParameterPathParam = QString("{%1}").arg("floatParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"floatParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(floatParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(floatParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelNotExplodeFloatCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelNotExplodeFloatErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIFloatResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelNotExplodeFloatCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIFloatResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelNotExplodeFloatFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelNotExplodeInt(const qint64 &intParameter)
* 'labelNotExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] intParameter qint64 [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelNotExplodeInt(const qint64 &intParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelNotExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelNotExplodeInt(intParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelNotExplodeIntWithDataImpl() in derived class.
* The virtual labelNotExplodeIntWithDataImpl() is being called by the template
* function.

* @param[in] intParameter qint64 [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelNotExplodeIntCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelNotExplodeIntFinished() or
* being returned as a callback parameter of labelNotExplodeInt() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelNotExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelNotExplodeInt() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelNotExplodeInt() operation calls.

* @param[in] intParameter qint64 [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelNotExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelNotExplodeInt"][m_serverIndices.value("labelNotExplodeInt")].serverUrl();
    QString fullPath = "/path/int/label-not-explode/{intParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString intParameterPathParam = QString("{%1}").arg("intParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"intParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(intParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(intParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelNotExplodeIntCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelNotExplodeIntErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelNotExplodeIntCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelNotExplodeIntFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelNotExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter)
* 'labelNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelNotExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelNotExplodeModelMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelNotExplodeModelMapWithDataImpl() in derived class.
* The virtual labelNotExplodeModelMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelNotExplodeModelMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelNotExplodeModelMapFinished() or
* being returned as a callback parameter of labelNotExplodeModelMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelNotExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelNotExplodeModelMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelNotExplodeModelMap() operation calls.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelNotExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelNotExplodeModelMap"][m_serverIndices.value("labelNotExplodeModelMap")].serverUrl();
    QString fullPath = "/path/map/model-mapping/label-not-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, false, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        qWarning() << "Serialization of complex array or object properties in path or query "
                      "parameters is undefined. The generated result will not conform to the "
                      "OpenAPI standard.";
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelNotExplodeModelMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelNotExplodeModelMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelNotExplodeModelMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelNotExplodeModelMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelNotExplodeObject(const QtOAITestObject &objectParameter)
* 'labelNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelNotExplodeObject(const QtOAITestObject &objectParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelNotExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelNotExplodeObjectWithDataImpl() in derived class.
* The virtual labelNotExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelNotExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelNotExplodeObjectFinished() or
* being returned as a callback parameter of labelNotExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelNotExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelNotExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelNotExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelNotExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelNotExplodeObject"][m_serverIndices.value("labelNotExplodeObject")].serverUrl();
    QString fullPath = "/path/object/label-not-explode/{objectParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString objectParameterPathParam = QString("{%1}").arg("objectParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"objectParameter"_s, false, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeJsonValue(QJsonValue(objectParameter.asJsonObject()), pathStyle, false, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(objectParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelNotExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelNotExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelNotExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelNotExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelNotExplodeString(const QString &stringParameter)
* 'labelNotExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameter QString [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelNotExplodeString(const QString &stringParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelNotExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelNotExplodeString(stringParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelNotExplodeStringWithDataImpl() in derived class.
* The virtual labelNotExplodeStringWithDataImpl() is being called by the template
* function.

* @param[in] stringParameter QString [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelNotExplodeStringCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelNotExplodeStringFinished() or
* being returned as a callback parameter of labelNotExplodeString() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelNotExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelNotExplodeString() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelNotExplodeString() operation calls.

* @param[in] stringParameter QString [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelNotExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelNotExplodeString"][m_serverIndices.value("labelNotExplodeString")].serverUrl();
    QString fullPath = "/path/string/label-not-explode/{stringParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString stringParameterPathParam = QString("{%1}").arg("stringParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"stringParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(stringParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelNotExplodeStringCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelNotExplodeStringErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelNotExplodeStringCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelNotExplodeStringFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelNotExplodeStringMap(const QMap<QString, QString> &mapParameter)
* 'labelNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QString> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelNotExplodeStringMap(const QMap<QString, QString> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelNotExplodeStringMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelNotExplodeStringMapWithDataImpl() in derived class.
* The virtual labelNotExplodeStringMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelNotExplodeStringMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelNotExplodeStringMapFinished() or
* being returned as a callback parameter of labelNotExplodeStringMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelNotExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelNotExplodeStringMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelNotExplodeStringMap() operation calls.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelNotExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelNotExplodeStringMap"][m_serverIndices.value("labelNotExplodeStringMap")].serverUrl();
    QString fullPath = "/path/map/string-mapping/label-not-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, false, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelNotExplodeStringMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelNotExplodeStringMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelNotExplodeStringMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelNotExplodeStringMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::labelStringMatrixArrayNotExplode(const QString &stringParameter, const QList<qint32> &arrayParameter)
* 'labelStringMatrixArrayNotExplode' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameter QString [required]
* @param[in] arrayParameter QList<qint32> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::labelStringMatrixArrayNotExplode(const QString &stringParameter, const QList<qint32> &arrayParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'labelStringMatrixArrayNotExplode' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    labelStringMatrixArrayNotExplode(stringParameter, arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual labelStringMatrixArrayNotExplodeWithDataImpl() in derived class.
* The virtual labelStringMatrixArrayNotExplodeWithDataImpl() is being called by the template
* function.

* @param[in] stringParameter QString [required]
* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::labelStringMatrixArrayNotExplodeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by labelStringMatrixArrayNotExplodeFinished() or
* being returned as a callback parameter of labelStringMatrixArrayNotExplode() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::labelStringMatrixArrayNotExplodeWithDataImpl(const QString &stringParameter, const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the labelStringMatrixArrayNotExplode() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking labelStringMatrixArrayNotExplode() operation calls.

* @param[in] stringParameter QString [required]
* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::labelStringMatrixArrayNotExplodeWithDataImpl(const QString &stringParameter, const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["labelStringMatrixArrayNotExplode"][m_serverIndices.value("labelStringMatrixArrayNotExplode")].serverUrl();
    QString fullPath = "/path/string/label-not-explode/array/matrix-not-explode/{stringParameter}/{arrayParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString stringParameterPathParam = QString("{%1}").arg("stringParameter");
        QString pathStyle = "label";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"stringParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("label") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(stringParameterPathParam, paramString);
    }
    {
        QString arrayParameterPathParam = QString("{%1}").arg("arrayParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !true));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"arrayParameter"_s, false, (!false && !true));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeArrayValue(arrayParameter, pathStyle, false, pathSuffix, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(arrayParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            labelStringMatrixArrayNotExplodeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit labelStringMatrixArrayNotExplodeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::labelStringMatrixArrayNotExplodeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit labelStringMatrixArrayNotExplodeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixExplodeAnytype(const QJsonValue &anytypeParameter)
* 'matrixExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixExplodeAnytype(const QJsonValue &anytypeParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixExplodeAnytypeWithDataImpl() in derived class.
* The virtual matrixExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixExplodeAnytypeFinished() or
* being returned as a callback parameter of matrixExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixExplodeAnytype"][m_serverIndices.value("matrixExplodeAnytype")].serverUrl();
    QString fullPath = "/path/anytype/matrix-explode/{anytypeParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString anytypeParameterPathParam = QString("{%1}").arg("anytypeParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, anytypeParameter.type() == QJsonValue::Object);
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"anytypeParameter"_s, true, anytypeParameter.type() == QJsonValue::Object);
        QString paramString = pathPrefix + serializeJsonValue(anytypeParameter, pathStyle, true, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(anytypeParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixExplodeArray(const QList<qint32> &arrayParameter)
* 'matrixExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixExplodeArray(const QList<qint32> &arrayParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixExplodeArrayWithDataImpl() in derived class.
* The virtual matrixExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixExplodeArrayFinished() or
* being returned as a callback parameter of matrixExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixExplodeArray"][m_serverIndices.value("matrixExplodeArray")].serverUrl();
    QString fullPath = "/path/array/matrix-explode/{arrayParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString arrayParameterPathParam = QString("{%1}").arg("arrayParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !true));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"arrayParameter"_s, true, (!false && !true));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeArrayValue(arrayParameter, pathStyle, true, pathSuffix, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(arrayParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixExplodeDouble(const double &doubleParameter)
* 'matrixExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] doubleParameter double [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixExplodeDouble(const double &doubleParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixExplodeDouble(doubleParameter, this, [&](const QRestReply &reply, const QtOAIDoubleResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixExplodeDoubleWithDataImpl() in derived class.
* The virtual matrixExplodeDoubleWithDataImpl() is being called by the template
* function.

* @param[in] doubleParameter double [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixExplodeDoubleCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixExplodeDoubleFinished() or
* being returned as a callback parameter of matrixExplodeDouble() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixExplodeDouble() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixExplodeDouble() operation calls.

* @param[in] doubleParameter double [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixExplodeDouble"][m_serverIndices.value("matrixExplodeDouble")].serverUrl();
    QString fullPath = "/path/double/matrix-explode/{doubleParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString doubleParameterPathParam = QString("{%1}").arg("doubleParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"doubleParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(doubleParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(doubleParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixExplodeDoubleCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixExplodeDoubleErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIDoubleResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixExplodeDoubleCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIDoubleResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixExplodeDoubleFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixExplodeFloat(const float &floatParameter)
* 'matrixExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] floatParameter float [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixExplodeFloat(const float &floatParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixExplodeFloat(floatParameter, this, [&](const QRestReply &reply, const QtOAIFloatResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixExplodeFloatWithDataImpl() in derived class.
* The virtual matrixExplodeFloatWithDataImpl() is being called by the template
* function.

* @param[in] floatParameter float [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixExplodeFloatCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixExplodeFloatFinished() or
* being returned as a callback parameter of matrixExplodeFloat() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixExplodeFloat() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixExplodeFloat() operation calls.

* @param[in] floatParameter float [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixExplodeFloat"][m_serverIndices.value("matrixExplodeFloat")].serverUrl();
    QString fullPath = "/path/float/matrix-explode/{floatParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString floatParameterPathParam = QString("{%1}").arg("floatParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"floatParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(floatParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(floatParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixExplodeFloatCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixExplodeFloatErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIFloatResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixExplodeFloatCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIFloatResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixExplodeFloatFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixExplodeInt(const qint64 &intParameter)
* 'matrixExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] intParameter qint64 [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixExplodeInt(const qint64 &intParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixExplodeInt(intParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixExplodeIntWithDataImpl() in derived class.
* The virtual matrixExplodeIntWithDataImpl() is being called by the template
* function.

* @param[in] intParameter qint64 [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixExplodeIntCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixExplodeIntFinished() or
* being returned as a callback parameter of matrixExplodeInt() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixExplodeInt() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixExplodeInt() operation calls.

* @param[in] intParameter qint64 [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixExplodeInt"][m_serverIndices.value("matrixExplodeInt")].serverUrl();
    QString fullPath = "/path/int/matrix-explode/{intParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString intParameterPathParam = QString("{%1}").arg("intParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"intParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(intParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(intParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixExplodeIntCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixExplodeIntErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixExplodeIntCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixExplodeIntFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter)
* 'matrixExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixExplodeModelMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixExplodeModelMapWithDataImpl() in derived class.
* The virtual matrixExplodeModelMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixExplodeModelMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixExplodeModelMapFinished() or
* being returned as a callback parameter of matrixExplodeModelMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixExplodeModelMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixExplodeModelMap() operation calls.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixExplodeModelMap"][m_serverIndices.value("matrixExplodeModelMap")].serverUrl();
    QString fullPath = "/path/map/model-mapping/matrix-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, true, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        qWarning() << "Serialization of complex array or object properties in path or query "
                      "parameters is undefined. The generated result will not conform to the "
                      "OpenAPI standard.";
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixExplodeModelMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixExplodeModelMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixExplodeModelMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixExplodeModelMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixExplodeObject(const QtOAITestObject &objectParameter)
* 'matrixExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixExplodeObject(const QtOAITestObject &objectParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixExplodeObjectWithDataImpl() in derived class.
* The virtual matrixExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixExplodeObjectFinished() or
* being returned as a callback parameter of matrixExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixExplodeObject"][m_serverIndices.value("matrixExplodeObject")].serverUrl();
    QString fullPath = "/path/object/matrix-explode/{objectParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString objectParameterPathParam = QString("{%1}").arg("objectParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"objectParameter"_s, true, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeJsonValue(QJsonValue(objectParameter.asJsonObject()), pathStyle, true, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(objectParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixExplodeString(const QString &stringParameter)
* 'matrixExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameter QString [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixExplodeString(const QString &stringParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixExplodeString(stringParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixExplodeStringWithDataImpl() in derived class.
* The virtual matrixExplodeStringWithDataImpl() is being called by the template
* function.

* @param[in] stringParameter QString [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixExplodeStringCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixExplodeStringFinished() or
* being returned as a callback parameter of matrixExplodeString() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixExplodeString() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixExplodeString() operation calls.

* @param[in] stringParameter QString [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixExplodeString"][m_serverIndices.value("matrixExplodeString")].serverUrl();
    QString fullPath = "/path/string/matrix-explode/{stringParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString stringParameterPathParam = QString("{%1}").arg("stringParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"stringParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(stringParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixExplodeStringCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixExplodeStringErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixExplodeStringCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixExplodeStringFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixExplodeStringMap(const QMap<QString, QString> &mapParameter)
* 'matrixExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QString> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixExplodeStringMap(const QMap<QString, QString> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixExplodeStringMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixExplodeStringMapWithDataImpl() in derived class.
* The virtual matrixExplodeStringMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixExplodeStringMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixExplodeStringMapFinished() or
* being returned as a callback parameter of matrixExplodeStringMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixExplodeStringMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixExplodeStringMap() operation calls.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixExplodeStringMap"][m_serverIndices.value("matrixExplodeStringMap")].serverUrl();
    QString fullPath = "/path/map/string-mapping/matrix-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, true, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixExplodeStringMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixExplodeStringMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixExplodeStringMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixExplodeStringMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeAnytype(const QJsonValue &anytypeParameter)
* 'matrixNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixNotExplodeAnytype(const QJsonValue &anytypeParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixNotExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixNotExplodeAnytypeWithDataImpl() in derived class.
* The virtual matrixNotExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixNotExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixNotExplodeAnytypeFinished() or
* being returned as a callback parameter of matrixNotExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixNotExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixNotExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixNotExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixNotExplodeAnytype"][m_serverIndices.value("matrixNotExplodeAnytype")].serverUrl();
    QString fullPath = "/path/anytype/matrix-not-explode/{anytypeParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString anytypeParameterPathParam = QString("{%1}").arg("anytypeParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, anytypeParameter.type() == QJsonValue::Object);
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"anytypeParameter"_s, false, anytypeParameter.type() == QJsonValue::Object);
        QString paramString = pathPrefix + serializeJsonValue(anytypeParameter, pathStyle, false, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(anytypeParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixNotExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixNotExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixNotExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixNotExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeArray(const QList<qint32> &arrayParameter)
* 'matrixNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixNotExplodeArray(const QList<qint32> &arrayParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixNotExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixNotExplodeArrayWithDataImpl() in derived class.
* The virtual matrixNotExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixNotExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixNotExplodeArrayFinished() or
* being returned as a callback parameter of matrixNotExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixNotExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixNotExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixNotExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixNotExplodeArray"][m_serverIndices.value("matrixNotExplodeArray")].serverUrl();
    QString fullPath = "/path/array/matrix-not-explode/{arrayParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString arrayParameterPathParam = QString("{%1}").arg("arrayParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !true));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"arrayParameter"_s, false, (!false && !true));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeArrayValue(arrayParameter, pathStyle, false, pathSuffix, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(arrayParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixNotExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixNotExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixNotExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixNotExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeDouble(const double &doubleParameter)
* 'matrixNotExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] doubleParameter double [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixNotExplodeDouble(const double &doubleParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixNotExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixNotExplodeDouble(doubleParameter, this, [&](const QRestReply &reply, const QtOAIDoubleResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixNotExplodeDoubleWithDataImpl() in derived class.
* The virtual matrixNotExplodeDoubleWithDataImpl() is being called by the template
* function.

* @param[in] doubleParameter double [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixNotExplodeDoubleCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixNotExplodeDoubleFinished() or
* being returned as a callback parameter of matrixNotExplodeDouble() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixNotExplodeDouble() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixNotExplodeDouble() operation calls.

* @param[in] doubleParameter double [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixNotExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixNotExplodeDouble"][m_serverIndices.value("matrixNotExplodeDouble")].serverUrl();
    QString fullPath = "/path/double/matrix-not-explode/{doubleParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString doubleParameterPathParam = QString("{%1}").arg("doubleParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"doubleParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(doubleParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(doubleParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixNotExplodeDoubleCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixNotExplodeDoubleErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIDoubleResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixNotExplodeDoubleCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIDoubleResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixNotExplodeDoubleFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeFloat(const float &floatParameter)
* 'matrixNotExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] floatParameter float [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixNotExplodeFloat(const float &floatParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixNotExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixNotExplodeFloat(floatParameter, this, [&](const QRestReply &reply, const QtOAIFloatResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixNotExplodeFloatWithDataImpl() in derived class.
* The virtual matrixNotExplodeFloatWithDataImpl() is being called by the template
* function.

* @param[in] floatParameter float [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixNotExplodeFloatCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixNotExplodeFloatFinished() or
* being returned as a callback parameter of matrixNotExplodeFloat() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixNotExplodeFloat() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixNotExplodeFloat() operation calls.

* @param[in] floatParameter float [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixNotExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixNotExplodeFloat"][m_serverIndices.value("matrixNotExplodeFloat")].serverUrl();
    QString fullPath = "/path/float/matrix-not-explode/{floatParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString floatParameterPathParam = QString("{%1}").arg("floatParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"floatParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(floatParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(floatParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixNotExplodeFloatCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixNotExplodeFloatErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIFloatResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixNotExplodeFloatCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIFloatResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixNotExplodeFloatFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeInt(const qint64 &intParameter)
* 'matrixNotExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] intParameter qint64 [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixNotExplodeInt(const qint64 &intParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixNotExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixNotExplodeInt(intParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixNotExplodeIntWithDataImpl() in derived class.
* The virtual matrixNotExplodeIntWithDataImpl() is being called by the template
* function.

* @param[in] intParameter qint64 [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixNotExplodeIntCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixNotExplodeIntFinished() or
* being returned as a callback parameter of matrixNotExplodeInt() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixNotExplodeInt() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixNotExplodeInt() operation calls.

* @param[in] intParameter qint64 [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixNotExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixNotExplodeInt"][m_serverIndices.value("matrixNotExplodeInt")].serverUrl();
    QString fullPath = "/path/int/matrix-not-explode/{intParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString intParameterPathParam = QString("{%1}").arg("intParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"intParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(intParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(intParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixNotExplodeIntCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixNotExplodeIntErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixNotExplodeIntCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixNotExplodeIntFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter)
* 'matrixNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixNotExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixNotExplodeModelMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixNotExplodeModelMapWithDataImpl() in derived class.
* The virtual matrixNotExplodeModelMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixNotExplodeModelMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixNotExplodeModelMapFinished() or
* being returned as a callback parameter of matrixNotExplodeModelMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixNotExplodeModelMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixNotExplodeModelMap() operation calls.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixNotExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixNotExplodeModelMap"][m_serverIndices.value("matrixNotExplodeModelMap")].serverUrl();
    QString fullPath = "/path/map/model-mapping/matrix-not-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, false, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        qWarning() << "Serialization of complex array or object properties in path or query "
                      "parameters is undefined. The generated result will not conform to the "
                      "OpenAPI standard.";
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixNotExplodeModelMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixNotExplodeModelMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixNotExplodeModelMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixNotExplodeModelMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeObject(const QtOAITestObject &objectParameter)
* 'matrixNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixNotExplodeObject(const QtOAITestObject &objectParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixNotExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixNotExplodeObjectWithDataImpl() in derived class.
* The virtual matrixNotExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixNotExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixNotExplodeObjectFinished() or
* being returned as a callback parameter of matrixNotExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixNotExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixNotExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixNotExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixNotExplodeObject"][m_serverIndices.value("matrixNotExplodeObject")].serverUrl();
    QString fullPath = "/path/object/matrix-not-explode/{objectParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString objectParameterPathParam = QString("{%1}").arg("objectParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"objectParameter"_s, false, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeJsonValue(QJsonValue(objectParameter.asJsonObject()), pathStyle, false, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(objectParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixNotExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixNotExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixNotExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixNotExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeString(const QString &stringParameter)
* 'matrixNotExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameter QString [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixNotExplodeString(const QString &stringParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixNotExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixNotExplodeString(stringParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixNotExplodeStringWithDataImpl() in derived class.
* The virtual matrixNotExplodeStringWithDataImpl() is being called by the template
* function.

* @param[in] stringParameter QString [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixNotExplodeStringCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixNotExplodeStringFinished() or
* being returned as a callback parameter of matrixNotExplodeString() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixNotExplodeString() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixNotExplodeString() operation calls.

* @param[in] stringParameter QString [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixNotExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixNotExplodeString"][m_serverIndices.value("matrixNotExplodeString")].serverUrl();
    QString fullPath = "/path/string/matrix-not-explode/{stringParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString stringParameterPathParam = QString("{%1}").arg("stringParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"stringParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(stringParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixNotExplodeStringCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixNotExplodeStringErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixNotExplodeStringCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixNotExplodeStringFinished(output);
}

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeStringMap(const QMap<QString, QString> &mapParameter)
* 'matrixNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QString> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::matrixNotExplodeStringMap(const QMap<QString, QString> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'matrixNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    matrixNotExplodeStringMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual matrixNotExplodeStringMapWithDataImpl() in derived class.
* The virtual matrixNotExplodeStringMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::matrixNotExplodeStringMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by matrixNotExplodeStringMapFinished() or
* being returned as a callback parameter of matrixNotExplodeStringMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::matrixNotExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the matrixNotExplodeStringMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking matrixNotExplodeStringMap() operation calls.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::matrixNotExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["matrixNotExplodeStringMap"][m_serverIndices.value("matrixNotExplodeStringMap")].serverUrl();
    QString fullPath = "/path/map/string-mapping/matrix-not-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "matrix";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, false, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("matrix") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            matrixNotExplodeStringMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit matrixNotExplodeStringMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::matrixNotExplodeStringMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit matrixNotExplodeStringMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::pipeDelimitedExplodeAnytype(const QJsonValue &anytypeParameter)
* 'pipeDelimitedExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::pipeDelimitedExplodeAnytype(const QJsonValue &anytypeParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'pipeDelimitedExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    pipeDelimitedExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual pipeDelimitedExplodeAnytypeWithDataImpl() in derived class.
* The virtual pipeDelimitedExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::pipeDelimitedExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by pipeDelimitedExplodeAnytypeFinished() or
* being returned as a callback parameter of pipeDelimitedExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::pipeDelimitedExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the pipeDelimitedExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking pipeDelimitedExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::pipeDelimitedExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["pipeDelimitedExplodeAnytype"][m_serverIndices.value("pipeDelimitedExplodeAnytype")].serverUrl();
    QString fullPath = "/query/anytype/pipeDelimited-explode/pipeDelimitedExplodeAnytype";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "pipeDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"anytypeParameter"_s, true, anytypeParameter.type() == QJsonValue::Object);
        const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, anytypeParameter.type() == QJsonValue::Object);
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            paramString = serializeJsonValue(anytypeParameter, queryStyle, true, querySuffix, queryAssignOperator, queryDelimiter);
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            pipeDelimitedExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit pipeDelimitedExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::pipeDelimitedExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit pipeDelimitedExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::pipeDelimitedExplodeArray(const QList<qint32> &arrayParameter)
* 'pipeDelimitedExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::pipeDelimitedExplodeArray(const QList<qint32> &arrayParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'pipeDelimitedExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    pipeDelimitedExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual pipeDelimitedExplodeArrayWithDataImpl() in derived class.
* The virtual pipeDelimitedExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::pipeDelimitedExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by pipeDelimitedExplodeArrayFinished() or
* being returned as a callback parameter of pipeDelimitedExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::pipeDelimitedExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the pipeDelimitedExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking pipeDelimitedExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::pipeDelimitedExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["pipeDelimitedExplodeArray"][m_serverIndices.value("pipeDelimitedExplodeArray")].serverUrl();
    QString fullPath = "/query/array/pipeDelimited-explode/pipeDelimitedExplodeArray";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "pipeDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"arrayParameter"_s, true, (!false && !true));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !true));
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(serializeArrayValue(arrayParameter, queryStyle, true, querySuffix, queryDelimiter));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            pipeDelimitedExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit pipeDelimitedExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::pipeDelimitedExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit pipeDelimitedExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::pipeDelimitedExplodeObject(const QtOAITestObject &objectParameter)
* 'pipeDelimitedExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::pipeDelimitedExplodeObject(const QtOAITestObject &objectParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'pipeDelimitedExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    pipeDelimitedExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual pipeDelimitedExplodeObjectWithDataImpl() in derived class.
* The virtual pipeDelimitedExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::pipeDelimitedExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by pipeDelimitedExplodeObjectFinished() or
* being returned as a callback parameter of pipeDelimitedExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::pipeDelimitedExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the pipeDelimitedExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking pipeDelimitedExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::pipeDelimitedExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["pipeDelimitedExplodeObject"][m_serverIndices.value("pipeDelimitedExplodeObject")].serverUrl();
    QString fullPath = "/query/object/pipeDelimited-explode/pipeDelimitedExplodeObject";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "pipeDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"objectParameter"_s, true, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            const QJsonObject parameter = objectParameter.asJsonObject();
            if (queryStyle == "deepObject") {
                qsizetype index = 0;
                if (parameter.isEmpty())
                    qWarning() << "Serialized parameter objectParameter is empty!";
                for (const QString &key : parameter.keys()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"objectParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(convertJsonValueToString(parameter.value(key))));
                    index++;
                }
            } else {
                paramString = serializeJsonValue(QJsonValue(parameter), queryStyle, true, querySuffix, queryAssignOperator, queryDelimiter);
            }
            // style=form && explode=true && non-object => 'objectParameter' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'objectParameter='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (paramString.isEmpty() && queryStyle == "form")
                paramString = u"objectParameter="_s;
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            pipeDelimitedExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit pipeDelimitedExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::pipeDelimitedExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit pipeDelimitedExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::pipeDelimitedNotExplodeAnytype(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter = ::QtOpenAPI::OptionalParam<QJsonValue>())
* 'pipeDelimitedNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::pipeDelimitedNotExplodeAnytype(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter = ::QtOpenAPI::OptionalParam<QJsonValue>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'pipeDelimitedNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    pipeDelimitedNotExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual pipeDelimitedNotExplodeAnytypeWithDataImpl() in derived class.
* The virtual pipeDelimitedNotExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::pipeDelimitedNotExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by pipeDelimitedNotExplodeAnytypeFinished() or
* being returned as a callback parameter of pipeDelimitedNotExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::pipeDelimitedNotExplodeAnytypeWithDataImpl(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the pipeDelimitedNotExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking pipeDelimitedNotExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::pipeDelimitedNotExplodeAnytypeWithDataImpl(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["pipeDelimitedNotExplodeAnytype"][m_serverIndices.value("pipeDelimitedNotExplodeAnytype")].serverUrl();
    QString fullPath = "/query/anytype/pipeDelimited-not-explode/pipeDelimitedNotExplodeAnytype";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "pipeDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"anytypeParameter"_s, false, anytypeParameter.value().type() == QJsonValue::Object);
        const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, anytypeParameter.value().type() == QJsonValue::Object);
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (anytypeParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            paramString = serializeJsonValue(anytypeParameter.value(), queryStyle, false, querySuffix, queryAssignOperator, queryDelimiter);
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            pipeDelimitedNotExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit pipeDelimitedNotExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::pipeDelimitedNotExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit pipeDelimitedNotExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::pipeDelimitedNotExplodeArray(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter = ::QtOpenAPI::OptionalParam<QList<qint32>>())
* 'pipeDelimitedNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::pipeDelimitedNotExplodeArray(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter = ::QtOpenAPI::OptionalParam<QList<qint32>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'pipeDelimitedNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    pipeDelimitedNotExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual pipeDelimitedNotExplodeArrayWithDataImpl() in derived class.
* The virtual pipeDelimitedNotExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::pipeDelimitedNotExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by pipeDelimitedNotExplodeArrayFinished() or
* being returned as a callback parameter of pipeDelimitedNotExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::pipeDelimitedNotExplodeArrayWithDataImpl(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the pipeDelimitedNotExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking pipeDelimitedNotExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::pipeDelimitedNotExplodeArrayWithDataImpl(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["pipeDelimitedNotExplodeArray"][m_serverIndices.value("pipeDelimitedNotExplodeArray")].serverUrl();
    QString fullPath = "/query/array/pipeDelimited-not-explode/pipeDelimitedNotExplodeArray";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "pipeDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"arrayParameter"_s, false, (!false && !true));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !true));
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (arrayParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(serializeArrayValue(arrayParameter.value(), queryStyle, false, querySuffix, queryDelimiter));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            pipeDelimitedNotExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit pipeDelimitedNotExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::pipeDelimitedNotExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit pipeDelimitedNotExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::pipeDelimitedNotExplodeModelMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>>())
* 'pipeDelimitedNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::pipeDelimitedNotExplodeModelMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'pipeDelimitedNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    pipeDelimitedNotExplodeModelMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual pipeDelimitedNotExplodeModelMapWithDataImpl() in derived class.
* The virtual pipeDelimitedNotExplodeModelMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::pipeDelimitedNotExplodeModelMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by pipeDelimitedNotExplodeModelMapFinished() or
* being returned as a callback parameter of pipeDelimitedNotExplodeModelMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::pipeDelimitedNotExplodeModelMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the pipeDelimitedNotExplodeModelMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking pipeDelimitedNotExplodeModelMap() operation calls.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::pipeDelimitedNotExplodeModelMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["pipeDelimitedNotExplodeModelMap"][m_serverIndices.value("pipeDelimitedNotExplodeModelMap")].serverUrl();
    QString fullPath = "/query/map/model-mapping/pipeDelimited-not-explode/pipeDelimitedNotExplodeMap";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "pipeDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"mapParameter"_s, false, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (mapParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            qWarning() << "Serialization of complex array or object properties in path or query "
                          "parameters is undefined. The generated result will not conform to the"
                          " OpenAPI standard.";
            if (queryStyle == "deepObject"_L1) {
                qsizetype index = 0;
                if (mapParameter.value().isEmpty())
                    qWarning() << "Map is empty!";
                for (const auto &[key, value] : mapParameter.value().asKeyValueRange()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"mapParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(value)));
                    index++;
                }
            } else {
                paramString.append(serializeMapValue(mapParameter.value(), queryAssignOperator, queryDelimiter));
            }
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            pipeDelimitedNotExplodeModelMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit pipeDelimitedNotExplodeModelMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::pipeDelimitedNotExplodeModelMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit pipeDelimitedNotExplodeModelMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::pipeDelimitedNotExplodeObject(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter = ::QtOpenAPI::OptionalParam<QtOAITestObject>())
* 'pipeDelimitedNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::pipeDelimitedNotExplodeObject(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter = ::QtOpenAPI::OptionalParam<QtOAITestObject>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'pipeDelimitedNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    pipeDelimitedNotExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual pipeDelimitedNotExplodeObjectWithDataImpl() in derived class.
* The virtual pipeDelimitedNotExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::pipeDelimitedNotExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by pipeDelimitedNotExplodeObjectFinished() or
* being returned as a callback parameter of pipeDelimitedNotExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::pipeDelimitedNotExplodeObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the pipeDelimitedNotExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking pipeDelimitedNotExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::pipeDelimitedNotExplodeObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["pipeDelimitedNotExplodeObject"][m_serverIndices.value("pipeDelimitedNotExplodeObject")].serverUrl();
    QString fullPath = "/query/object/pipeDelimited-not-explode/pipeDelimitedNotExplodeObject";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "pipeDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"objectParameter"_s, false, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (objectParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            const QJsonObject parameter = objectParameter.value().asJsonObject();
            if (queryStyle == "deepObject") {
                qsizetype index = 0;
                if (parameter.isEmpty())
                    qWarning() << "Serialized parameter objectParameter is empty!";
                for (const QString &key : parameter.keys()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"objectParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(convertJsonValueToString(parameter.value(key))));
                    index++;
                }
            } else {
                paramString = serializeJsonValue(QJsonValue(parameter), queryStyle, false, querySuffix, queryAssignOperator, queryDelimiter);
            }
            // style=form && explode=true && non-object => 'objectParameter' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'objectParameter='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (paramString.isEmpty() && queryStyle == "form")
                paramString = u"objectParameter="_s;
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            pipeDelimitedNotExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit pipeDelimitedNotExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::pipeDelimitedNotExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit pipeDelimitedNotExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::pipeDelimitedNotExplodeStringMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QString>>())
* 'pipeDelimitedNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QString> [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::pipeDelimitedNotExplodeStringMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QString>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'pipeDelimitedNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    pipeDelimitedNotExplodeStringMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual pipeDelimitedNotExplodeStringMapWithDataImpl() in derived class.
* The virtual pipeDelimitedNotExplodeStringMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QString> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::pipeDelimitedNotExplodeStringMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by pipeDelimitedNotExplodeStringMapFinished() or
* being returned as a callback parameter of pipeDelimitedNotExplodeStringMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::pipeDelimitedNotExplodeStringMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the pipeDelimitedNotExplodeStringMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking pipeDelimitedNotExplodeStringMap() operation calls.

* @param[in] mapParameter QMap<QString, QString> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::pipeDelimitedNotExplodeStringMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["pipeDelimitedNotExplodeStringMap"][m_serverIndices.value("pipeDelimitedNotExplodeStringMap")].serverUrl();
    QString fullPath = "/query/map/string-mapping/pipeDelimited-not-explode/pipeDelimitedNotExplodeMap";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "pipeDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"mapParameter"_s, false, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (mapParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            if (queryStyle == "deepObject"_L1) {
                qsizetype index = 0;
                if (mapParameter.value().isEmpty())
                    qWarning() << "Map is empty!";
                for (const auto &[key, value] : mapParameter.value().asKeyValueRange()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"mapParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(value)));
                    index++;
                }
            } else {
                paramString.append(serializeMapValue(mapParameter.value(), queryAssignOperator, queryDelimiter));
            }
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            pipeDelimitedNotExplodeStringMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit pipeDelimitedNotExplodeStringMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::pipeDelimitedNotExplodeStringMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit pipeDelimitedNotExplodeStringMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleExplodeAnytype(const QJsonValue &anytypeParameter)
* 'simpleExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleExplodeAnytype(const QJsonValue &anytypeParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleExplodeAnytypeWithDataImpl() in derived class.
* The virtual simpleExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleExplodeAnytypeFinished() or
* being returned as a callback parameter of simpleExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleExplodeAnytype"][m_serverIndices.value("simpleExplodeAnytype")].serverUrl();
    QString fullPath = "/path/anytype/simple-explode/{anytypeParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString anytypeParameterPathParam = QString("{%1}").arg("anytypeParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, anytypeParameter.type() == QJsonValue::Object);
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"anytypeParameter"_s, true, anytypeParameter.type() == QJsonValue::Object);
        QString paramString = pathPrefix + serializeJsonValue(anytypeParameter, pathStyle, true, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(anytypeParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleExplodeArray(const QList<qint32> &arrayParameter)
* 'simpleExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleExplodeArray(const QList<qint32> &arrayParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleExplodeArrayWithDataImpl() in derived class.
* The virtual simpleExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleExplodeArrayFinished() or
* being returned as a callback parameter of simpleExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleExplodeArray"][m_serverIndices.value("simpleExplodeArray")].serverUrl();
    QString fullPath = "/path/array/simple-explode/{arrayParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString arrayParameterPathParam = QString("{%1}").arg("arrayParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !true));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"arrayParameter"_s, true, (!false && !true));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeArrayValue(arrayParameter, pathStyle, true, pathSuffix, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(arrayParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleExplodeDouble(const double &doubleParameter)
* 'simpleExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] doubleParameter double [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleExplodeDouble(const double &doubleParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleExplodeDouble(doubleParameter, this, [&](const QRestReply &reply, const QtOAIDoubleResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleExplodeDoubleWithDataImpl() in derived class.
* The virtual simpleExplodeDoubleWithDataImpl() is being called by the template
* function.

* @param[in] doubleParameter double [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleExplodeDoubleCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleExplodeDoubleFinished() or
* being returned as a callback parameter of simpleExplodeDouble() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleExplodeDouble() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleExplodeDouble() operation calls.

* @param[in] doubleParameter double [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleExplodeDouble"][m_serverIndices.value("simpleExplodeDouble")].serverUrl();
    QString fullPath = "/path/double/simple-explode/{doubleParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString doubleParameterPathParam = QString("{%1}").arg("doubleParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"doubleParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(doubleParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(doubleParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleExplodeDoubleCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleExplodeDoubleErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIDoubleResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleExplodeDoubleCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIDoubleResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleExplodeDoubleFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleExplodeFloat(const float &floatParameter)
* 'simpleExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] floatParameter float [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleExplodeFloat(const float &floatParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleExplodeFloat(floatParameter, this, [&](const QRestReply &reply, const QtOAIFloatResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleExplodeFloatWithDataImpl() in derived class.
* The virtual simpleExplodeFloatWithDataImpl() is being called by the template
* function.

* @param[in] floatParameter float [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleExplodeFloatCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleExplodeFloatFinished() or
* being returned as a callback parameter of simpleExplodeFloat() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleExplodeFloat() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleExplodeFloat() operation calls.

* @param[in] floatParameter float [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleExplodeFloat"][m_serverIndices.value("simpleExplodeFloat")].serverUrl();
    QString fullPath = "/path/float/simple-explode/{floatParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString floatParameterPathParam = QString("{%1}").arg("floatParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"floatParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(floatParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(floatParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleExplodeFloatCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleExplodeFloatErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIFloatResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleExplodeFloatCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIFloatResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleExplodeFloatFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleExplodeInt(const qint64 &intParameter)
* 'simpleExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] intParameter qint64 [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleExplodeInt(const qint64 &intParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleExplodeInt(intParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleExplodeIntWithDataImpl() in derived class.
* The virtual simpleExplodeIntWithDataImpl() is being called by the template
* function.

* @param[in] intParameter qint64 [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleExplodeIntCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleExplodeIntFinished() or
* being returned as a callback parameter of simpleExplodeInt() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleExplodeInt() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleExplodeInt() operation calls.

* @param[in] intParameter qint64 [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleExplodeInt"][m_serverIndices.value("simpleExplodeInt")].serverUrl();
    QString fullPath = "/path/int/simple-explode/{intParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString intParameterPathParam = QString("{%1}").arg("intParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"intParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(intParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(intParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleExplodeIntCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleExplodeIntErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleExplodeIntCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleExplodeIntFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter)
* 'simpleExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleExplodeModelMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleExplodeModelMapWithDataImpl() in derived class.
* The virtual simpleExplodeModelMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleExplodeModelMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleExplodeModelMapFinished() or
* being returned as a callback parameter of simpleExplodeModelMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleExplodeModelMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleExplodeModelMap() operation calls.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleExplodeModelMap"][m_serverIndices.value("simpleExplodeModelMap")].serverUrl();
    QString fullPath = "/path/map/model-mapping/simple-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, true, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        qWarning() << "Serialization of complex array or object properties in path or query "
                      "parameters is undefined. The generated result will not conform to the "
                      "OpenAPI standard.";
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleExplodeModelMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleExplodeModelMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleExplodeModelMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleExplodeModelMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleExplodeObject(const QtOAITestObject &objectParameter)
* 'simpleExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleExplodeObject(const QtOAITestObject &objectParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleExplodeObjectWithDataImpl() in derived class.
* The virtual simpleExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleExplodeObjectFinished() or
* being returned as a callback parameter of simpleExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleExplodeObject"][m_serverIndices.value("simpleExplodeObject")].serverUrl();
    QString fullPath = "/path/object/simple-explode/{objectParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString objectParameterPathParam = QString("{%1}").arg("objectParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"objectParameter"_s, true, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeJsonValue(QJsonValue(objectParameter.asJsonObject()), pathStyle, true, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(objectParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleExplodeString(const QString &stringParameter)
* 'simpleExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameter QString [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleExplodeString(const QString &stringParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleExplodeString(stringParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleExplodeStringWithDataImpl() in derived class.
* The virtual simpleExplodeStringWithDataImpl() is being called by the template
* function.

* @param[in] stringParameter QString [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleExplodeStringCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleExplodeStringFinished() or
* being returned as a callback parameter of simpleExplodeString() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleExplodeString() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleExplodeString() operation calls.

* @param[in] stringParameter QString [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleExplodeString"][m_serverIndices.value("simpleExplodeString")].serverUrl();
    QString fullPath = "/path/string/simple-explode/{stringParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString stringParameterPathParam = QString("{%1}").arg("stringParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"stringParameter"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(stringParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleExplodeStringCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleExplodeStringErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleExplodeStringCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleExplodeStringFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleExplodeStringMap(const QMap<QString, QString> &mapParameter)
* 'simpleExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QString> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleExplodeStringMap(const QMap<QString, QString> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleExplodeStringMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleExplodeStringMapWithDataImpl() in derived class.
* The virtual simpleExplodeStringMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleExplodeStringMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleExplodeStringMapFinished() or
* being returned as a callback parameter of simpleExplodeStringMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleExplodeStringMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleExplodeStringMap() operation calls.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleExplodeStringMap"][m_serverIndices.value("simpleExplodeStringMap")].serverUrl();
    QString fullPath = "/path/map/string-mapping/simple-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, true, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleExplodeStringMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleExplodeStringMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleExplodeStringMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleExplodeStringMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleExplodeStrings(const QString &stringParameter1, const QString &stringParameter2)
* 'simpleExplodeStrings' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameter1 QString [required]
* @param[in] stringParameter2 QString [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleExplodeStrings(const QString &stringParameter1, const QString &stringParameter2, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleExplodeStrings' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleExplodeStrings(stringParameter1, stringParameter2, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleExplodeStringsWithDataImpl() in derived class.
* The virtual simpleExplodeStringsWithDataImpl() is being called by the template
* function.

* @param[in] stringParameter1 QString [required]
* @param[in] stringParameter2 QString [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleExplodeStringsCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleExplodeStringsFinished() or
* being returned as a callback parameter of simpleExplodeStrings() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleExplodeStringsWithDataImpl(const QString &stringParameter1, const QString &stringParameter2, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleExplodeStrings() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleExplodeStrings() operation calls.

* @param[in] stringParameter1 QString [required]
* @param[in] stringParameter2 QString [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleExplodeStringsWithDataImpl(const QString &stringParameter1, const QString &stringParameter2, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleExplodeStrings"][m_serverIndices.value("simpleExplodeStrings")].serverUrl();
    QString fullPath = "/path/strings/simple-explode/{stringParameter1}/{stringParameter2}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString stringParameter1PathParam = QString("{%1}").arg("stringParameter1");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"stringParameter1"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameter1));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(stringParameter1PathParam, paramString);
    }
    {
        QString stringParameter2PathParam = QString("{%1}").arg("stringParameter2");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, true);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, true, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"stringParameter2"_s, true, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameter2));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(stringParameter2PathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleExplodeStringsCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleExplodeStringsErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleExplodeStringsCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleExplodeStringsFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeAnytype(const QJsonValue &anytypeParameter)
* 'simpleNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleNotExplodeAnytype(const QJsonValue &anytypeParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleNotExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleNotExplodeAnytypeWithDataImpl() in derived class.
* The virtual simpleNotExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleNotExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleNotExplodeAnytypeFinished() or
* being returned as a callback parameter of simpleNotExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleNotExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleNotExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleNotExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleNotExplodeAnytype"][m_serverIndices.value("simpleNotExplodeAnytype")].serverUrl();
    QString fullPath = "/path/anytype/simple-not-explode/{anytypeParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString anytypeParameterPathParam = QString("{%1}").arg("anytypeParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, anytypeParameter.type() == QJsonValue::Object);
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"anytypeParameter"_s, false, anytypeParameter.type() == QJsonValue::Object);
        QString paramString = pathPrefix + serializeJsonValue(anytypeParameter, pathStyle, false, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(anytypeParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleNotExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleNotExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleNotExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleNotExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeArray(const QList<qint32> &arrayParameter)
* 'simpleNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleNotExplodeArray(const QList<qint32> &arrayParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleNotExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleNotExplodeArrayWithDataImpl() in derived class.
* The virtual simpleNotExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleNotExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleNotExplodeArrayFinished() or
* being returned as a callback parameter of simpleNotExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleNotExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleNotExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleNotExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleNotExplodeArray"][m_serverIndices.value("simpleNotExplodeArray")].serverUrl();
    QString fullPath = "/path/array/simple-not-explode/{arrayParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString arrayParameterPathParam = QString("{%1}").arg("arrayParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !true));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"arrayParameter"_s, false, (!false && !true));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeArrayValue(arrayParameter, pathStyle, false, pathSuffix, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(arrayParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleNotExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleNotExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleNotExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleNotExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeDouble(const double &doubleParameter)
* 'simpleNotExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] doubleParameter double [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleNotExplodeDouble(const double &doubleParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleNotExplodeDouble' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleNotExplodeDouble(doubleParameter, this, [&](const QRestReply &reply, const QtOAIDoubleResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleNotExplodeDoubleWithDataImpl() in derived class.
* The virtual simpleNotExplodeDoubleWithDataImpl() is being called by the template
* function.

* @param[in] doubleParameter double [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleNotExplodeDoubleCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleNotExplodeDoubleFinished() or
* being returned as a callback parameter of simpleNotExplodeDouble() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleNotExplodeDouble() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleNotExplodeDouble() operation calls.

* @param[in] doubleParameter double [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleNotExplodeDoubleWithDataImpl(const double &doubleParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleNotExplodeDouble"][m_serverIndices.value("simpleNotExplodeDouble")].serverUrl();
    QString fullPath = "/path/double/simple-not-explode/{doubleParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString doubleParameterPathParam = QString("{%1}").arg("doubleParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"doubleParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(doubleParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(doubleParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleNotExplodeDoubleCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleNotExplodeDoubleErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIDoubleResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleNotExplodeDoubleCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIDoubleResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleNotExplodeDoubleFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeFloat(const float &floatParameter)
* 'simpleNotExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] floatParameter float [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleNotExplodeFloat(const float &floatParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleNotExplodeFloat' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleNotExplodeFloat(floatParameter, this, [&](const QRestReply &reply, const QtOAIFloatResponse &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleNotExplodeFloatWithDataImpl() in derived class.
* The virtual simpleNotExplodeFloatWithDataImpl() is being called by the template
* function.

* @param[in] floatParameter float [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleNotExplodeFloatCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleNotExplodeFloatFinished() or
* being returned as a callback parameter of simpleNotExplodeFloat() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleNotExplodeFloat() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleNotExplodeFloat() operation calls.

* @param[in] floatParameter float [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleNotExplodeFloatWithDataImpl(const float &floatParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleNotExplodeFloat"][m_serverIndices.value("simpleNotExplodeFloat")].serverUrl();
    QString fullPath = "/path/float/simple-not-explode/{floatParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString floatParameterPathParam = QString("{%1}").arg("floatParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"floatParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(floatParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(floatParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleNotExplodeFloatCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleNotExplodeFloatErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIFloatResponse empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleNotExplodeFloatCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIFloatResponse output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleNotExplodeFloatFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeInt(const qint64 &intParameter)
* 'simpleNotExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] intParameter qint64 [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleNotExplodeInt(const qint64 &intParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleNotExplodeInt' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleNotExplodeInt(intParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleNotExplodeIntWithDataImpl() in derived class.
* The virtual simpleNotExplodeIntWithDataImpl() is being called by the template
* function.

* @param[in] intParameter qint64 [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleNotExplodeIntCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleNotExplodeIntFinished() or
* being returned as a callback parameter of simpleNotExplodeInt() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleNotExplodeInt() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleNotExplodeInt() operation calls.

* @param[in] intParameter qint64 [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleNotExplodeIntWithDataImpl(const qint64 &intParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleNotExplodeInt"][m_serverIndices.value("simpleNotExplodeInt")].serverUrl();
    QString fullPath = "/path/int/simple-not-explode/{intParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString intParameterPathParam = QString("{%1}").arg("intParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"intParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(intParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(intParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleNotExplodeIntCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleNotExplodeIntErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleNotExplodeIntCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleNotExplodeIntFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter)
* 'simpleNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleNotExplodeModelMap(const QMap<QString, QtOAITestObject> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleNotExplodeModelMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleNotExplodeModelMapWithDataImpl() in derived class.
* The virtual simpleNotExplodeModelMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleNotExplodeModelMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleNotExplodeModelMapFinished() or
* being returned as a callback parameter of simpleNotExplodeModelMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleNotExplodeModelMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleNotExplodeModelMap() operation calls.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleNotExplodeModelMapWithDataImpl(const QMap<QString, QtOAITestObject> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleNotExplodeModelMap"][m_serverIndices.value("simpleNotExplodeModelMap")].serverUrl();
    QString fullPath = "/path/map/model-mapping/simple-not-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, false, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        qWarning() << "Serialization of complex array or object properties in path or query "
                      "parameters is undefined. The generated result will not conform to the "
                      "OpenAPI standard.";
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleNotExplodeModelMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleNotExplodeModelMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleNotExplodeModelMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleNotExplodeModelMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeObject(const QtOAITestObject &objectParameter)
* 'simpleNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleNotExplodeObject(const QtOAITestObject &objectParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleNotExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleNotExplodeObjectWithDataImpl() in derived class.
* The virtual simpleNotExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleNotExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleNotExplodeObjectFinished() or
* being returned as a callback parameter of simpleNotExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleNotExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleNotExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleNotExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleNotExplodeObject"][m_serverIndices.value("simpleNotExplodeObject")].serverUrl();
    QString fullPath = "/path/object/simple-not-explode/{objectParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString objectParameterPathParam = QString("{%1}").arg("objectParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"objectParameter"_s, false, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString = pathPrefix + serializeJsonValue(QJsonValue(objectParameter.asJsonObject()), pathStyle, false, pathSuffix, assignOperator, pathDelimiter);
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(objectParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleNotExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleNotExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleNotExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleNotExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeString(const QString &stringParameter)
* 'simpleNotExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] stringParameter QString [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleNotExplodeString(const QString &stringParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleNotExplodeString' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleNotExplodeString(stringParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleNotExplodeStringWithDataImpl() in derived class.
* The virtual simpleNotExplodeStringWithDataImpl() is being called by the template
* function.

* @param[in] stringParameter QString [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleNotExplodeStringCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleNotExplodeStringFinished() or
* being returned as a callback parameter of simpleNotExplodeString() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleNotExplodeString() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleNotExplodeString() operation calls.

* @param[in] stringParameter QString [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleNotExplodeStringWithDataImpl(const QString &stringParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleNotExplodeString"][m_serverIndices.value("simpleNotExplodeString")].serverUrl();
    QString fullPath = "/path/string/simple-not-explode/{stringParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString stringParameterPathParam = QString("{%1}").arg("stringParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"stringParameter"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(stringParameter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(stringParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleNotExplodeStringCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleNotExplodeStringErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleNotExplodeStringCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleNotExplodeStringFinished(output);
}

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeStringMap(const QMap<QString, QString> &mapParameter)
* 'simpleNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QString> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::simpleNotExplodeStringMap(const QMap<QString, QString> &mapParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'simpleNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    simpleNotExplodeStringMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual simpleNotExplodeStringMapWithDataImpl() in derived class.
* The virtual simpleNotExplodeStringMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::simpleNotExplodeStringMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by simpleNotExplodeStringMapFinished() or
* being returned as a callback parameter of simpleNotExplodeStringMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::simpleNotExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the simpleNotExplodeStringMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking simpleNotExplodeStringMap() operation calls.

* @param[in] mapParameter QMap<QString, QString> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::simpleNotExplodeStringMapWithDataImpl(const QMap<QString, QString> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["simpleNotExplodeStringMap"][m_serverIndices.value("simpleNotExplodeStringMap")].serverUrl();
    QString fullPath = "/path/map/string-mapping/simple-not-explode/{mapParameter}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString mapParameterPathParam = QString("{%1}").arg("mapParameter");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!false && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"mapParameter"_s, false, (!false && !false));
        QString paramString = pathPrefix + pathSuffix;
        paramString.append(serializeMapValue(mapParameter, assignOperator, pathDelimiter));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(mapParameterPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            simpleNotExplodeStringMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit simpleNotExplodeStringMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::simpleNotExplodeStringMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit simpleNotExplodeStringMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::spaceDelimitedExplodeAnytype(const QJsonValue &anytypeParameter)
* 'spaceDelimitedExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::spaceDelimitedExplodeAnytype(const QJsonValue &anytypeParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'spaceDelimitedExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    spaceDelimitedExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual spaceDelimitedExplodeAnytypeWithDataImpl() in derived class.
* The virtual spaceDelimitedExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::spaceDelimitedExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by spaceDelimitedExplodeAnytypeFinished() or
* being returned as a callback parameter of spaceDelimitedExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::spaceDelimitedExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the spaceDelimitedExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking spaceDelimitedExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::spaceDelimitedExplodeAnytypeWithDataImpl(const QJsonValue &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["spaceDelimitedExplodeAnytype"][m_serverIndices.value("spaceDelimitedExplodeAnytype")].serverUrl();
    QString fullPath = "/query/anytype/spaceDelimited-explode/spaceDelimitedExplodeAnytype";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "spaceDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"anytypeParameter"_s, true, anytypeParameter.type() == QJsonValue::Object);
        const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, anytypeParameter.type() == QJsonValue::Object);
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            paramString = serializeJsonValue(anytypeParameter, queryStyle, true, querySuffix, queryAssignOperator, queryDelimiter);
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            spaceDelimitedExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit spaceDelimitedExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::spaceDelimitedExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit spaceDelimitedExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::spaceDelimitedExplodeArray(const QList<qint32> &arrayParameter)
* 'spaceDelimitedExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::spaceDelimitedExplodeArray(const QList<qint32> &arrayParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'spaceDelimitedExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    spaceDelimitedExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual spaceDelimitedExplodeArrayWithDataImpl() in derived class.
* The virtual spaceDelimitedExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::spaceDelimitedExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by spaceDelimitedExplodeArrayFinished() or
* being returned as a callback parameter of spaceDelimitedExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::spaceDelimitedExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the spaceDelimitedExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking spaceDelimitedExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::spaceDelimitedExplodeArrayWithDataImpl(const QList<qint32> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["spaceDelimitedExplodeArray"][m_serverIndices.value("spaceDelimitedExplodeArray")].serverUrl();
    QString fullPath = "/query/array/spaceDelimited-explode/spaceDelimitedExplodeArray";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "spaceDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"arrayParameter"_s, true, (!false && !true));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !true));
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(serializeArrayValue(arrayParameter, queryStyle, true, querySuffix, queryDelimiter));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            spaceDelimitedExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit spaceDelimitedExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::spaceDelimitedExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit spaceDelimitedExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::spaceDelimitedExplodeObject(const QtOAITestObject &objectParameter)
* 'spaceDelimitedExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::spaceDelimitedExplodeObject(const QtOAITestObject &objectParameter, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'spaceDelimitedExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    spaceDelimitedExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual spaceDelimitedExplodeObjectWithDataImpl() in derived class.
* The virtual spaceDelimitedExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::spaceDelimitedExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by spaceDelimitedExplodeObjectFinished() or
* being returned as a callback parameter of spaceDelimitedExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::spaceDelimitedExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the spaceDelimitedExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking spaceDelimitedExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::spaceDelimitedExplodeObjectWithDataImpl(const QtOAITestObject &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["spaceDelimitedExplodeObject"][m_serverIndices.value("spaceDelimitedExplodeObject")].serverUrl();
    QString fullPath = "/query/object/spaceDelimited-explode/spaceDelimitedExplodeObject";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "spaceDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"objectParameter"_s, true, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        {
            if (queryParamCounter > 0)
                fullPath.append("&");
            const QJsonObject parameter = objectParameter.asJsonObject();
            if (queryStyle == "deepObject") {
                qsizetype index = 0;
                if (parameter.isEmpty())
                    qWarning() << "Serialized parameter objectParameter is empty!";
                for (const QString &key : parameter.keys()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"objectParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(convertJsonValueToString(parameter.value(key))));
                    index++;
                }
            } else {
                paramString = serializeJsonValue(QJsonValue(parameter), queryStyle, true, querySuffix, queryAssignOperator, queryDelimiter);
            }
            // style=form && explode=true && non-object => 'objectParameter' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'objectParameter='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (paramString.isEmpty() && queryStyle == "form")
                paramString = u"objectParameter="_s;
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            spaceDelimitedExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit spaceDelimitedExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::spaceDelimitedExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit spaceDelimitedExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::spaceDelimitedNotExplodeAnytype(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter = ::QtOpenAPI::OptionalParam<QJsonValue>())
* 'spaceDelimitedNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] anytypeParameter QJsonValue [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::spaceDelimitedNotExplodeAnytype(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter = ::QtOpenAPI::OptionalParam<QJsonValue>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'spaceDelimitedNotExplodeAnytype' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    spaceDelimitedNotExplodeAnytype(anytypeParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual spaceDelimitedNotExplodeAnytypeWithDataImpl() in derived class.
* The virtual spaceDelimitedNotExplodeAnytypeWithDataImpl() is being called by the template
* function.

* @param[in] anytypeParameter QJsonValue [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::spaceDelimitedNotExplodeAnytypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by spaceDelimitedNotExplodeAnytypeFinished() or
* being returned as a callback parameter of spaceDelimitedNotExplodeAnytype() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::spaceDelimitedNotExplodeAnytypeWithDataImpl(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the spaceDelimitedNotExplodeAnytype() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking spaceDelimitedNotExplodeAnytype() operation calls.

* @param[in] anytypeParameter QJsonValue [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::spaceDelimitedNotExplodeAnytypeWithDataImpl(const ::QtOpenAPI::OptionalParam<QJsonValue> &anytypeParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["spaceDelimitedNotExplodeAnytype"][m_serverIndices.value("spaceDelimitedNotExplodeAnytype")].serverUrl();
    QString fullPath = "/query/anytype/spaceDelimited-not-explode/spaceDelimitedNotExplodeAnytype";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "spaceDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"anytypeParameter"_s, false, anytypeParameter.value().type() == QJsonValue::Object);
        const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, anytypeParameter.value().type() == QJsonValue::Object);
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (anytypeParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            paramString = serializeJsonValue(anytypeParameter.value(), queryStyle, false, querySuffix, queryAssignOperator, queryDelimiter);
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            spaceDelimitedNotExplodeAnytypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit spaceDelimitedNotExplodeAnytypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::spaceDelimitedNotExplodeAnytypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit spaceDelimitedNotExplodeAnytypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::spaceDelimitedNotExplodeArray(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter = ::QtOpenAPI::OptionalParam<QList<qint32>>())
* 'spaceDelimitedNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] arrayParameter QList<qint32> [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::spaceDelimitedNotExplodeArray(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter = ::QtOpenAPI::OptionalParam<QList<qint32>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'spaceDelimitedNotExplodeArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    spaceDelimitedNotExplodeArray(arrayParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual spaceDelimitedNotExplodeArrayWithDataImpl() in derived class.
* The virtual spaceDelimitedNotExplodeArrayWithDataImpl() is being called by the template
* function.

* @param[in] arrayParameter QList<qint32> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::spaceDelimitedNotExplodeArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by spaceDelimitedNotExplodeArrayFinished() or
* being returned as a callback parameter of spaceDelimitedNotExplodeArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::spaceDelimitedNotExplodeArrayWithDataImpl(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the spaceDelimitedNotExplodeArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking spaceDelimitedNotExplodeArray() operation calls.

* @param[in] arrayParameter QList<qint32> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::spaceDelimitedNotExplodeArrayWithDataImpl(const ::QtOpenAPI::OptionalParam<QList<qint32>> &arrayParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["spaceDelimitedNotExplodeArray"][m_serverIndices.value("spaceDelimitedNotExplodeArray")].serverUrl();
    QString fullPath = "/query/array/spaceDelimited-not-explode/spaceDelimitedNotExplodeArray";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "spaceDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"arrayParameter"_s, false, (!false && !true));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !true));
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (arrayParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(serializeArrayValue(arrayParameter.value(), queryStyle, false, querySuffix, queryDelimiter));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            spaceDelimitedNotExplodeArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit spaceDelimitedNotExplodeArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::spaceDelimitedNotExplodeArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit spaceDelimitedNotExplodeArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::spaceDelimitedNotExplodeModelMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>>())
* 'spaceDelimitedNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::spaceDelimitedNotExplodeModelMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'spaceDelimitedNotExplodeModelMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    spaceDelimitedNotExplodeModelMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual spaceDelimitedNotExplodeModelMapWithDataImpl() in derived class.
* The virtual spaceDelimitedNotExplodeModelMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::spaceDelimitedNotExplodeModelMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by spaceDelimitedNotExplodeModelMapFinished() or
* being returned as a callback parameter of spaceDelimitedNotExplodeModelMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::spaceDelimitedNotExplodeModelMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the spaceDelimitedNotExplodeModelMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking spaceDelimitedNotExplodeModelMap() operation calls.

* @param[in] mapParameter QMap<QString, QtOAITestObject> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::spaceDelimitedNotExplodeModelMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAITestObject>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["spaceDelimitedNotExplodeModelMap"][m_serverIndices.value("spaceDelimitedNotExplodeModelMap")].serverUrl();
    QString fullPath = "/query/map/model-mapping/spaceDelimited-not-explode/spaceDelimitedNotExplodeMap";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "spaceDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"mapParameter"_s, false, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (mapParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            qWarning() << "Serialization of complex array or object properties in path or query "
                          "parameters is undefined. The generated result will not conform to the"
                          " OpenAPI standard.";
            if (queryStyle == "deepObject"_L1) {
                qsizetype index = 0;
                if (mapParameter.value().isEmpty())
                    qWarning() << "Map is empty!";
                for (const auto &[key, value] : mapParameter.value().asKeyValueRange()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"mapParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(value)));
                    index++;
                }
            } else {
                paramString.append(serializeMapValue(mapParameter.value(), queryAssignOperator, queryDelimiter));
            }
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            spaceDelimitedNotExplodeModelMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit spaceDelimitedNotExplodeModelMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::spaceDelimitedNotExplodeModelMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit spaceDelimitedNotExplodeModelMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::spaceDelimitedNotExplodeObject(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter = ::QtOpenAPI::OptionalParam<QtOAITestObject>())
* 'spaceDelimitedNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] objectParameter QtOAITestObject [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::spaceDelimitedNotExplodeObject(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter = ::QtOpenAPI::OptionalParam<QtOAITestObject>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'spaceDelimitedNotExplodeObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    spaceDelimitedNotExplodeObject(objectParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual spaceDelimitedNotExplodeObjectWithDataImpl() in derived class.
* The virtual spaceDelimitedNotExplodeObjectWithDataImpl() is being called by the template
* function.

* @param[in] objectParameter QtOAITestObject [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::spaceDelimitedNotExplodeObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by spaceDelimitedNotExplodeObjectFinished() or
* being returned as a callback parameter of spaceDelimitedNotExplodeObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::spaceDelimitedNotExplodeObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the spaceDelimitedNotExplodeObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking spaceDelimitedNotExplodeObject() operation calls.

* @param[in] objectParameter QtOAITestObject [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::spaceDelimitedNotExplodeObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAITestObject> &objectParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["spaceDelimitedNotExplodeObject"][m_serverIndices.value("spaceDelimitedNotExplodeObject")].serverUrl();
    QString fullPath = "/query/object/spaceDelimited-not-explode/spaceDelimitedNotExplodeObject";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "spaceDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"objectParameter"_s, false, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (objectParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            const QJsonObject parameter = objectParameter.value().asJsonObject();
            if (queryStyle == "deepObject") {
                qsizetype index = 0;
                if (parameter.isEmpty())
                    qWarning() << "Serialized parameter objectParameter is empty!";
                for (const QString &key : parameter.keys()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"objectParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(convertJsonValueToString(parameter.value(key))));
                    index++;
                }
            } else {
                paramString = serializeJsonValue(QJsonValue(parameter), queryStyle, false, querySuffix, queryAssignOperator, queryDelimiter);
            }
            // style=form && explode=true && non-object => 'objectParameter' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'objectParameter='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (paramString.isEmpty() && queryStyle == "form")
                paramString = u"objectParameter="_s;
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            spaceDelimitedNotExplodeObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit spaceDelimitedNotExplodeObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::spaceDelimitedNotExplodeObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit spaceDelimitedNotExplodeObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::spaceDelimitedNotExplodeStringMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QString>>())
* 'spaceDelimitedNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] mapParameter QMap<QString, QString> [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::spaceDelimitedNotExplodeStringMap(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter = ::QtOpenAPI::OptionalParam<QMap<QString, QString>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'spaceDelimitedNotExplodeStringMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    spaceDelimitedNotExplodeStringMap(mapParameter, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual spaceDelimitedNotExplodeStringMapWithDataImpl() in derived class.
* The virtual spaceDelimitedNotExplodeStringMapWithDataImpl() is being called by the template
* function.

* @param[in] mapParameter QMap<QString, QString> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::spaceDelimitedNotExplodeStringMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by spaceDelimitedNotExplodeStringMapFinished() or
* being returned as a callback parameter of spaceDelimitedNotExplodeStringMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::spaceDelimitedNotExplodeStringMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the spaceDelimitedNotExplodeStringMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking spaceDelimitedNotExplodeStringMap() operation calls.

* @param[in] mapParameter QMap<QString, QString> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::spaceDelimitedNotExplodeStringMapWithDataImpl(const ::QtOpenAPI::OptionalParam<QMap<QString, QString>> &mapParameter, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["spaceDelimitedNotExplodeStringMap"][m_serverIndices.value("spaceDelimitedNotExplodeStringMap")].serverUrl();
    QString fullPath = "/query/map/string-mapping/spaceDelimited-not-explode/spaceDelimitedNotExplodeMap";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "spaceDelimited";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, false);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"mapParameter"_s, false, (!false && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, false, (!false && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (mapParameter.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            if (queryStyle == "deepObject"_L1) {
                qsizetype index = 0;
                if (mapParameter.value().isEmpty())
                    qWarning() << "Map is empty!";
                for (const auto &[key, value] : mapParameter.value().asKeyValueRange()) {
                    if (index > 0)
                        paramString.append(queryDelimiter);
                    paramString.append(QUrl::toPercentEncoding(u"mapParameter[%1]"_s.arg(key)) + queryAssignOperator + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(value)));
                    index++;
                }
            } else {
                paramString.append(serializeMapValue(mapParameter.value(), queryAssignOperator, queryDelimiter));
            }
            fullPath.append(paramString);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            spaceDelimitedNotExplodeStringMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit spaceDelimitedNotExplodeStringMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::spaceDelimitedNotExplodeStringMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit spaceDelimitedNotExplodeStringMapFinished(output);
}

} // namespace QtOpenAPI
