/**
 * Qt Quick Demo - RESTful API client
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "QtOAIUsersApi.h"
#include "QtOAIServerConfiguration.h"

#include <QtCore/qanystringview.h>
#include <QtCore/qjsonarray.h>
#include <QtCore/qjsondocument.h>
#include <QtNetwork/qhttpmultipart.h>
#include <QtNetwork/qnetworkaccessmanager.h>
#include <QtNetwork/qnetworkreply.h>
#include <QtNetwork/qnetworkrequestfactory.h>
#include <QtNetwork/qrestaccessmanager.h>

using namespace Qt::StringLiterals;

namespace QtOpenAPI {

QtOAIUsersApi::QtOAIUsersApi(QObject *parent)
    : QtOAIBaseApi(parent)
{
    initializeServerConfigs();
}

QtOAIUsersApi::~QtOAIUsersApi()
{
}

void QtOAIUsersApi::initializeServerConfigs()
{
    //Default server
    QList<QtOAIServerConfiguration> defaultConf = QList<QtOAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(QtOAIServerConfiguration(
    QUrl("https://reqres.in/api"),
    "Public REST API test service",
    QMap<QString, QtOAIServerVariable>()));
    defaultConf.append(QtOAIServerConfiguration(
    QUrl("http://127.0.0.1:49425/api"),
    "Qt-based REST API server",
    QMap<QString, QtOAIServerVariable>()));
    m_serverConfigs.insert("getUserById", defaultConf);
    m_serverIndices.insert("getUserById", 0);
    m_serverConfigs.insert("getUsersByPage", defaultConf);
    m_serverIndices.insert("getUsersByPage", 0);
    m_serverConfigs.insert("loginUser", defaultConf);
    m_serverIndices.insert("loginUser", 0);
    m_serverConfigs.insert("logoutUser", defaultConf);
    m_serverIndices.insert("logoutUser", 0);
    m_serverConfigs.insert("updateUser", defaultConf);
    m_serverIndices.insert("updateUser", 0);
}

/**
* \fn virtual void QtOAIUsersApi::getUserById(const qint64 &id)
* 'getUserById' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] id qint64 [required]
*/

/**
* \fn template < Functor, > void QtOAIUsersApi::getUserById(const qint64 &id, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'getUserById' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    getUserById(id, this, [&](const QRestReply &reply, const QtOAIUser &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual getUserByIdWithDataImpl() in derived class.
* The virtual getUserByIdWithDataImpl() is being called by the template
* function.

* @param[in] id qint64 [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAIUsersApi::getUserByIdCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by getUserByIdFinished() or
* being returned as a callback parameter of getUserById() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAIUsersApi::getUserByIdWithDataImpl(const qint64 &id, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the getUserById() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking getUserById() operation calls.

* @param[in] id qint64 [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAIUsersApi::getUserByIdWithDataImpl(const qint64 &id, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["getUserById"][m_serverIndices.value("getUserById")].serverUrl();
    QString fullPath = "/users/{id}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString idPathParam = QString("{%1}").arg("id");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        const bool isObject = false || false;
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, isObject);
        const QString pathSuffix = getParamStyleSuffix(pathStyle, QUrl::toPercentEncoding(u"id"_s), false, isObject);
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(id));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(idPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            getUserByIdCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit getUserByIdErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIUser empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAIUsersApi::getUserByIdCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIUser output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit getUserByIdFinished(output);
}

/**
* \fn virtual void QtOAIUsersApi::getUsersByPage(const ::QtOpenAPI::OptionalParam<qint32> &page = ::QtOpenAPI::OptionalParam<qint32>())
* 'getUsersByPage' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] page qint32 [optional]
*/

/**
* \fn template < Functor, > void QtOAIUsersApi::getUsersByPage(const ::QtOpenAPI::OptionalParam<qint32> &page = ::QtOpenAPI::OptionalParam<qint32>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'getUsersByPage' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    getUsersByPage(page, this, [&](const QRestReply &reply, const QtOAIUserPage &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual getUsersByPageWithDataImpl() in derived class.
* The virtual getUsersByPageWithDataImpl() is being called by the template
* function.

* @param[in] page qint32 [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAIUsersApi::getUsersByPageCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by getUsersByPageFinished() or
* being returned as a callback parameter of getUsersByPage() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAIUsersApi::getUsersByPageWithDataImpl(const ::QtOpenAPI::OptionalParam<qint32> &page, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the getUsersByPage() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking getUsersByPage() operation calls.

* @param[in] page qint32 [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAIUsersApi::getUsersByPageWithDataImpl(const ::QtOpenAPI::OptionalParam<qint32> &page, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["getUsersByPage"][m_serverIndices.value("getUsersByPage")].serverUrl();
    QString fullPath = "/users";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const bool isObject = false || false;
        const QString querySuffix = getParamStyleSuffix(queryStyle, QUrl::toPercentEncoding(u"page"_s), true, isObject);
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, isObject);
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (page.hasValue()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            fullPath.append(querySuffix + QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(page.value())));
            queryParamCounter++;
        } else if (page.isNull()) {
            if (queryParamCounter > 0)
                fullPath.append("&");
            // style=form && explode=true && non-object => 'page' isn't used in serialization
            // style=form && explode=true && empty object => need to be 'page='
            // see https://spec.openapis.org/oas/v3.1.1.html#style-values
            if (queryStyle == "form")
                fullPath.append(u"page="_s);
            else
                fullPath.append(querySuffix);
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "GET");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            getUsersByPageCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit getUsersByPageErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QtOAIUserPage empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAIUsersApi::getUsersByPageCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QtOAIUserPage output(response);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit getUsersByPageFinished(output);
}

/**
* \fn virtual void QtOAIUsersApi::loginUser(const ::QtOpenAPI::OptionalParam<QtOAITestOperation_request> &qtOAITestOperationRequest = ::QtOpenAPI::OptionalParam<QtOAITestOperation_request>())
* 'loginUser' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] qtOAITestOperationRequest QtOAITestOperation_request [optional]
*/

/**
* \fn template < Functor, > void QtOAIUsersApi::loginUser(const ::QtOpenAPI::OptionalParam<QtOAITestOperation_request> &qtOAITestOperationRequest = ::QtOpenAPI::OptionalParam<QtOAITestOperation_request>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'loginUser' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    loginUser(qtOAITestOperationRequest, this, [&](const QRestReply &reply) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual loginUserWithDataImpl() in derived class.
* The virtual loginUserWithDataImpl() is being called by the template
* function.

* @param[in] qtOAITestOperationRequest QtOAITestOperation_request [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAIUsersApi::loginUserCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by loginUserFinished() or
* being returned as a callback parameter of loginUser() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAIUsersApi::loginUserWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAITestOperation_request> &qtOAITestOperationRequest, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the loginUser() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking loginUser() operation calls.

* @param[in] qtOAITestOperationRequest QtOAITestOperation_request [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAIUsersApi::loginUserWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAITestOperation_request> &qtOAITestOperationRequest, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["loginUser"][m_serverIndices.value("loginUser")].serverUrl();
    QString fullPath = "/login";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
    if (qtOAITestOperationRequest.hasValue()) {
        QByteArray output = qtOAITestOperationRequest.value().asJson().toUtf8();
        input.m_requestBody.append(output);
    } else if (qtOAITestOperationRequest.isNull()) {
        input.m_requestBody.append("null");
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            loginUserCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit loginUserErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAIUsersApi::loginUserCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply) };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit loginUserFinished();
}

/**
* \fn virtual void QtOAIUsersApi::logoutUser()
* 'logoutUser' operation sends the request to a server.
* The request parameters are defined by a specification file.

*/

/**
* \fn template < Functor, > void QtOAIUsersApi::logoutUser(const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'logoutUser' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    logoutUser(this, [&](const QRestReply &reply) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual logoutUserWithDataImpl() in derived class.
* The virtual logoutUserWithDataImpl() is being called by the template
* function.

* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAIUsersApi::logoutUserCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by logoutUserFinished() or
* being returned as a callback parameter of logoutUser() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAIUsersApi::logoutUserWithDataImpl(const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the logoutUser() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking logoutUser() operation calls.

* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAIUsersApi::logoutUserWithDataImpl(const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["logoutUser"][m_serverIndices.value("logoutUser")].serverUrl();
    QString fullPath = "/logout";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            logoutUserCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit logoutUserErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAIUsersApi::logoutUserCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply) };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit logoutUserFinished();
}

/**
* \fn virtual void QtOAIUsersApi::updateUser(const qint64 &id, const ::QtOpenAPI::OptionalParam<QtOAIUpdateUser_request> &qtOAIUpdateUserRequest = ::QtOpenAPI::OptionalParam<QtOAIUpdateUser_request>())
* 'updateUser' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] id qint64 [required]
* @param[in] qtOAIUpdateUserRequest QtOAIUpdateUser_request [optional]
*/

/**
* \fn template < Functor, > void QtOAIUsersApi::updateUser(const qint64 &id, const ::QtOpenAPI::OptionalParam<QtOAIUpdateUser_request> &qtOAIUpdateUserRequest = ::QtOpenAPI::OptionalParam<QtOAIUpdateUser_request>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'updateUser' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    updateUser(id, qtOAIUpdateUserRequest, this, [&](const QRestReply &reply) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual updateUserWithDataImpl() in derived class.
* The virtual updateUserWithDataImpl() is being called by the template
* function.

* @param[in] id qint64 [required]
* @param[in] qtOAIUpdateUserRequest QtOAIUpdateUser_request [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAIUsersApi::updateUserCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by updateUserFinished() or
* being returned as a callback parameter of updateUser() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAIUsersApi::updateUserWithDataImpl(const qint64 &id, const ::QtOpenAPI::OptionalParam<QtOAIUpdateUser_request> &qtOAIUpdateUserRequest, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the updateUser() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking updateUser() operation calls.

* @param[in] id qint64 [required]
* @param[in] qtOAIUpdateUserRequest QtOAIUpdateUser_request [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAIUsersApi::updateUserWithDataImpl(const qint64 &id, const ::QtOpenAPI::OptionalParam<QtOAIUpdateUser_request> &qtOAIUpdateUserRequest, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["updateUser"][m_serverIndices.value("updateUser")].serverUrl();
    QString fullPath = "/users/{id}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString idPathParam = QString("{%1}").arg("id");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        const bool isObject = false || false;
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, isObject);
        const QString pathSuffix = getParamStyleSuffix(pathStyle, QUrl::toPercentEncoding(u"id"_s), false, isObject);
        QString paramString = pathPrefix + pathSuffix;
        paramString += QUrl::toPercentEncoding(::QtOpenAPI::toStringValue(id));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(idPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "PATCH");
    input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
    if (qtOAIUpdateUserRequest.hasValue()) {
        QByteArray output = qtOAIUpdateUserRequest.value().asJson().toUtf8();
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            updateUserCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit updateUserErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAIUsersApi::updateUserCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply) };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit updateUserFinished();
}

} // namespace QtOpenAPI
