/**
 * Qt Quick Demo - RESTful API client
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAIColorsApi.h"
#include "OAIServerConfiguration.h"

#include <QtCore/qanystringview.h>
#include <QtCore/qjsonarray.h>
#include <QtCore/qjsondocument.h>
#include <QtNetwork/qnetworkaccessmanager.h>
#include <QtNetwork/qnetworkreply.h>
#include <QtNetwork/qnetworkrequestfactory.h>
#include <QtNetwork/qrestaccessmanager.h>

using namespace Qt::StringLiterals;

namespace OpenAPI {

OAIColorsApi::OAIColorsApi(QObject *parent)
    : OAIBaseApi(parent)
{
    initializeServerConfigs();
}

OAIColorsApi::~OAIColorsApi()
{
}

void OAIColorsApi::initializeServerConfigs()
{
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("https://reqres.in/api"),
    "Public REST API test service",
    QMap<QString, OAIServerVariable>()));
    defaultConf.append(OAIServerConfiguration(
    QUrl("http://127.0.0.1:49425/api"),
    "Qt-based REST API server",
    QMap<QString, OAIServerVariable>()));
    m_serverConfigs.insert("addColor", defaultConf);
    m_serverIndices.insert("addColor", 0);
    m_serverConfigs.insert("deleteColorById", defaultConf);
    m_serverIndices.insert("deleteColorById", 0);
    m_serverConfigs.insert("getColorById", defaultConf);
    m_serverIndices.insert("getColorById", 0);
    m_serverConfigs.insert("getColors", defaultConf);
    m_serverIndices.insert("getColors", 0);
    m_serverConfigs.insert("updateColorById", defaultConf);
    m_serverIndices.insert("updateColorById", 0);
}

/**
* \fn virtual void OAIColorsApi::addColor(const OAIColor &oAIColor)
* 'addColor' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] oAIColor OAIColor [required]
*/

/**
* \fn template < Functor, > void OAIColorsApi::addColor(const OAIColor &oAIColor, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'addColor' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    addColor(oAIColor, this, [&](const QRestReply &reply, const OAIColor &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual addColorWithDataImpl() in derived class.
* The virtual addColorWithDataImpl() is being called by the template
* function.

* @param[in] oAIColor OAIColor [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAIColorsApi::addColorCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by addColorFinished() or
* being returned as a callback parameter of addColor() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAIColorsApi::addColorWithDataImpl(const OAIColor &oAIColor, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the addColor() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking addColor() operation calls.

* @param[in] oAIColor OAIColor [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAIColorsApi::addColorWithDataImpl(const OAIColor &oAIColor, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["addColor"][m_serverIndices.value("addColor")].serverUrl();
    QString fullPath = "/colors";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    {

        QByteArray output = oAIColor.asJson().toUtf8();
        input.m_requestBody.append(output);
    }
    QByteArray requestContent;
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            addColorCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit addColorErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    OAIColor empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAIColorsApi::addColorCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        disconnect(netReply, nullptr, nullptr, nullptr);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    OAIColor output(response);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit addColorFinished(output);
}

/**
* \fn virtual void OAIColorsApi::deleteColorById(const qint64 &id)
* 'deleteColorById' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] id qint64 [required]
*/

/**
* \fn template < Functor, > void OAIColorsApi::deleteColorById(const qint64 &id, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'deleteColorById' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    deleteColorById(id, this, [&](const QRestReply &reply) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual deleteColorByIdWithDataImpl() in derived class.
* The virtual deleteColorByIdWithDataImpl() is being called by the template
* function.

* @param[in] id qint64 [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAIColorsApi::deleteColorByIdCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by deleteColorByIdFinished() or
* being returned as a callback parameter of deleteColorById() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAIColorsApi::deleteColorByIdWithDataImpl(const qint64 &id, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the deleteColorById() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking deleteColorById() operation calls.

* @param[in] id qint64 [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAIColorsApi::deleteColorByIdWithDataImpl(const qint64 &id, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["deleteColorById"][m_serverIndices.value("deleteColorById")].serverUrl();
    QString fullPath = "/colors/{id}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString idPathParam = QString("{%1}").arg("id");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"id"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;

        paramString += QUrl::toPercentEncoding(::OpenAPI::toStringValue(id));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(idPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "DELETE");
    QByteArray requestContent;
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            deleteColorByIdCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit deleteColorByIdErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAIColorsApi::deleteColorByIdCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        disconnect(netReply, nullptr, nullptr, nullptr);
    if (!reply.isSuccess())
        return;

    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply) };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit deleteColorByIdFinished();
}

/**
* \fn virtual void OAIColorsApi::getColorById(const qint64 &id)
* 'getColorById' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] id qint64 [required]
*/

/**
* \fn template < Functor, > void OAIColorsApi::getColorById(const qint64 &id, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'getColorById' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    getColorById(id, this, [&](const QRestReply &reply, const OAIColor &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual getColorByIdWithDataImpl() in derived class.
* The virtual getColorByIdWithDataImpl() is being called by the template
* function.

* @param[in] id qint64 [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAIColorsApi::getColorByIdCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by getColorByIdFinished() or
* being returned as a callback parameter of getColorById() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAIColorsApi::getColorByIdWithDataImpl(const qint64 &id, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the getColorById() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking getColorById() operation calls.

* @param[in] id qint64 [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAIColorsApi::getColorByIdWithDataImpl(const qint64 &id, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["getColorById"][m_serverIndices.value("getColorById")].serverUrl();
    QString fullPath = "/colors/{id}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString idPathParam = QString("{%1}").arg("id");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"id"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;

        paramString += QUrl::toPercentEncoding(::OpenAPI::toStringValue(id));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(idPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "GET");
    QByteArray requestContent;
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            getColorByIdCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit getColorByIdErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    OAIColor empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAIColorsApi::getColorByIdCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        disconnect(netReply, nullptr, nullptr, nullptr);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    OAIColor output(response);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit getColorByIdFinished(output);
}

/**
* \fn virtual void OAIColorsApi::getColors(const ::OpenAPI::OptionalParam<qint32> &page = ::OpenAPI::OptionalParam<qint32>())
* 'getColors' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] page qint32 [optional]
*/

/**
* \fn template < Functor, > void OAIColorsApi::getColors(const ::OpenAPI::OptionalParam<qint32> &page = ::OpenAPI::OptionalParam<qint32>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'getColors' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    getColors(page, this, [&](const QRestReply &reply, const OAIColorPage &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual getColorsWithDataImpl() in derived class.
* The virtual getColorsWithDataImpl() is being called by the template
* function.

* @param[in] page qint32 [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAIColorsApi::getColorsCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by getColorsFinished() or
* being returned as a callback parameter of getColors() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAIColorsApi::getColorsWithDataImpl(const ::OpenAPI::OptionalParam<qint32> &page, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the getColors() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking getColors() operation calls.

* @param[in] page qint32 [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAIColorsApi::getColorsWithDataImpl(const ::OpenAPI::OptionalParam<qint32> &page, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["getColors"][m_serverIndices.value("getColors")].serverUrl();
    QString fullPath = "/colors";
    m_networkFactory->setBaseUrl(serverUrl);
    int queryParamCounter = 0;
    {
        [[maybe_unused]] QString paramString;
        QString queryStyle = "form";
        if (queryStyle.isEmpty())
            queryStyle = "form";
        const QString queryPrefix = getParamStylePrefix(queryStyle);
        [[maybe_unused]] const QString queryDelimiter = getParamStyleDelimiter(queryStyle, true);
        const QString querySuffix = getParamStyleSuffix(queryStyle, u"page"_s, true, (!true && !false));
        [[maybe_unused]] const QString queryAssignOperator = getParamStyleAssignOperator(queryStyle, true, (!true && !false));
        paramString = querySuffix;
        if ((fullPath.indexOf("?") != fullPath.size() - 1) && (queryParamCounter == 0))
            fullPath.append(queryPrefix);
        if (queryParamCounter > 0)
            fullPath.append("&");
        if (page.hasValue()) {

            fullPath.append(querySuffix + QUrl::toPercentEncoding(::OpenAPI::toStringValue(page.value())));
            queryParamCounter++;
        }
    }
    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "GET");
    QByteArray requestContent;
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            getColorsCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit getColorsErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    OAIColorPage empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAIColorsApi::getColorsCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        disconnect(netReply, nullptr, nullptr, nullptr);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    OAIColorPage output(response);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit getColorsFinished(output);
}

/**
* \fn virtual void OAIColorsApi::updateColorById(const qint64 &id, const OAIColor &oAIColor)
* 'updateColorById' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] id qint64 [required]
* @param[in] oAIColor OAIColor [required]
*/

/**
* \fn template < Functor, > void OAIColorsApi::updateColorById(const qint64 &id, const OAIColor &oAIColor, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'updateColorById' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    updateColorById(id, oAIColor, this, [&](const QRestReply &reply, const QList<OAIColor> &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual updateColorByIdWithDataImpl() in derived class.
* The virtual updateColorByIdWithDataImpl() is being called by the template
* function.

* @param[in] id qint64 [required]
* @param[in] oAIColor OAIColor [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAIColorsApi::updateColorByIdCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by updateColorByIdFinished() or
* being returned as a callback parameter of updateColorById() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAIColorsApi::updateColorByIdWithDataImpl(const qint64 &id, const OAIColor &oAIColor, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the updateColorById() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking updateColorById() operation calls.

* @param[in] id qint64 [required]
* @param[in] oAIColor OAIColor [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAIColorsApi::updateColorByIdWithDataImpl(const qint64 &id, const OAIColor &oAIColor, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["updateColorById"][m_serverIndices.value("updateColorById")].serverUrl();
    QString fullPath = "/colors/{id}";
    m_networkFactory->setBaseUrl(serverUrl);
    {
        QString idPathParam = QString("{%1}").arg("id");
        QString pathStyle = "simple";
        if (pathStyle.isEmpty())
            pathStyle = "simple";
        const QString pathPrefix = getParamStylePrefix(pathStyle);
        const QString pathDelimiter = getParamStyleDelimiter(pathStyle, false);
        [[maybe_unused]] const QString assignOperator = getParamStyleAssignOperator(pathStyle, false, (!true && !false));
        const QString pathSuffix = getParamStyleSuffix(pathStyle, u"id"_s, false, (!true && !false));
        QString paramString = pathPrefix + pathSuffix;

        paramString += QUrl::toPercentEncoding(::OpenAPI::toStringValue(id));
        // In case style=matrix and paramString is empty due to any reasons,
        // we serialize it like undefined value and delete '='.
        // Described here: https://spec.openapis.org/oas/v3.1.1.html#style-values
        if ((paramString == pathPrefix + pathSuffix) && QString("simple") == "matrix"_L1)
            paramString.chop(1);
        fullPath.replace(idPathParam, paramString);
    }

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "PUT");
    {

        QByteArray output = oAIColor.asJson().toUtf8();
        input.m_requestBody.append(output);
    }
    QByteArray requestContent;
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            updateColorByIdCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit updateColorByIdErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QList<OAIColor> empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAIColorsApi::updateColorByIdCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        disconnect(netReply, nullptr, nullptr, nullptr);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QList<OAIColor> output;
    const QString json(response);
    QByteArray array(json.toStdString().c_str());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonArray jsonArray = doc.array();
    for (QJsonValue obj : jsonArray) {
        OAIColor val;
        ::OpenAPI::fromJsonValue(val, obj);
        output.append(val);
    }
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit updateColorByIdFinished(output);
}

} // namespace OpenAPI
