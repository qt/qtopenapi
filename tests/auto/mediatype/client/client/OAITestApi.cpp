/**
 * Operation Parameters Test
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAITestApi.h"
#include "OAIServerConfiguration.h"

#include <QtCore/qanystringview.h>
#include <QtCore/qjsonarray.h>
#include <QtCore/qjsondocument.h>
#include <QtNetwork/qhttpmultipart.h>
#include <QtNetwork/qnetworkaccessmanager.h>
#include <QtNetwork/qnetworkreply.h>
#include <QtNetwork/qnetworkrequestfactory.h>
#include <QtNetwork/qrestaccessmanager.h>

using namespace Qt::StringLiterals;

namespace OpenAPI {

OAITestApi::OAITestApi(QObject *parent)
    : OAIBaseApi(parent)
{
    initializeServerConfigs();
}

OAITestApi::~OAITestApi()
{
}

void OAITestApi::initializeServerConfigs()
{
    //Default server
    QList<OAIServerConfiguration> defaultConf = QList<OAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(OAIServerConfiguration(
    QUrl("http://127.0.0.1:10209/v2"),
    "No description provided",
    QMap<QString, OAIServerVariable>()));
    m_serverConfigs.insert("binaryType", defaultConf);
    m_serverIndices.insert("binaryType", 0);
    m_serverConfigs.insert("postApplicationJsonArray", defaultConf);
    m_serverIndices.insert("postApplicationJsonArray", 0);
    m_serverConfigs.insert("postApplicationJsonBool", defaultConf);
    m_serverIndices.insert("postApplicationJsonBool", 0);
    m_serverConfigs.insert("postApplicationJsonInt", defaultConf);
    m_serverIndices.insert("postApplicationJsonInt", 0);
    m_serverConfigs.insert("postApplicationJsonMap", defaultConf);
    m_serverIndices.insert("postApplicationJsonMap", 0);
    m_serverConfigs.insert("postApplicationJsonObject", defaultConf);
    m_serverIndices.insert("postApplicationJsonObject", 0);
    m_serverConfigs.insert("postApplicationJsonSeveralObjects", defaultConf);
    m_serverIndices.insert("postApplicationJsonSeveralObjects", 0);
    m_serverConfigs.insert("postApplicationJsonString", defaultConf);
    m_serverIndices.insert("postApplicationJsonString", 0);
    m_serverConfigs.insert("postMultiPartData", defaultConf);
    m_serverIndices.insert("postMultiPartData", 0);
    m_serverConfigs.insert("postPlainTextType", defaultConf);
    m_serverIndices.insert("postPlainTextType", 0);
    m_serverConfigs.insert("postUrlEncodedFields", defaultConf);
    m_serverIndices.insert("postUrlEncodedFields", 0);
    m_serverConfigs.insert("postUrlEncodedNestedObject", defaultConf);
    m_serverIndices.insert("postUrlEncodedNestedObject", 0);
    m_serverConfigs.insert("postUrlEncodedObject", defaultConf);
    m_serverIndices.insert("postUrlEncodedObject", 0);
}

/**
* \fn virtual void OAITestApi::binaryType(const ::OpenAPI::OptionalParam<OAIHttpFileElement> &body = ::OpenAPI::OptionalParam<OAIHttpFileElement>())
* 'binaryType' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] body OAIHttpFileElement [optional]
*/

/**
* \fn template < Functor, > void OAITestApi::binaryType(const ::OpenAPI::OptionalParam<OAIHttpFileElement> &body = ::OpenAPI::OptionalParam<OAIHttpFileElement>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'binaryType' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    binaryType(body, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual binaryTypeWithDataImpl() in derived class.
* The virtual binaryTypeWithDataImpl() is being called by the template
* function.

* @param[in] body OAIHttpFileElement [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::binaryTypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by binaryTypeFinished() or
* being returned as a callback parameter of binaryType() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::binaryTypeWithDataImpl(const ::OpenAPI::OptionalParam<OAIHttpFileElement> &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the binaryType() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking binaryType() operation calls.

* @param[in] body OAIHttpFileElement [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::binaryTypeWithDataImpl(const ::OpenAPI::OptionalParam<OAIHttpFileElement> &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["binaryType"][m_serverIndices.value("binaryType")].serverUrl();
    QString fullPath = "/reqBody/octetstream/binaryType";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    if (body.hasValue()) {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/octet-stream"_L1);
        QByteArray output = body.value().asByteArray();
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            binaryTypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit binaryTypeErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::binaryTypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit binaryTypeFinished(output);
}

/**
* \fn virtual void OAITestApi::postApplicationJsonArray(const QList<OAIUser> &oAIUser)
* 'postApplicationJsonArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] oAIUser QList<OAIUser> [required]
*/

/**
* \fn template < Functor, > void OAITestApi::postApplicationJsonArray(const QList<OAIUser> &oAIUser, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonArray(oAIUser, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonArrayWithDataImpl() in derived class.
* The virtual postApplicationJsonArrayWithDataImpl() is being called by the template
* function.

* @param[in] oAIUser QList<OAIUser> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postApplicationJsonArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonArrayFinished() or
* being returned as a callback parameter of postApplicationJsonArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postApplicationJsonArrayWithDataImpl(const QList<OAIUser> &oAIUser, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonArray() operation calls.

* @param[in] oAIUser QList<OAIUser> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postApplicationJsonArrayWithDataImpl(const QList<OAIUser> &oAIUser, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonArray"][m_serverIndices.value("postApplicationJsonArray")].serverUrl();
    QString fullPath = "/reqBody/appjson/array/postApplicationJsonArray";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QJsonDocument doc(::OpenAPI::toJsonValue(oAIUser).toArray());
        QByteArray bytes = doc.toJson(QJsonDocument::Compact);
        input.m_requestBody.append(bytes);
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonArrayErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postApplicationJsonArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonArrayFinished(output);
}

/**
* \fn virtual void OAITestApi::postApplicationJsonBool(const bool &body)
* 'postApplicationJsonBool' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] body bool [required]
*/

/**
* \fn template < Functor, > void OAITestApi::postApplicationJsonBool(const bool &body, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonBool' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonBool(body, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonBoolWithDataImpl() in derived class.
* The virtual postApplicationJsonBoolWithDataImpl() is being called by the template
* function.

* @param[in] body bool [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postApplicationJsonBoolCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonBoolFinished() or
* being returned as a callback parameter of postApplicationJsonBool() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postApplicationJsonBoolWithDataImpl(const bool &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonBool() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonBool() operation calls.

* @param[in] body bool [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postApplicationJsonBoolWithDataImpl(const bool &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonBool"][m_serverIndices.value("postApplicationJsonBool")].serverUrl();
    QString fullPath = "/reqBody/appjson/bool/postApplicationJsonBool";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QByteArray output = ::OpenAPI::toStringValue(body).toUtf8();
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonBoolCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonBoolErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postApplicationJsonBoolCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonBoolFinished(output);
}

/**
* \fn virtual void OAITestApi::postApplicationJsonInt(const qint32 &body)
* 'postApplicationJsonInt' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] body qint32 [required]
*/

/**
* \fn template < Functor, > void OAITestApi::postApplicationJsonInt(const qint32 &body, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonInt' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonInt(body, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonIntWithDataImpl() in derived class.
* The virtual postApplicationJsonIntWithDataImpl() is being called by the template
* function.

* @param[in] body qint32 [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postApplicationJsonIntCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonIntFinished() or
* being returned as a callback parameter of postApplicationJsonInt() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postApplicationJsonIntWithDataImpl(const qint32 &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonInt() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonInt() operation calls.

* @param[in] body qint32 [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postApplicationJsonIntWithDataImpl(const qint32 &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonInt"][m_serverIndices.value("postApplicationJsonInt")].serverUrl();
    QString fullPath = "/reqBody/appjson/integer/postApplicationJsonInt";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QByteArray output = QByteArray::number(body);
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonIntCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonIntErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postApplicationJsonIntCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonIntFinished(output);
}

/**
* \fn virtual void OAITestApi::postApplicationJsonMap(const QMap<QString, OAIUser> &requestBody)
* 'postApplicationJsonMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] requestBody QMap<QString, OAIUser> [required]
*/

/**
* \fn template < Functor, > void OAITestApi::postApplicationJsonMap(const QMap<QString, OAIUser> &requestBody, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonMap(requestBody, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonMapWithDataImpl() in derived class.
* The virtual postApplicationJsonMapWithDataImpl() is being called by the template
* function.

* @param[in] requestBody QMap<QString, OAIUser> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postApplicationJsonMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonMapFinished() or
* being returned as a callback parameter of postApplicationJsonMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postApplicationJsonMapWithDataImpl(const QMap<QString, OAIUser> &requestBody, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonMap() operation calls.

* @param[in] requestBody QMap<QString, OAIUser> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postApplicationJsonMapWithDataImpl(const QMap<QString, OAIUser> &requestBody, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonMap"][m_serverIndices.value("postApplicationJsonMap")].serverUrl();
    QString fullPath = "/reqBody/appjson/object/postApplicationJsonMap";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QJsonDocument doc(::OpenAPI::toJsonValue(requestBody).toObject());
        QByteArray bytes = doc.toJson(QJsonDocument::Compact);
        input.m_requestBody.append(bytes);
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonMapErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postApplicationJsonMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonMapFinished(output);
}

/**
* \fn virtual void OAITestApi::postApplicationJsonObject(const OAIUser &oAIUser)
* 'postApplicationJsonObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] oAIUser OAIUser [required]
*/

/**
* \fn template < Functor, > void OAITestApi::postApplicationJsonObject(const OAIUser &oAIUser, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonObject(oAIUser, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonObjectWithDataImpl() in derived class.
* The virtual postApplicationJsonObjectWithDataImpl() is being called by the template
* function.

* @param[in] oAIUser OAIUser [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postApplicationJsonObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonObjectFinished() or
* being returned as a callback parameter of postApplicationJsonObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postApplicationJsonObjectWithDataImpl(const OAIUser &oAIUser, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonObject() operation calls.

* @param[in] oAIUser OAIUser [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postApplicationJsonObjectWithDataImpl(const OAIUser &oAIUser, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonObject"][m_serverIndices.value("postApplicationJsonObject")].serverUrl();
    QString fullPath = "/reqBody/appjson/object/postApplicationJsonObject";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QByteArray output = oAIUser.asJson().toUtf8();
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonObjectErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postApplicationJsonObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonObjectFinished(output);
}

/**
* \fn virtual void OAITestApi::postApplicationJsonSeveralObjects(const OAIPostApplicationJsonSeveralObjects_request &oAIPostApplicationJsonSeveralObjectsRequest)
* 'postApplicationJsonSeveralObjects' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] oAIPostApplicationJsonSeveralObjectsRequest OAIPostApplicationJsonSeveralObjects_request [required]
*/

/**
* \fn template < Functor, > void OAITestApi::postApplicationJsonSeveralObjects(const OAIPostApplicationJsonSeveralObjects_request &oAIPostApplicationJsonSeveralObjectsRequest, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonSeveralObjects' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonSeveralObjects(oAIPostApplicationJsonSeveralObjectsRequest, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonSeveralObjectsWithDataImpl() in derived class.
* The virtual postApplicationJsonSeveralObjectsWithDataImpl() is being called by the template
* function.

* @param[in] oAIPostApplicationJsonSeveralObjectsRequest OAIPostApplicationJsonSeveralObjects_request [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postApplicationJsonSeveralObjectsCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonSeveralObjectsFinished() or
* being returned as a callback parameter of postApplicationJsonSeveralObjects() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postApplicationJsonSeveralObjectsWithDataImpl(const OAIPostApplicationJsonSeveralObjects_request &oAIPostApplicationJsonSeveralObjectsRequest, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonSeveralObjects() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonSeveralObjects() operation calls.

* @param[in] oAIPostApplicationJsonSeveralObjectsRequest OAIPostApplicationJsonSeveralObjects_request [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postApplicationJsonSeveralObjectsWithDataImpl(const OAIPostApplicationJsonSeveralObjects_request &oAIPostApplicationJsonSeveralObjectsRequest, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonSeveralObjects"][m_serverIndices.value("postApplicationJsonSeveralObjects")].serverUrl();
    QString fullPath = "/reqBody/appjson/object/postApplicationJsonSeveralObjects";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QByteArray output = oAIPostApplicationJsonSeveralObjectsRequest.asJson().toUtf8();
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonSeveralObjectsCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonSeveralObjectsErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postApplicationJsonSeveralObjectsCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonSeveralObjectsFinished(output);
}

/**
* \fn virtual void OAITestApi::postApplicationJsonString(const ::OpenAPI::OptionalParam<QString> &body = ::OpenAPI::OptionalParam<QString>())
* 'postApplicationJsonString' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] body QString [optional]
*/

/**
* \fn template < Functor, > void OAITestApi::postApplicationJsonString(const ::OpenAPI::OptionalParam<QString> &body = ::OpenAPI::OptionalParam<QString>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonString' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonString(body, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonStringWithDataImpl() in derived class.
* The virtual postApplicationJsonStringWithDataImpl() is being called by the template
* function.

* @param[in] body QString [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postApplicationJsonStringCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonStringFinished() or
* being returned as a callback parameter of postApplicationJsonString() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postApplicationJsonStringWithDataImpl(const ::OpenAPI::OptionalParam<QString> &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonString() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonString() operation calls.

* @param[in] body QString [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postApplicationJsonStringWithDataImpl(const ::OpenAPI::OptionalParam<QString> &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonString"][m_serverIndices.value("postApplicationJsonString")].serverUrl();
    QString fullPath = "/reqBody/appjson/string/postApplicationJsonString";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    if (body.hasValue()) {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QByteArray output = body.value().toUtf8();
        input.m_requestBody.append(output);
    } else if (body.isNull()) {
        input.m_requestBody.append("null");
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonStringCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonStringErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postApplicationJsonStringCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonStringFinished(output);
}

/**
* \fn virtual void OAITestApi::postMultiPartData(const QString &formId, const QList<OAIUser> &formAddresses, const ::OpenAPI::OptionalParam<qint32> &formIndex = ::OpenAPI::OptionalParam<qint32>(), const ::OpenAPI::OptionalParam<OAIHttpFileElement> &formProfileImage = ::OpenAPI::OptionalParam<OAIHttpFileElement>(), const ::OpenAPI::OptionalParam<OAIPostMultiPartData_request_formObject> &formObject = ::OpenAPI::OptionalParam<OAIPostMultiPartData_request_formObject>(), const ::OpenAPI::OptionalParam<QMap<QString, OAIUser>> &formMap = ::OpenAPI::OptionalParam<QMap<QString, OAIUser>>())
* 'postMultiPartData' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] formId QString [required]
* @param[in] formAddresses QList<OAIUser> [required]
* @param[in] formIndex qint32 [optional]
* @param[in] formProfileImage OAIHttpFileElement [optional]
* @param[in] formObject OAIPostMultiPartData_request_formObject [optional]
* @param[in] formMap QMap<QString, OAIUser> [optional]
*/

/**
* \fn template < Functor, > void OAITestApi::postMultiPartData(const QString &formId, const QList<OAIUser> &formAddresses, const ::OpenAPI::OptionalParam<qint32> &formIndex = ::OpenAPI::OptionalParam<qint32>(), const ::OpenAPI::OptionalParam<OAIHttpFileElement> &formProfileImage = ::OpenAPI::OptionalParam<OAIHttpFileElement>(), const ::OpenAPI::OptionalParam<OAIPostMultiPartData_request_formObject> &formObject = ::OpenAPI::OptionalParam<OAIPostMultiPartData_request_formObject>(), const ::OpenAPI::OptionalParam<QMap<QString, OAIUser>> &formMap = ::OpenAPI::OptionalParam<QMap<QString, OAIUser>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postMultiPartData' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postMultiPartData(formId, formAddresses, formIndex, formProfileImage, formObject, formMap, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postMultiPartDataWithDataImpl() in derived class.
* The virtual postMultiPartDataWithDataImpl() is being called by the template
* function.

* @param[in] formId QString [required]
* @param[in] formAddresses QList<OAIUser> [required]
* @param[in] formIndex qint32 [optional]
* @param[in] formProfileImage OAIHttpFileElement [optional]
* @param[in] formObject OAIPostMultiPartData_request_formObject [optional]
* @param[in] formMap QMap<QString, OAIUser> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postMultiPartDataCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postMultiPartDataFinished() or
* being returned as a callback parameter of postMultiPartData() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postMultiPartDataWithDataImpl(const QString &formId, const QList<OAIUser> &formAddresses, const ::OpenAPI::OptionalParam<qint32> &formIndex, const ::OpenAPI::OptionalParam<OAIHttpFileElement> &formProfileImage, const ::OpenAPI::OptionalParam<OAIPostMultiPartData_request_formObject> &formObject, const ::OpenAPI::OptionalParam<QMap<QString, OAIUser>> &formMap, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postMultiPartData() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postMultiPartData() operation calls.

* @param[in] formId QString [required]
* @param[in] formAddresses QList<OAIUser> [required]
* @param[in] formIndex qint32 [optional]
* @param[in] formProfileImage OAIHttpFileElement [optional]
* @param[in] formObject OAIPostMultiPartData_request_formObject [optional]
* @param[in] formMap QMap<QString, OAIUser> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postMultiPartDataWithDataImpl(const QString &formId, const QList<OAIUser> &formAddresses, const ::OpenAPI::OptionalParam<qint32> &formIndex, const ::OpenAPI::OptionalParam<OAIHttpFileElement> &formProfileImage, const ::OpenAPI::OptionalParam<OAIPostMultiPartData_request_formObject> &formObject, const ::OpenAPI::OptionalParam<QMap<QString, OAIUser>> &formMap, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postMultiPartData"][m_serverIndices.value("postMultiPartData")].serverUrl();
    QString fullPath = "/reqBody/multipart/postMultiPartData";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "multipart/form-data"_L1);
    input.addVarLayout(MULTIPART);
    {
        QString contentType;
        contentType = u"application/json"_s;
        input.addFieldHeaders(u"formId"_s, addContentField(contentType, formId));
        input.addVar(u"formId"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, formId));
    }
    if (formIndex.hasValue()) {
        QString contentType;
        input.addFieldHeaders(u"formIndex"_s, addContentField(contentType, formIndex.value()));
        input.addVar(u"formIndex"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, formIndex.value()));
    }
    if (formProfileImage.hasValue()) {
        QString contentType;
        input.addFile("formProfileImage", formProfileImage.value().m_localFilename, formProfileImage.value().m_requestFilename, contentType);
    }
    {
        QString contentType;
        // if contentType is explicitly specified as json,
        // or if it is not specified and the elements of the
        // array are non-primitive, also serialize as a json-object
        using VT = std::decay_t<decltype(formAddresses)>::value_type;
        if (contentType == "application/json"_L1
            || (contentType.isEmpty() && !isPrimitiveMediaType<VT>)) {
            input.addFieldHeaders(u"formAddresses"_s, "application/json");
            input.addVar(u"formAddresses"_s, ::OpenAPI::convertJsonValueToString(::OpenAPI::toJsonValue(formAddresses)));
        } else {
            // if none of the above - serialize according to the content type of items
            for (const auto &item : formAddresses) {
                input.addVar(u"formAddresses"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, item));
            }
            input.addFieldHeaders(u"formAddresses"_s, "text/plain");
        }
    }
    if (formObject.hasValue()) {
        QString contentType;
        input.addFieldHeaders(u"formObject"_s, addContentField(contentType, formObject.value()));
        input.addVar(u"formObject"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, formObject.value()));
    }
    if (formMap.hasValue()) {
        QString contentType;
        contentType = u"application/json"_s;
        // if contentType is explicitly specified as json,
        // or if it is not specified and the elements of the
        // array are non-primitive, also serialize as a json-object
        using MapType = std::decay_t<decltype(formMap.value())>;
        using VT = typename MapType::mapped_type;
        if (contentType == "application/json"_L1
            || (contentType.isEmpty() && !isPrimitiveMediaType<VT>)) {
            input.addFieldHeaders(u"formMap"_s, "application/json");
            input.addVar(u"formMap"_s, ::OpenAPI::convertJsonValueToString(::OpenAPI::toJsonValue(formMap.value())));
        } else {
            // if none of the above - serialize according to the content type of items
            for (const auto &[key, value] : formMap.value().asKeyValueRange()) {
                input.addVar(key, ::OpenAPI::serializeMediaTypeContentField(contentType, value));
            }
            input.addFieldHeaders(u"formMap"_s, "text/plain");
        }
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postMultiPartDataCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postMultiPartDataErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postMultiPartDataCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postMultiPartDataFinished(output);
}

/**
* \fn virtual void OAITestApi::postPlainTextType(const QString &body)
* 'postPlainTextType' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] body QString [required]
*/

/**
* \fn template < Functor, > void OAITestApi::postPlainTextType(const QString &body, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postPlainTextType' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postPlainTextType(body, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postPlainTextTypeWithDataImpl() in derived class.
* The virtual postPlainTextTypeWithDataImpl() is being called by the template
* function.

* @param[in] body QString [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postPlainTextTypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postPlainTextTypeFinished() or
* being returned as a callback parameter of postPlainTextType() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postPlainTextTypeWithDataImpl(const QString &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postPlainTextType() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postPlainTextType() operation calls.

* @param[in] body QString [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postPlainTextTypeWithDataImpl(const QString &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postPlainTextType"][m_serverIndices.value("postPlainTextType")].serverUrl();
    QString fullPath = "/reqBody/string/postPlainTextType";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "text/plain"_L1);
        QByteArray output = body.toUtf8();
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postPlainTextTypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postPlainTextTypeErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postPlainTextTypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postPlainTextTypeFinished(output);
}

/**
* \fn virtual void OAITestApi::postUrlEncodedFields(const ::OpenAPI::OptionalParam<QString> &name = ::OpenAPI::OptionalParam<QString>(), const ::OpenAPI::OptionalParam<qint32> &status = ::OpenAPI::OptionalParam<qint32>(), const ::OpenAPI::OptionalParam<bool> &availability = ::OpenAPI::OptionalParam<bool>(), const ::OpenAPI::OptionalParam<QList<QString>> &visits = ::OpenAPI::OptionalParam<QList<QString>>(), const ::OpenAPI::OptionalParam<QMap<QString, OAIUser>> &mapfield = ::OpenAPI::OptionalParam<QMap<QString, OAIUser>>())
* 'postUrlEncodedFields' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] name QString [optional]
* @param[in] status qint32 [optional]
* @param[in] availability bool [optional]
* @param[in] visits QList<QString> [optional]
* @param[in] mapfield QMap<QString, OAIUser> [optional]
*/

/**
* \fn template < Functor, > void OAITestApi::postUrlEncodedFields(const ::OpenAPI::OptionalParam<QString> &name = ::OpenAPI::OptionalParam<QString>(), const ::OpenAPI::OptionalParam<qint32> &status = ::OpenAPI::OptionalParam<qint32>(), const ::OpenAPI::OptionalParam<bool> &availability = ::OpenAPI::OptionalParam<bool>(), const ::OpenAPI::OptionalParam<QList<QString>> &visits = ::OpenAPI::OptionalParam<QList<QString>>(), const ::OpenAPI::OptionalParam<QMap<QString, OAIUser>> &mapfield = ::OpenAPI::OptionalParam<QMap<QString, OAIUser>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postUrlEncodedFields' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postUrlEncodedFields(name, status, availability, visits, mapfield, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postUrlEncodedFieldsWithDataImpl() in derived class.
* The virtual postUrlEncodedFieldsWithDataImpl() is being called by the template
* function.

* @param[in] name QString [optional]
* @param[in] status qint32 [optional]
* @param[in] availability bool [optional]
* @param[in] visits QList<QString> [optional]
* @param[in] mapfield QMap<QString, OAIUser> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postUrlEncodedFieldsCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postUrlEncodedFieldsFinished() or
* being returned as a callback parameter of postUrlEncodedFields() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postUrlEncodedFieldsWithDataImpl(const ::OpenAPI::OptionalParam<QString> &name, const ::OpenAPI::OptionalParam<qint32> &status, const ::OpenAPI::OptionalParam<bool> &availability, const ::OpenAPI::OptionalParam<QList<QString>> &visits, const ::OpenAPI::OptionalParam<QMap<QString, OAIUser>> &mapfield, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postUrlEncodedFields() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postUrlEncodedFields() operation calls.

* @param[in] name QString [optional]
* @param[in] status qint32 [optional]
* @param[in] availability bool [optional]
* @param[in] visits QList<QString> [optional]
* @param[in] mapfield QMap<QString, OAIUser> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postUrlEncodedFieldsWithDataImpl(const ::OpenAPI::OptionalParam<QString> &name, const ::OpenAPI::OptionalParam<qint32> &status, const ::OpenAPI::OptionalParam<bool> &availability, const ::OpenAPI::OptionalParam<QList<QString>> &visits, const ::OpenAPI::OptionalParam<QMap<QString, OAIUser>> &mapfield, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postUrlEncodedFields"][m_serverIndices.value("postUrlEncodedFields")].serverUrl();
    QString fullPath = "/reqBody/urlencoded/postUrlEncodedFields";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/x-www-form-urlencoded"_L1);
    input.addVarLayout(URL_ENCODED);
    if (name.hasValue()) {
        QString contentType;
        input.addFieldHeaders(u"name"_s, addContentField(contentType, name.value()));
        input.addVar(u"name"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, name.value()));
    }
    if (status.hasValue()) {
        QString contentType;
        input.addFieldHeaders(u"status"_s, addContentField(contentType, status.value()));
        input.addVar(u"status"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, status.value()));
    }
    if (availability.hasValue()) {
        QString contentType;
        input.addFieldHeaders(u"availability"_s, addContentField(contentType, availability.value()));
        input.addVar(u"availability"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, availability.value()));
    }
    if (visits.hasValue()) {
        QString contentType;
        // if contentType is explicitly specified as json,
        // or if it is not specified and the elements of the
        // array are non-primitive, also serialize as a json-object
        using VT = std::decay_t<decltype(visits.value())>::value_type;
        if (contentType == "application/json"_L1
            || (contentType.isEmpty() && !isPrimitiveMediaType<VT>)) {
            input.addFieldHeaders(u"visits"_s, "application/json");
            input.addVar(u"visits"_s, ::OpenAPI::convertJsonValueToString(::OpenAPI::toJsonValue(visits.value())));
        } else {
            // if none of the above - serialize according to the content type of items
            for (const auto &item : visits.value()) {
                input.addVar(u"visits"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, item));
            }
            input.addFieldHeaders(u"visits"_s, "text/plain");
        }
    }
    if (mapfield.hasValue()) {
        QString contentType;
        // if contentType is explicitly specified as json,
        // or if it is not specified and the elements of the
        // array are non-primitive, also serialize as a json-object
        using MapType = std::decay_t<decltype(mapfield.value())>;
        using VT = typename MapType::mapped_type;
        if (contentType == "application/json"_L1
            || (contentType.isEmpty() && !isPrimitiveMediaType<VT>)) {
            input.addFieldHeaders(u"mapfield"_s, "application/json");
            input.addVar(u"mapfield"_s, ::OpenAPI::convertJsonValueToString(::OpenAPI::toJsonValue(mapfield.value())));
        } else {
            // if none of the above - serialize according to the content type of items
            for (const auto &[key, value] : mapfield.value().asKeyValueRange()) {
                input.addVar(key, ::OpenAPI::serializeMediaTypeContentField(contentType, value));
            }
            input.addFieldHeaders(u"mapfield"_s, "text/plain");
        }
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postUrlEncodedFieldsCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postUrlEncodedFieldsErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postUrlEncodedFieldsCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postUrlEncodedFieldsFinished(output);
}

/**
* \fn virtual void OAITestApi::postUrlEncodedNestedObject(const ::OpenAPI::OptionalParam<OAIUser> &user = ::OpenAPI::OptionalParam<OAIUser>(), const ::OpenAPI::OptionalParam<QString> &comment = ::OpenAPI::OptionalParam<QString>())
* 'postUrlEncodedNestedObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] user OAIUser [optional]
* @param[in] comment QString [optional]
*/

/**
* \fn template < Functor, > void OAITestApi::postUrlEncodedNestedObject(const ::OpenAPI::OptionalParam<OAIUser> &user = ::OpenAPI::OptionalParam<OAIUser>(), const ::OpenAPI::OptionalParam<QString> &comment = ::OpenAPI::OptionalParam<QString>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postUrlEncodedNestedObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postUrlEncodedNestedObject(user, comment, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postUrlEncodedNestedObjectWithDataImpl() in derived class.
* The virtual postUrlEncodedNestedObjectWithDataImpl() is being called by the template
* function.

* @param[in] user OAIUser [optional]
* @param[in] comment QString [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postUrlEncodedNestedObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postUrlEncodedNestedObjectFinished() or
* being returned as a callback parameter of postUrlEncodedNestedObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postUrlEncodedNestedObjectWithDataImpl(const ::OpenAPI::OptionalParam<OAIUser> &user, const ::OpenAPI::OptionalParam<QString> &comment, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postUrlEncodedNestedObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postUrlEncodedNestedObject() operation calls.

* @param[in] user OAIUser [optional]
* @param[in] comment QString [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postUrlEncodedNestedObjectWithDataImpl(const ::OpenAPI::OptionalParam<OAIUser> &user, const ::OpenAPI::OptionalParam<QString> &comment, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postUrlEncodedNestedObject"][m_serverIndices.value("postUrlEncodedNestedObject")].serverUrl();
    QString fullPath = "/reqBody/urlencoded/postUrlEncodedNestedObject";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/x-www-form-urlencoded"_L1);
    input.addVarLayout(URL_ENCODED);
    if (user.hasValue()) {
        QString contentType;
        input.addFieldHeaders(u"user"_s, addContentField(contentType, user.value()));
        input.addVar(u"user"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, user.value()));
    }
    if (comment.hasValue()) {
        QString contentType;
        input.addFieldHeaders(u"comment"_s, addContentField(contentType, comment.value()));
        input.addVar(u"comment"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, comment.value()));
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postUrlEncodedNestedObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postUrlEncodedNestedObjectErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postUrlEncodedNestedObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postUrlEncodedNestedObjectFinished(output);
}

/**
* \fn virtual void OAITestApi::postUrlEncodedObject(const ::OpenAPI::OptionalParam<QString> &name = ::OpenAPI::OptionalParam<QString>(), const ::OpenAPI::OptionalParam<QString> &status = ::OpenAPI::OptionalParam<QString>(), const ::OpenAPI::OptionalParam<qint32> &age = ::OpenAPI::OptionalParam<qint32>())
* 'postUrlEncodedObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] name QString [optional]
* @param[in] status QString [optional]
* @param[in] age qint32 [optional]
*/

/**
* \fn template < Functor, > void OAITestApi::postUrlEncodedObject(const ::OpenAPI::OptionalParam<QString> &name = ::OpenAPI::OptionalParam<QString>(), const ::OpenAPI::OptionalParam<QString> &status = ::OpenAPI::OptionalParam<QString>(), const ::OpenAPI::OptionalParam<qint32> &age = ::OpenAPI::OptionalParam<qint32>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postUrlEncodedObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postUrlEncodedObject(name, status, age, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postUrlEncodedObjectWithDataImpl() in derived class.
* The virtual postUrlEncodedObjectWithDataImpl() is being called by the template
* function.

* @param[in] name QString [optional]
* @param[in] status QString [optional]
* @param[in] age qint32 [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void OAITestApi::postUrlEncodedObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postUrlEncodedObjectFinished() or
* being returned as a callback parameter of postUrlEncodedObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void OAITestApi::postUrlEncodedObjectWithDataImpl(const ::OpenAPI::OptionalParam<QString> &name, const ::OpenAPI::OptionalParam<QString> &status, const ::OpenAPI::OptionalParam<qint32> &age, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postUrlEncodedObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postUrlEncodedObject() operation calls.

* @param[in] name QString [optional]
* @param[in] status QString [optional]
* @param[in] age qint32 [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void OAITestApi::postUrlEncodedObjectWithDataImpl(const ::OpenAPI::OptionalParam<QString> &name, const ::OpenAPI::OptionalParam<QString> &status, const ::OpenAPI::OptionalParam<qint32> &age, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postUrlEncodedObject"][m_serverIndices.value("postUrlEncodedObject")].serverUrl();
    QString fullPath = "/reqBody/urlencoded/postUrlEncodedObject";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    OAIHttpRequestInput input(fullPath, "POST");
    input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/x-www-form-urlencoded"_L1);
    input.addVarLayout(URL_ENCODED);
    if (name.hasValue()) {
        QString contentType;
        input.addFieldHeaders(u"name"_s, addContentField(contentType, name.value()));
        input.addVar(u"name"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, name.value()));
    }
    if (status.hasValue()) {
        QString contentType;
        input.addFieldHeaders(u"status"_s, addContentField(contentType, status.value()));
        input.addVar(u"status"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, status.value()));
    }
    if (age.hasValue()) {
        QString contentType;
        input.addFieldHeaders(u"age"_s, addContentField(contentType, age.value()));
        input.addVar(u"age"_s, ::OpenAPI::serializeMediaTypeContentField(contentType, age.value()));
    }
    QNetworkRequest request
        = OAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, OAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postUrlEncodedObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postUrlEncodedObjectErrorOccurred(reply->error(), reply->errorString());
                OAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void OAITestApi::postUrlEncodedObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray &response = OAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    ::OpenAPI::fromStringValue(response, output);
    // Check if callback is provided
    OAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postUrlEncodedObjectFinished(output);
}

} // namespace OpenAPI
