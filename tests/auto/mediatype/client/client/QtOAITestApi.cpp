/**
 * Operation Parameters Test
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "QtOAITestApi.h"
#include "QtOAIServerConfiguration.h"

#include <QtCore/qanystringview.h>
#include <QtCore/qjsonarray.h>
#include <QtCore/qjsondocument.h>
#include <QtNetwork/qhttpmultipart.h>
#include <QtNetwork/qnetworkaccessmanager.h>
#include <QtNetwork/qnetworkreply.h>
#include <QtNetwork/qnetworkrequestfactory.h>
#include <QtNetwork/qrestaccessmanager.h>

using namespace Qt::StringLiterals;

namespace QtOpenAPI {

QtOAITestApi::QtOAITestApi(QObject *parent)
    : QtOAIBaseApi(parent)
{
    initializeServerConfigs();
}

QtOAITestApi::~QtOAITestApi()
{
}

void QtOAITestApi::initializeServerConfigs()
{
    //Default server
    QList<QtOAIServerConfiguration> defaultConf = QList<QtOAIServerConfiguration>();
    //varying endpoint server
    defaultConf.append(QtOAIServerConfiguration(
    QUrl("http://127.0.0.1:10209/v2"),
    "No description provided",
    QMap<QString, QtOAIServerVariable>()));
    m_serverConfigs.insert("binaryType", defaultConf);
    m_serverIndices.insert("binaryType", 0);
    m_serverConfigs.insert("postApplicationJsonArray", defaultConf);
    m_serverIndices.insert("postApplicationJsonArray", 0);
    m_serverConfigs.insert("postApplicationJsonBool", defaultConf);
    m_serverIndices.insert("postApplicationJsonBool", 0);
    m_serverConfigs.insert("postApplicationJsonInt", defaultConf);
    m_serverIndices.insert("postApplicationJsonInt", 0);
    m_serverConfigs.insert("postApplicationJsonMap", defaultConf);
    m_serverIndices.insert("postApplicationJsonMap", 0);
    m_serverConfigs.insert("postApplicationJsonObject", defaultConf);
    m_serverIndices.insert("postApplicationJsonObject", 0);
    m_serverConfigs.insert("postApplicationJsonSeveralObjects", defaultConf);
    m_serverIndices.insert("postApplicationJsonSeveralObjects", 0);
    m_serverConfigs.insert("postApplicationJsonString", defaultConf);
    m_serverIndices.insert("postApplicationJsonString", 0);
    m_serverConfigs.insert("postMultiPartData", defaultConf);
    m_serverIndices.insert("postMultiPartData", 0);
    m_serverConfigs.insert("postPlainTextType", defaultConf);
    m_serverIndices.insert("postPlainTextType", 0);
    m_serverConfigs.insert("postUrlEncodedFields", defaultConf);
    m_serverIndices.insert("postUrlEncodedFields", 0);
    m_serverConfigs.insert("postUrlEncodedNestedObject", defaultConf);
    m_serverIndices.insert("postUrlEncodedNestedObject", 0);
    m_serverConfigs.insert("postUrlEncodedObject", defaultConf);
    m_serverIndices.insert("postUrlEncodedObject", 0);
}

/**
* \fn virtual void QtOAITestApi::binaryType(const ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement> &body = ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement>())
* 'binaryType' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] body QtOAIHttpFileElement [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::binaryType(const ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement> &body = ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'binaryType' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    binaryType(body, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual binaryTypeWithDataImpl() in derived class.
* The virtual binaryTypeWithDataImpl() is being called by the template
* function.

* @param[in] body QtOAIHttpFileElement [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::binaryTypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by binaryTypeFinished() or
* being returned as a callback parameter of binaryType() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::binaryTypeWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement> &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the binaryType() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking binaryType() operation calls.

* @param[in] body QtOAIHttpFileElement [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::binaryTypeWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement> &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["binaryType"][m_serverIndices.value("binaryType")].serverUrl();
    QString fullPath = "/reqBody/octetstream/binaryType";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    if (body.hasValue()) {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/octet-stream"_L1);
        QByteArray output = body.value().asByteArray();
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            binaryTypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit binaryTypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::binaryTypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit binaryTypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postApplicationJsonArray(const QList<QtOAIUser> &qtOAIUser)
* 'postApplicationJsonArray' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] qtOAIUser QList<QtOAIUser> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postApplicationJsonArray(const QList<QtOAIUser> &qtOAIUser, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonArray' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonArray(qtOAIUser, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonArrayWithDataImpl() in derived class.
* The virtual postApplicationJsonArrayWithDataImpl() is being called by the template
* function.

* @param[in] qtOAIUser QList<QtOAIUser> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postApplicationJsonArrayCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonArrayFinished() or
* being returned as a callback parameter of postApplicationJsonArray() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postApplicationJsonArrayWithDataImpl(const QList<QtOAIUser> &qtOAIUser, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonArray() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonArray() operation calls.

* @param[in] qtOAIUser QList<QtOAIUser> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postApplicationJsonArrayWithDataImpl(const QList<QtOAIUser> &qtOAIUser, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonArray"][m_serverIndices.value("postApplicationJsonArray")].serverUrl();
    QString fullPath = "/reqBody/appjson/array/postApplicationJsonArray";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QJsonDocument doc(::QtOpenAPI::toJsonValue(qtOAIUser).toArray());
        QByteArray bytes = doc.toJson(QJsonDocument::Compact);
        input.m_requestBody.append(bytes);
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonArrayCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonArrayErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postApplicationJsonArrayCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonArrayFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postApplicationJsonBool(const bool &body)
* 'postApplicationJsonBool' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] body bool [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postApplicationJsonBool(const bool &body, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonBool' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonBool(body, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonBoolWithDataImpl() in derived class.
* The virtual postApplicationJsonBoolWithDataImpl() is being called by the template
* function.

* @param[in] body bool [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postApplicationJsonBoolCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonBoolFinished() or
* being returned as a callback parameter of postApplicationJsonBool() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postApplicationJsonBoolWithDataImpl(const bool &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonBool() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonBool() operation calls.

* @param[in] body bool [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postApplicationJsonBoolWithDataImpl(const bool &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonBool"][m_serverIndices.value("postApplicationJsonBool")].serverUrl();
    QString fullPath = "/reqBody/appjson/bool/postApplicationJsonBool";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QByteArray output = ::QtOpenAPI::toStringValue(body).toUtf8();
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonBoolCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonBoolErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postApplicationJsonBoolCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonBoolFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postApplicationJsonInt(const qint32 &body)
* 'postApplicationJsonInt' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] body qint32 [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postApplicationJsonInt(const qint32 &body, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonInt' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonInt(body, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonIntWithDataImpl() in derived class.
* The virtual postApplicationJsonIntWithDataImpl() is being called by the template
* function.

* @param[in] body qint32 [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postApplicationJsonIntCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonIntFinished() or
* being returned as a callback parameter of postApplicationJsonInt() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postApplicationJsonIntWithDataImpl(const qint32 &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonInt() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonInt() operation calls.

* @param[in] body qint32 [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postApplicationJsonIntWithDataImpl(const qint32 &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonInt"][m_serverIndices.value("postApplicationJsonInt")].serverUrl();
    QString fullPath = "/reqBody/appjson/integer/postApplicationJsonInt";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QByteArray output = QByteArray::number(body);
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonIntCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonIntErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postApplicationJsonIntCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonIntFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postApplicationJsonMap(const QMap<QString, QtOAIUser> &requestBody)
* 'postApplicationJsonMap' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] requestBody QMap<QString, QtOAIUser> [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postApplicationJsonMap(const QMap<QString, QtOAIUser> &requestBody, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonMap' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonMap(requestBody, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonMapWithDataImpl() in derived class.
* The virtual postApplicationJsonMapWithDataImpl() is being called by the template
* function.

* @param[in] requestBody QMap<QString, QtOAIUser> [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postApplicationJsonMapCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonMapFinished() or
* being returned as a callback parameter of postApplicationJsonMap() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postApplicationJsonMapWithDataImpl(const QMap<QString, QtOAIUser> &requestBody, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonMap() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonMap() operation calls.

* @param[in] requestBody QMap<QString, QtOAIUser> [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postApplicationJsonMapWithDataImpl(const QMap<QString, QtOAIUser> &requestBody, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonMap"][m_serverIndices.value("postApplicationJsonMap")].serverUrl();
    QString fullPath = "/reqBody/appjson/object/postApplicationJsonMap";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QJsonDocument doc(::QtOpenAPI::toJsonValue(requestBody).toObject());
        QByteArray bytes = doc.toJson(QJsonDocument::Compact);
        input.m_requestBody.append(bytes);
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonMapCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonMapErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postApplicationJsonMapCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonMapFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postApplicationJsonObject(const QtOAIUser &qtOAIUser)
* 'postApplicationJsonObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] qtOAIUser QtOAIUser [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postApplicationJsonObject(const QtOAIUser &qtOAIUser, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonObject(qtOAIUser, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonObjectWithDataImpl() in derived class.
* The virtual postApplicationJsonObjectWithDataImpl() is being called by the template
* function.

* @param[in] qtOAIUser QtOAIUser [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postApplicationJsonObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonObjectFinished() or
* being returned as a callback parameter of postApplicationJsonObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postApplicationJsonObjectWithDataImpl(const QtOAIUser &qtOAIUser, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonObject() operation calls.

* @param[in] qtOAIUser QtOAIUser [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postApplicationJsonObjectWithDataImpl(const QtOAIUser &qtOAIUser, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonObject"][m_serverIndices.value("postApplicationJsonObject")].serverUrl();
    QString fullPath = "/reqBody/appjson/object/postApplicationJsonObject";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QByteArray output = qtOAIUser.asJson().toUtf8();
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postApplicationJsonObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postApplicationJsonSeveralObjects(const QtOAIPostApplicationJsonSeveralObjects_request &qtOAIPostApplicationJsonSeveralObjectsRequest)
* 'postApplicationJsonSeveralObjects' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] qtOAIPostApplicationJsonSeveralObjectsRequest QtOAIPostApplicationJsonSeveralObjects_request [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postApplicationJsonSeveralObjects(const QtOAIPostApplicationJsonSeveralObjects_request &qtOAIPostApplicationJsonSeveralObjectsRequest, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonSeveralObjects' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonSeveralObjects(qtOAIPostApplicationJsonSeveralObjectsRequest, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonSeveralObjectsWithDataImpl() in derived class.
* The virtual postApplicationJsonSeveralObjectsWithDataImpl() is being called by the template
* function.

* @param[in] qtOAIPostApplicationJsonSeveralObjectsRequest QtOAIPostApplicationJsonSeveralObjects_request [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postApplicationJsonSeveralObjectsCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonSeveralObjectsFinished() or
* being returned as a callback parameter of postApplicationJsonSeveralObjects() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postApplicationJsonSeveralObjectsWithDataImpl(const QtOAIPostApplicationJsonSeveralObjects_request &qtOAIPostApplicationJsonSeveralObjectsRequest, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonSeveralObjects() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonSeveralObjects() operation calls.

* @param[in] qtOAIPostApplicationJsonSeveralObjectsRequest QtOAIPostApplicationJsonSeveralObjects_request [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postApplicationJsonSeveralObjectsWithDataImpl(const QtOAIPostApplicationJsonSeveralObjects_request &qtOAIPostApplicationJsonSeveralObjectsRequest, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonSeveralObjects"][m_serverIndices.value("postApplicationJsonSeveralObjects")].serverUrl();
    QString fullPath = "/reqBody/appjson/object/postApplicationJsonSeveralObjects";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QByteArray output = qtOAIPostApplicationJsonSeveralObjectsRequest.asJson().toUtf8();
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonSeveralObjectsCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonSeveralObjectsErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postApplicationJsonSeveralObjectsCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonSeveralObjectsFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postApplicationJsonString(const ::QtOpenAPI::OptionalParam<QString> &body = ::QtOpenAPI::OptionalParam<QString>())
* 'postApplicationJsonString' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] body QString [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postApplicationJsonString(const ::QtOpenAPI::OptionalParam<QString> &body = ::QtOpenAPI::OptionalParam<QString>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postApplicationJsonString' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postApplicationJsonString(body, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postApplicationJsonStringWithDataImpl() in derived class.
* The virtual postApplicationJsonStringWithDataImpl() is being called by the template
* function.

* @param[in] body QString [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postApplicationJsonStringCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postApplicationJsonStringFinished() or
* being returned as a callback parameter of postApplicationJsonString() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postApplicationJsonStringWithDataImpl(const ::QtOpenAPI::OptionalParam<QString> &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postApplicationJsonString() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postApplicationJsonString() operation calls.

* @param[in] body QString [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postApplicationJsonStringWithDataImpl(const ::QtOpenAPI::OptionalParam<QString> &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postApplicationJsonString"][m_serverIndices.value("postApplicationJsonString")].serverUrl();
    QString fullPath = "/reqBody/appjson/string/postApplicationJsonString";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    if (body.hasValue()) {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/json"_L1);
        QByteArray output = body.value().toUtf8();
        input.m_requestBody.append(output);
    } else if (body.isNull()) {
        input.m_requestBody.append("null");
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postApplicationJsonStringCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postApplicationJsonStringErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postApplicationJsonStringCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postApplicationJsonStringFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postMultiPartData(const QString &formId, const QList<QtOAIUser> &formAddresses, const ::QtOpenAPI::OptionalParam<qint32> &formIndex = ::QtOpenAPI::OptionalParam<qint32>(), const ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement> &formProfileImage = ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement>(), const ::QtOpenAPI::OptionalParam<QtOAIPostMultiPartData_request_formObject> &formObject = ::QtOpenAPI::OptionalParam<QtOAIPostMultiPartData_request_formObject>(), const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>> &formMap = ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>>())
* 'postMultiPartData' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] formId QString [required]
* @param[in] formAddresses QList<QtOAIUser> [required]
* @param[in] formIndex qint32 [optional]
* @param[in] formProfileImage QtOAIHttpFileElement [optional]
* @param[in] formObject QtOAIPostMultiPartData_request_formObject [optional]
* @param[in] formMap QMap<QString, QtOAIUser> [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postMultiPartData(const QString &formId, const QList<QtOAIUser> &formAddresses, const ::QtOpenAPI::OptionalParam<qint32> &formIndex = ::QtOpenAPI::OptionalParam<qint32>(), const ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement> &formProfileImage = ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement>(), const ::QtOpenAPI::OptionalParam<QtOAIPostMultiPartData_request_formObject> &formObject = ::QtOpenAPI::OptionalParam<QtOAIPostMultiPartData_request_formObject>(), const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>> &formMap = ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postMultiPartData' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postMultiPartData(formId, formAddresses, formIndex, formProfileImage, formObject, formMap, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postMultiPartDataWithDataImpl() in derived class.
* The virtual postMultiPartDataWithDataImpl() is being called by the template
* function.

* @param[in] formId QString [required]
* @param[in] formAddresses QList<QtOAIUser> [required]
* @param[in] formIndex qint32 [optional]
* @param[in] formProfileImage QtOAIHttpFileElement [optional]
* @param[in] formObject QtOAIPostMultiPartData_request_formObject [optional]
* @param[in] formMap QMap<QString, QtOAIUser> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postMultiPartDataCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postMultiPartDataFinished() or
* being returned as a callback parameter of postMultiPartData() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postMultiPartDataWithDataImpl(const QString &formId, const QList<QtOAIUser> &formAddresses, const ::QtOpenAPI::OptionalParam<qint32> &formIndex, const ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement> &formProfileImage, const ::QtOpenAPI::OptionalParam<QtOAIPostMultiPartData_request_formObject> &formObject, const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>> &formMap, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postMultiPartData() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postMultiPartData() operation calls.

* @param[in] formId QString [required]
* @param[in] formAddresses QList<QtOAIUser> [required]
* @param[in] formIndex qint32 [optional]
* @param[in] formProfileImage QtOAIHttpFileElement [optional]
* @param[in] formObject QtOAIPostMultiPartData_request_formObject [optional]
* @param[in] formMap QMap<QString, QtOAIUser> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postMultiPartDataWithDataImpl(const QString &formId, const QList<QtOAIUser> &formAddresses, const ::QtOpenAPI::OptionalParam<qint32> &formIndex, const ::QtOpenAPI::OptionalParam<QtOAIHttpFileElement> &formProfileImage, const ::QtOpenAPI::OptionalParam<QtOAIPostMultiPartData_request_formObject> &formObject, const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>> &formMap, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postMultiPartData"][m_serverIndices.value("postMultiPartData")].serverUrl();
    QString fullPath = "/reqBody/multipart/postMultiPartData";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "multipart/form-data"_L1);
    input.addVarLayout(MULTIPART);
    {
        QString contentType;
        contentType = u"application/json"_s;
        using VT = std::decay_t<decltype(formId)>;
        input.addFieldHeaders(u"formId"_s, addContentField<VT>(contentType));
        input.addVar(u"formId"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, formId));
    }
    if (formIndex.hasValue()) {
        QString contentType;
        using VT = std::decay_t<decltype(formIndex.value())>;
        input.addFieldHeaders(u"formIndex"_s, addContentField<VT>(contentType));
        input.addVar(u"formIndex"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, formIndex.value()));
    }
    if (formProfileImage.hasValue()) {
        QString contentType;
        input.addFile("formProfileImage", formProfileImage.value().m_localFilename, formProfileImage.value().m_requestFilename, contentType);
    }
    {
        QString contentType;
        // if contentType is explicitly specified as json,
        // or if it is not specified and the elements of the
        // array are non-primitive, also serialize as a json-object
        using VT = std::decay_t<decltype(formAddresses)>::value_type;
        if (contentType == "application/json"_L1
            || (contentType.isEmpty() && !isPrimitiveMediaType<VT>)) {
            input.addFieldHeaders(u"formAddresses"_s, "application/json"_L1);
            input.addVar(u"formAddresses"_s, ::QtOpenAPI::convertJsonValueToString(::QtOpenAPI::toJsonValue(formAddresses)));
        } else {
            // if none of the above - serialize according to the content type of items
            for (const auto &item : formAddresses) {
                input.addVar(u"formAddresses"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, item));
            }
            input.addFieldHeaders(u"formAddresses"_s, "text/plain"_L1);
        }
    }
    if (formObject.hasValue()) {
        QString contentType;
        using VT = std::decay_t<decltype(formObject.value())>;
        input.addFieldHeaders(u"formObject"_s, addContentField<VT>(contentType));
        input.addVar(u"formObject"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, formObject.value()));
    }
    if (formMap.hasValue()) {
        QString contentType;
        contentType = u"application/json"_s;
        // if contentType is explicitly specified as json,
        // or if it is not specified and the elements of the
        // array are non-primitive, also serialize as a json-object
        using MapType = std::decay_t<decltype(formMap.value())>;
        using VT = typename MapType::mapped_type;
        if (contentType == "application/json"_L1
            || (contentType.isEmpty() && !isPrimitiveMediaType<VT>)) {
            input.addFieldHeaders(u"formMap"_s, "application/json"_L1);
            input.addVar(u"formMap"_s, ::QtOpenAPI::convertJsonValueToString(::QtOpenAPI::toJsonValue(formMap.value())));
        } else {
            // if none of the above - serialize according to the content type of items
            for (const auto &[key, value] : formMap.value().asKeyValueRange()) {
                input.addVar(key, ::QtOpenAPI::serializeMediaTypeContentField(contentType, value));
            }
            input.addFieldHeaders(u"formMap"_s, "text/plain"_L1);
        }
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postMultiPartDataCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postMultiPartDataErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postMultiPartDataCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postMultiPartDataFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postPlainTextType(const QString &body)
* 'postPlainTextType' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] body QString [required]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postPlainTextType(const QString &body, const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postPlainTextType' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postPlainTextType(body, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postPlainTextTypeWithDataImpl() in derived class.
* The virtual postPlainTextTypeWithDataImpl() is being called by the template
* function.

* @param[in] body QString [required]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postPlainTextTypeCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postPlainTextTypeFinished() or
* being returned as a callback parameter of postPlainTextType() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postPlainTextTypeWithDataImpl(const QString &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postPlainTextType() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postPlainTextType() operation calls.

* @param[in] body QString [required]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postPlainTextTypeWithDataImpl(const QString &body, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postPlainTextType"][m_serverIndices.value("postPlainTextType")].serverUrl();
    QString fullPath = "/reqBody/string/postPlainTextType";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    {
        input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "text/plain"_L1);
        QByteArray output = body.toUtf8();
        input.m_requestBody.append(output);
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postPlainTextTypeCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postPlainTextTypeErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postPlainTextTypeCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postPlainTextTypeFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postUrlEncodedFields(const ::QtOpenAPI::OptionalParam<QString> &name = ::QtOpenAPI::OptionalParam<QString>(), const ::QtOpenAPI::OptionalParam<qint32> &status = ::QtOpenAPI::OptionalParam<qint32>(), const ::QtOpenAPI::OptionalParam<bool> &availability = ::QtOpenAPI::OptionalParam<bool>(), const ::QtOpenAPI::OptionalParam<QList<QString>> &visits = ::QtOpenAPI::OptionalParam<QList<QString>>(), const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>> &mapfield = ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>>())
* 'postUrlEncodedFields' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] name QString [optional]
* @param[in] status qint32 [optional]
* @param[in] availability bool [optional]
* @param[in] visits QList<QString> [optional]
* @param[in] mapfield QMap<QString, QtOAIUser> [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postUrlEncodedFields(const ::QtOpenAPI::OptionalParam<QString> &name = ::QtOpenAPI::OptionalParam<QString>(), const ::QtOpenAPI::OptionalParam<qint32> &status = ::QtOpenAPI::OptionalParam<qint32>(), const ::QtOpenAPI::OptionalParam<bool> &availability = ::QtOpenAPI::OptionalParam<bool>(), const ::QtOpenAPI::OptionalParam<QList<QString>> &visits = ::QtOpenAPI::OptionalParam<QList<QString>>(), const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>> &mapfield = ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postUrlEncodedFields' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postUrlEncodedFields(name, status, availability, visits, mapfield, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postUrlEncodedFieldsWithDataImpl() in derived class.
* The virtual postUrlEncodedFieldsWithDataImpl() is being called by the template
* function.

* @param[in] name QString [optional]
* @param[in] status qint32 [optional]
* @param[in] availability bool [optional]
* @param[in] visits QList<QString> [optional]
* @param[in] mapfield QMap<QString, QtOAIUser> [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postUrlEncodedFieldsCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postUrlEncodedFieldsFinished() or
* being returned as a callback parameter of postUrlEncodedFields() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postUrlEncodedFieldsWithDataImpl(const ::QtOpenAPI::OptionalParam<QString> &name, const ::QtOpenAPI::OptionalParam<qint32> &status, const ::QtOpenAPI::OptionalParam<bool> &availability, const ::QtOpenAPI::OptionalParam<QList<QString>> &visits, const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>> &mapfield, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postUrlEncodedFields() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postUrlEncodedFields() operation calls.

* @param[in] name QString [optional]
* @param[in] status qint32 [optional]
* @param[in] availability bool [optional]
* @param[in] visits QList<QString> [optional]
* @param[in] mapfield QMap<QString, QtOAIUser> [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postUrlEncodedFieldsWithDataImpl(const ::QtOpenAPI::OptionalParam<QString> &name, const ::QtOpenAPI::OptionalParam<qint32> &status, const ::QtOpenAPI::OptionalParam<bool> &availability, const ::QtOpenAPI::OptionalParam<QList<QString>> &visits, const ::QtOpenAPI::OptionalParam<QMap<QString, QtOAIUser>> &mapfield, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postUrlEncodedFields"][m_serverIndices.value("postUrlEncodedFields")].serverUrl();
    QString fullPath = "/reqBody/urlencoded/postUrlEncodedFields";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/x-www-form-urlencoded"_L1);
    input.addVarLayout(URL_ENCODED);
    if (name.hasValue()) {
        QString contentType;
        using VT = std::decay_t<decltype(name.value())>;
        input.addFieldHeaders(u"name"_s, addContentField<VT>(contentType));
        input.addVar(u"name"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, name.value()));
    }
    if (status.hasValue()) {
        QString contentType;
        using VT = std::decay_t<decltype(status.value())>;
        input.addFieldHeaders(u"status"_s, addContentField<VT>(contentType));
        input.addVar(u"status"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, status.value()));
    }
    if (availability.hasValue()) {
        QString contentType;
        using VT = std::decay_t<decltype(availability.value())>;
        input.addFieldHeaders(u"availability"_s, addContentField<VT>(contentType));
        input.addVar(u"availability"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, availability.value()));
    }
    if (visits.hasValue()) {
        QString contentType;
        // if contentType is explicitly specified as json,
        // or if it is not specified and the elements of the
        // array are non-primitive, also serialize as a json-object
        using VT = std::decay_t<decltype(visits.value())>::value_type;
        if (contentType == "application/json"_L1
            || (contentType.isEmpty() && !isPrimitiveMediaType<VT>)) {
            input.addFieldHeaders(u"visits"_s, "application/json"_L1);
            input.addVar(u"visits"_s, ::QtOpenAPI::convertJsonValueToString(::QtOpenAPI::toJsonValue(visits.value())));
        } else {
            // if none of the above - serialize according to the content type of items
            for (const auto &item : visits.value()) {
                input.addVar(u"visits"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, item));
            }
            input.addFieldHeaders(u"visits"_s, "text/plain"_L1);
        }
    }
    if (mapfield.hasValue()) {
        QString contentType;
        // if contentType is explicitly specified as json,
        // or if it is not specified and the elements of the
        // array are non-primitive, also serialize as a json-object
        using MapType = std::decay_t<decltype(mapfield.value())>;
        using VT = typename MapType::mapped_type;
        if (contentType == "application/json"_L1
            || (contentType.isEmpty() && !isPrimitiveMediaType<VT>)) {
            input.addFieldHeaders(u"mapfield"_s, "application/json"_L1);
            input.addVar(u"mapfield"_s, ::QtOpenAPI::convertJsonValueToString(::QtOpenAPI::toJsonValue(mapfield.value())));
        } else {
            // if none of the above - serialize according to the content type of items
            for (const auto &[key, value] : mapfield.value().asKeyValueRange()) {
                input.addVar(key, ::QtOpenAPI::serializeMediaTypeContentField(contentType, value));
            }
            input.addFieldHeaders(u"mapfield"_s, "text/plain"_L1);
        }
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postUrlEncodedFieldsCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postUrlEncodedFieldsErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postUrlEncodedFieldsCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postUrlEncodedFieldsFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postUrlEncodedNestedObject(const ::QtOpenAPI::OptionalParam<QtOAIUser> &user = ::QtOpenAPI::OptionalParam<QtOAIUser>(), const ::QtOpenAPI::OptionalParam<QString> &comment = ::QtOpenAPI::OptionalParam<QString>())
* 'postUrlEncodedNestedObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] user QtOAIUser [optional]
* @param[in] comment QString [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postUrlEncodedNestedObject(const ::QtOpenAPI::OptionalParam<QtOAIUser> &user = ::QtOpenAPI::OptionalParam<QtOAIUser>(), const ::QtOpenAPI::OptionalParam<QString> &comment = ::QtOpenAPI::OptionalParam<QString>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postUrlEncodedNestedObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postUrlEncodedNestedObject(user, comment, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postUrlEncodedNestedObjectWithDataImpl() in derived class.
* The virtual postUrlEncodedNestedObjectWithDataImpl() is being called by the template
* function.

* @param[in] user QtOAIUser [optional]
* @param[in] comment QString [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postUrlEncodedNestedObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postUrlEncodedNestedObjectFinished() or
* being returned as a callback parameter of postUrlEncodedNestedObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postUrlEncodedNestedObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAIUser> &user, const ::QtOpenAPI::OptionalParam<QString> &comment, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postUrlEncodedNestedObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postUrlEncodedNestedObject() operation calls.

* @param[in] user QtOAIUser [optional]
* @param[in] comment QString [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postUrlEncodedNestedObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QtOAIUser> &user, const ::QtOpenAPI::OptionalParam<QString> &comment, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postUrlEncodedNestedObject"][m_serverIndices.value("postUrlEncodedNestedObject")].serverUrl();
    QString fullPath = "/reqBody/urlencoded/postUrlEncodedNestedObject";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/x-www-form-urlencoded"_L1);
    input.addVarLayout(URL_ENCODED);
    if (user.hasValue()) {
        QString contentType;
        using VT = std::decay_t<decltype(user.value())>;
        input.addFieldHeaders(u"user"_s, addContentField<VT>(contentType));
        input.addVar(u"user"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, user.value()));
    }
    if (comment.hasValue()) {
        QString contentType;
        using VT = std::decay_t<decltype(comment.value())>;
        input.addFieldHeaders(u"comment"_s, addContentField<VT>(contentType));
        input.addVar(u"comment"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, comment.value()));
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postUrlEncodedNestedObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postUrlEncodedNestedObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postUrlEncodedNestedObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postUrlEncodedNestedObjectFinished(output);
}

/**
* \fn virtual void QtOAITestApi::postUrlEncodedObject(const ::QtOpenAPI::OptionalParam<QString> &name = ::QtOpenAPI::OptionalParam<QString>(), const ::QtOpenAPI::OptionalParam<QString> &status = ::QtOpenAPI::OptionalParam<QString>(), const ::QtOpenAPI::OptionalParam<qint32> &age = ::QtOpenAPI::OptionalParam<qint32>())
* 'postUrlEncodedObject' operation sends the request to a server.
* The request parameters are defined by a specification file.

* @param[in] name QString [optional]
* @param[in] status QString [optional]
* @param[in] age qint32 [optional]
*/

/**
* \fn template < Functor, > void QtOAITestApi::postUrlEncodedObject(const ::QtOpenAPI::OptionalParam<QString> &name = ::QtOpenAPI::OptionalParam<QString>(), const ::QtOpenAPI::OptionalParam<QString> &status = ::QtOpenAPI::OptionalParam<QString>(), const ::QtOpenAPI::OptionalParam<qint32> &age = ::QtOpenAPI::OptionalParam<qint32>(), const ContextTypeForFunctor< Functor > *context = nullptr, Functor &&callback = (){})
* 'postUrlEncodedObject' operation sends the request to a server.
* The request parameters are defined by a specification file.
*
* \attention Use the operation with following parameters in the callback:
* \code {c++}
*    postUrlEncodedObject(name, status, age, this, [&](const QRestReply &reply, const QString &summary) { if (reply.isSuccess()) ... });
* \endcode
* \note The template function can not be virtual in C++17.
* If you want to use 'makeOperationsVirtual' option for mocking API,
* please override virtual postUrlEncodedObjectWithDataImpl() in derived class.
* The virtual postUrlEncodedObjectWithDataImpl() is being called by the template
* function.

* @param[in] name QString [optional]
* @param[in] status QString [optional]
* @param[in] age qint32 [optional]
* @param[in] context const ContextTypeForFunctor< Functor > * [optional]
* @param[in] callback Functor && [optional]
*/

/**
* \fn void QtOAITestApi::postUrlEncodedObjectCallback(const QRestReply &reply)
* Processes a \a reply response from a server.
* The result of processed data is emitted by postUrlEncodedObjectFinished() or
* being returned as a callback parameter of postUrlEncodedObject() request.
* @param[in] reply const QRestReply &
*/

/**
* \fn virtual void QtOAITestApi::postUrlEncodedObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QString> &name, const ::QtOpenAPI::OptionalParam<QString> &status, const ::QtOpenAPI::OptionalParam<qint32> &age, const QObject *context, QtPrivate::QSlotObjectBase *slot)
* Implements the postUrlEncodedObject() operation request.
* \note If 'makeOperationsVirtual' option is true, this function is declared as virtual
* and can be overloaded for mocking postUrlEncodedObject() operation calls.

* @param[in] name QString [optional]
* @param[in] status QString [optional]
* @param[in] age qint32 [optional]
* @param[in] context const QObject * [optional]
* @param[in] slot QtPrivate::QSlotObjectBase * [optional]
*/
void QtOAITestApi::postUrlEncodedObjectWithDataImpl(const ::QtOpenAPI::OptionalParam<QString> &name, const ::QtOpenAPI::OptionalParam<QString> &status, const ::QtOpenAPI::OptionalParam<qint32> &age, const QObject *context, QtPrivate::QSlotObjectBase *slot)
{
    const QUrl serverUrl = m_serverConfigs["postUrlEncodedObject"][m_serverIndices.value("postUrlEncodedObject")].serverUrl();
    QString fullPath = "/reqBody/urlencoded/postUrlEncodedObject";
    m_networkFactory->setBaseUrl(serverUrl);

    // set m_testOperationPath for serialization tests
    m_testOperationPath = fullPath;
    QtOAIHttpRequestInput input(fullPath, "POST");
    input.m_headers.replaceOrAppend(QHttpHeaders::WellKnownHeader::ContentType, "application/x-www-form-urlencoded"_L1);
    input.addVarLayout(URL_ENCODED);
    if (name.hasValue()) {
        QString contentType;
        using VT = std::decay_t<decltype(name.value())>;
        input.addFieldHeaders(u"name"_s, addContentField<VT>(contentType));
        input.addVar(u"name"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, name.value()));
    }
    if (status.hasValue()) {
        QString contentType;
        using VT = std::decay_t<decltype(status.value())>;
        input.addFieldHeaders(u"status"_s, addContentField<VT>(contentType));
        input.addVar(u"status"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, status.value()));
    }
    if (age.hasValue()) {
        QString contentType;
        using VT = std::decay_t<decltype(age.value())>;
        input.addFieldHeaders(u"age"_s, addContentField<VT>(contentType));
        input.addVar(u"age"_s, ::QtOpenAPI::serializeMediaTypeContentField(contentType, age.value()));
    }
    QNetworkRequest request
        = QtOAIHttpRequestWorker::getNetworkRequest(input, m_requestContent, m_networkFactory,
                                                  m_isResponseCompressionEnabled, m_isRequestCompressionEnabled);
    QNetworkReply *reply = execute(input, request, m_requestContent);
    if (reply != nullptr) {
        reply->setParent(this);
        m_callerData.insert(reply, QtOAICallerInfo{context, slot});
        connect(reply, &QNetworkReply::finished, this, [this, reply] {
            postUrlEncodedObjectCallback(QRestReply(reply));
        });
        connect(reply, &QNetworkReply::errorOccurred, this, [this, reply] {
            if (reply) {
                emit postUrlEncodedObjectErrorOccurred(reply->error(), reply->errorString());
                QtOAICallerInfo callerInfo = m_callerData.take(reply);
                if (callerInfo.slot) {
                    QString empty;
                    QRestReply restRepl(reply);
                    void *argv[] = { nullptr, &restRepl, &empty };
                    QObject *context = callerInfo.contextObject ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
                    callerInfo.slot->call(context, argv);
                }
            }
        });
    }
}

void QtOAITestApi::postUrlEncodedObjectCallback(const QRestReply &reply)
{
    auto netReply = reply.networkReply();
    if (netReply)
        netReply->disconnect(this);
    if (!reply.isSuccess())
        return;

    const QByteArray response = QtOAIHttpRequestWorker::parseResponse(reply, m_workingDirectory);
    QString output;
    const bool ok = ::QtOpenAPI::fromByteArray(response, output);
    if (!ok)
        qWarning("%s: Failed to convert the response to QString.", Q_FUNC_INFO);
    // Check if callback is provided
    QtOAICallerInfo callerInfo = m_callerData.take(netReply);
    if (callerInfo.slot) {
        void *argv[] = { nullptr, const_cast<QRestReply*>(&reply), &output };
        QObject *context = callerInfo.contextObject
                        ? const_cast<QObject*>(callerInfo.contextObject) : nullptr;
        callerInfo.slot->call(context, argv);
    }
    emit postUrlEncodedObjectFinished(output);
}

} // namespace QtOpenAPI
