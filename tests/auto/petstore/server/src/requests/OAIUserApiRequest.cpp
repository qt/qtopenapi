/**
 * OpenAPI Petstore
 * This is a sample of Petstore application. For this sample, you can use the api key `special-key` to test the authorization filters.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include <QJsonArray>
#include <QJsonDocument>
#include <QJsonObject>
#include <QJsonValue>
#include <QUrlQuery>
#include <QVariantMap>
#include <QDebug>

#include "OAIHelpers.h"
#include "OAIUserApiRequest.h"

namespace OpenAPI {

OAIUserApiRequest::OAIUserApiRequest(QHttpEngine::Socket *s, QSharedPointer<OAIUserApiHandler> hdl) : QObject(s), socket(s), handler(hdl) {
    auto headers = s->headers();
    for(auto itr = headers.begin(); itr != headers.end(); itr++) {
        requestHeaders.insert(QString(itr.key()), QString(itr.value()));
    }
}

OAIUserApiRequest::~OAIUserApiRequest(){
    disconnect(this, nullptr, nullptr, nullptr);
    qDebug() << "OAIUserApiRequest::~OAIUserApiRequest()";
}

QMap<QString, QString>
OAIUserApiRequest::getRequestHeaders() const {
    return requestHeaders;
}

void OAIUserApiRequest::setResponseHeaders(const QMultiMap<QString, QString>& headers){
    for(auto itr = headers.begin(); itr != headers.end(); ++itr) {
        responseHeaders.insert(itr.key(), itr.value());
    }
}


QHttpEngine::Socket* OAIUserApiRequest::getRawSocket(){
    return socket;
}

void OAIUserApiRequest::createInQueryMapRequest()
{
    qDebug() << "/v2/user/createInQueryMap";
    connect(this, &OAIUserApiRequest::createInQueryMap, handler.data(), &OAIUserApiHandler::createInQueryMap);

    Q_EMIT createInQueryMap(socket->queryString());
}

void OAIUserApiRequest::createUserRequest(){
    qDebug() << "/v2/user";
    connect(this, &OAIUserApiRequest::createUser, handler.data(), &OAIUserApiHandler::createUser);

    
 
    
    QJsonDocument doc;
    socket->readJson(doc);
    QJsonObject obj = doc.object();
    OAIUser body;
    ::OpenAPI::fromJsonValue(body, obj);
    

    Q_EMIT createUser(body);
}


void OAIUserApiRequest::createUsersWithArrayInputRequest(){
    qDebug() << "/v2/user/createWithArray";
    connect(this, &OAIUserApiRequest::createUsersWithArrayInput, handler.data(), &OAIUserApiHandler::createUsersWithArrayInput);

    
 
    QJsonDocument doc;
    QList<OAIUser> body;
    if(socket->readJson(doc)){
        QJsonArray jsonArray = doc.array();
        foreach(QJsonValue obj, jsonArray) {
            OAIUser o;
            ::OpenAPI::fromJsonValue(o, obj);
            body.append(o);
        }
    }
    

    Q_EMIT createUsersWithArrayInput(body);
}


void OAIUserApiRequest::createUsersWithListInputRequest(){
    qDebug() << "/v2/user/createWithList";
    connect(this, &OAIUserApiRequest::createUsersWithListInput, handler.data(), &OAIUserApiHandler::createUsersWithListInput);

    
 
    QJsonDocument doc;
    QList<OAIUser> body;
    if(socket->readJson(doc)){
        QJsonArray jsonArray = doc.array();
        foreach(QJsonValue obj, jsonArray) {
            OAIUser o;
            ::OpenAPI::fromJsonValue(o, obj);
            body.append(o);
        }
    }
    

    Q_EMIT createUsersWithListInput(body);
}

// Since OpenAPI 3.0 supports arrays and objects in operation parameters(path, query, header, and cookie),
// Path Parameters object are being serialized as a string of values devided by 'symbol'.
// For example: '/users/role,admin,firstName,Alex' (explode = false)
// See https://swagger.io/docs/specification/v3_0/serialization/
QString takeNextValue(const QString& parameters, const QString &value, const QString &splitter = ",")
{
    QStringList fields = parameters.split(splitter);
    Q_ASSERT(fields.size() % 2 == 0);
    int index = 0;
    for (auto i = 0; i < fields.size() - 1; i += 2) {
        if (fields[i].toLower() == value.toLower()) {
            index = i + 1;
            break;
        }
    }
    if (index > 0)
        return fields[index];
    return QString();
}

void OAIUserApiRequest::deleteUserRequest(const QString& usernamestr){
    qDebug() << "/v2/user/{username}";
    connect(this, &OAIUserApiRequest::deleteUser, handler.data(), &OAIUserApiHandler::deleteUser);

    OAIUser user;
    qint64 id;
    qint32 status;

    fromStringValue(takeNextValue(usernamestr, "id"), id);
    fromStringValue(takeNextValue(usernamestr, "userstatus"), status);

    user.setId(id);
    user.setUsername(takeNextValue(usernamestr, "username"));
    user.setFirstName(takeNextValue(usernamestr, "firstname"));
    user.setLastName(takeNextValue(usernamestr, "lastname"));
    user.setEmail(takeNextValue(usernamestr, "email"));
    user.setPassword(takeNextValue(usernamestr, "password"));
    user.setPhone(takeNextValue(usernamestr, "phone"));
    user.setUserStatus(status);

    Q_EMIT deleteUser(user);
}


void OAIUserApiRequest::getUserByNameRequest(const QString& usernamestr){
    qDebug() << "/v2/user/{username}";
    connect(this, &OAIUserApiRequest::getUserByName, handler.data(), &OAIUserApiHandler::getUserByName);

    QMap<QString, qint32> username;
    QJsonObject object;
    QStringList list = usernamestr.split(",");
    if (list.size() % 2 == 0) {
        for (int i = 0; i < list.size(); i += 2) {
            const QString key = list.at(i);
            const qint32 value = QVariant(list.at(i + 1)).toInt();
            object.insert(key, value);
        }
    }
    fromJsonValue(username, object);
    Q_EMIT getUserByName(username);
}


void OAIUserApiRequest::loginUserRequest(){
    qDebug() << "/v2/user/login";
    connect(this, &OAIUserApiRequest::loginUser, handler.data(), &OAIUserApiHandler::loginUser);

    QString username;
    if(socket->queryString().keys().contains("username")){
        fromStringValue(socket->queryString().value("username"), username);
    }
    
    QString password;
    if(socket->queryString().keys().contains("password")){
        fromStringValue(socket->queryString().value("password"), password);
    }
    
    responseHeaders.insert("Content-Type", "text/plain; charset=utf-8");

    Q_EMIT loginUser(username, password);
}


void OAIUserApiRequest::logoutUserRequest(){
    qDebug() << "/v2/user/logout";
    connect(this, &OAIUserApiRequest::logoutUser, handler.data(), &OAIUserApiHandler::logoutUser);

    QJsonValue username;
    if(socket->queryString().keys().contains("username")){
        username = QJsonValue(socket->queryString().value("username"));
    }
    Q_EMIT logoutUser(username);
}


void OAIUserApiRequest::updateUserRequest(const QString& usernamestr){
    qDebug() << "/v2/user/{username}";
    connect(this, &OAIUserApiRequest::updateUser, handler.data(), &OAIUserApiHandler::updateUser);

    QString username;
    fromStringValue(usernamestr, username);

    QJsonDocument doc;
    auto queryBody = socket->queryString();
    OAIUser body;
    QJsonObject object = body.asJsonObject();

    for (auto i = queryBody.cbegin(), end = queryBody.cend(); i != end; ++i) {
        QUrlQuery url(QUrl::fromPercentEncoding(i.key().toLatin1()));
        QPair<QString, QString> pair;
        foreach (pair, url.queryItems()) {
            object.insert(pair.first, pair.second);
        }
    }

    body.fromJsonObject(object);
    Q_EMIT updateUser(username, body);
}

void OAIUserApiRequest::createInQueryMapResponse(bool inserted)
{
    setSocketResponseHeaders();
    if (inserted)
        socket->setStatusCode(QHttpEngine::Socket::OK);
    else
        socket->setStatusCode(QHttpEngine::Socket::NotFound);
    socket->writeHeaders();
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::createUserResponse(){
    setSocketResponseHeaders();
    socket->setStatusCode(QHttpEngine::Socket::OK);
    socket->writeHeaders();
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::createUsersWithArrayInputResponse(){
    setSocketResponseHeaders();
    socket->setStatusCode(QHttpEngine::Socket::OK);
    socket->writeHeaders();
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::createUsersWithListInputResponse(){
    setSocketResponseHeaders();
    socket->setStatusCode(QHttpEngine::Socket::OK);
    socket->writeHeaders();
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::deleteUserResponse(bool deleted){
    setSocketResponseHeaders();
    if (deleted)
        socket->setStatusCode(QHttpEngine::Socket::OK);
    else
        socket->setStatusCode(QHttpEngine::Socket::NotFound);
    socket->writeHeaders();
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::getUserByNameResponse(bool found, const OAIUser& res){
    setSocketResponseHeaders();
    if (found) {
        socket->setStatusCode(QHttpEngine::Socket::OK);
        QJsonDocument resDoc(::OpenAPI::toJsonValue(res).toObject());
        socket->writeJson(resDoc);
    } else {
        socket->setStatusCode(QHttpEngine::Socket::NotFound);
        socket->writeHeaders();
    }
    if (socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::loginUserResponse(const QString& res){
    setSocketResponseHeaders();
    socket->write(res.toUtf8());
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::logoutUserResponse(){
    setSocketResponseHeaders();
    socket->setStatusCode(QHttpEngine::Socket::OK);
    socket->writeHeaders();
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::updateUserResponse(bool updated, const OAIUser &res) {
    setSocketResponseHeaders();
    if (updated) {
        socket->setStatusCode(QHttpEngine::Socket::OK);
        QJsonDocument resDoc(::OpenAPI::toJsonValue(res).toObject());
        socket->writeJson(resDoc);
    } else {
        socket->setStatusCode(QHttpEngine::Socket::NotFound);
        socket->writeHeaders();
    }

    if(socket->isOpen()){
        socket->close();
    }
}


void OAIUserApiRequest::createInQueryMapError(QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    setSocketResponseHeaders();
    socket->setStatusCode(QHttpEngine::Socket::NotFound);
    socket->write(error_str.toUtf8());
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::createUserError(QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    setSocketResponseHeaders();
    socket->setStatusCode(QHttpEngine::Socket::NotFound);
    socket->write(error_str.toUtf8());
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::createUsersWithArrayInputError(QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    setSocketResponseHeaders();
    socket->setStatusCode(QHttpEngine::Socket::NotFound);
    socket->write(error_str.toUtf8());
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::createUsersWithListInputError(QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    setSocketResponseHeaders();
    socket->setStatusCode(QHttpEngine::Socket::NotFound);
    socket->write(error_str.toUtf8());
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::deleteUserError(QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    setSocketResponseHeaders();
    socket->setStatusCode(QHttpEngine::Socket::NotFound);
    socket->write(error_str.toUtf8());
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::getUserByNameError(const OAIUser& res, QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    setSocketResponseHeaders();
    Q_UNUSED(error_str);  // response will be used instead of error string
    QJsonDocument resDoc(::OpenAPI::toJsonValue(res).toObject());
    socket->writeJson(resDoc);
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::loginUserError(const QString& res, QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    setSocketResponseHeaders();
    Q_UNUSED(error_str);  // response will be used instead of error string
    socket->write(res.toUtf8());
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::logoutUserError(QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    setSocketResponseHeaders();
    socket->setStatusCode(QHttpEngine::Socket::NotFound);
    socket->write(error_str.toUtf8());
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::updateUserError(const OAIUser& res, QNetworkReply::NetworkError error_type, QString& error_str){
    Q_UNUSED(error_type); // TODO: Remap error_type to QHttpEngine::Socket errors
    setSocketResponseHeaders();
    Q_UNUSED(error_str);  // response will be used instead of error string
    QJsonDocument resDoc(::OpenAPI::toJsonValue(res).toObject());
    socket->writeJson(resDoc);
    if(socket->isOpen()){
        socket->close();
    }
}


void OAIUserApiRequest::sendCustomResponse(QByteArray & res, QNetworkReply::NetworkError error_type){
    Q_UNUSED(error_type); // TODO
    socket->write(res);
    if(socket->isOpen()){
        socket->close();
    }
}

void OAIUserApiRequest::sendCustomResponse(QIODevice *res, QNetworkReply::NetworkError error_type){
    Q_UNUSED(error_type);  // TODO
    socket->write(res->readAll());
    if(socket->isOpen()){
        socket->close();
    }
}

}
