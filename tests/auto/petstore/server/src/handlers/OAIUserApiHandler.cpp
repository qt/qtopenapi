/**
 * OpenAPI Petstore
 * This is a sample of Petstore application. For this sample, you can use the api key `special-key` to test the authorization filters.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include <QJsonArray>
#include <QJsonDocument>
#include <QJsonObject>
#include <QUrlQuery>
#include <QVariantMap>
#include <QDebug>

#include "OAIHelpers.h"
#include "OAIUserApiHandler.h"
#include "OAIUserApiRequest.h"

namespace OpenAPI {

OAIUserApiHandler::OAIUserApiHandler(){

}

OAIUserApiHandler::~OAIUserApiHandler(){

}

void OAIUserApiHandler::createInQueryMap(QMap<QString, QString> username) {
    auto reqObj = qobject_cast<OAIUserApiRequest*>(sender());
    if( reqObj != nullptr )
    {
        bool created = false;
        if (username.size() > 0) {
            for (auto iter = username.cbegin(), end = username.cend(); iter != end; ++iter) {
                const QUrlQuery url(QUrl::fromPercentEncoding(iter.key().toLatin1()));
                const QList<QPair<QString, QString>> pair = url.queryItems();
                if (pair.size() == 1) {
                    OAIUser user;
                    user.setUsername(pair.at(0).first);
                    user.setFirstName(pair.at(0).first);
                    user.setUserStatus(QVariant(pair.at(0).second).toInt());
                    m_users.insert(pair.at(0).first, user);
                    created = true;
                }
            }
        }
        reqObj->createInQueryMapResponse(created);
    }
}
void OAIUserApiHandler::createUser(OAIUser body) {
    m_users.insert(body.getUsername(), body);
    auto reqObj = qobject_cast<OAIUserApiRequest*>(sender());
    if( reqObj != nullptr )
    {
        
        reqObj->createUserResponse();
    }
}
void OAIUserApiHandler::createUsersWithArrayInput(QList<OAIUser> body) {
    for (auto eachBody: body)
        m_users.insert(eachBody.getUsername(), eachBody);
    auto reqObj = qobject_cast<OAIUserApiRequest*>(sender());
    if( reqObj != nullptr )
    {
        
        reqObj->createUsersWithArrayInputResponse();
    }
}
void OAIUserApiHandler::createUsersWithListInput(QList<OAIUser> body) {
    for (auto eachBody: body)
        m_users.insert(eachBody.getUsername(), eachBody);
    auto reqObj = qobject_cast<OAIUserApiRequest*>(sender());
    if( reqObj != nullptr )
    {
        
        reqObj->createUsersWithListInputResponse();
    }
}
void OAIUserApiHandler::deleteUser(OAIUser username) {
    auto reqObj = qobject_cast<OAIUserApiRequest*>(sender());
    if( reqObj != nullptr )
    {
        const OAIUser stored = m_users.take(username.getUsername());
        bool status = stored.asJson() == username.asJson();
        if (!status)
            qWarning() << "OAIUserApiHandler::deleteUser: no user is found.";
        reqObj->deleteUserResponse(status);
    }
}
void OAIUserApiHandler::getUserByName(const QMap<QString, qint32> &username) {
    auto reqObj = qobject_cast<OAIUserApiRequest*>(sender());
    if( reqObj != nullptr )
    {
        OAIUser res;
        bool found = false;
        for (auto userIt = username.cbegin(); userIt != username.cend(); ++userIt) {
            auto iter = m_users.constFind(userIt.key());
            if (iter != m_users.cend()) {
                res = iter.value();
                found = true;
                break;
            }
        }
        reqObj->getUserByNameResponse(found, res);
    }
}
void OAIUserApiHandler::loginUser(QString username, QString password) {
    auto reqObj = qobject_cast<OAIUserApiRequest*>(sender());
    if( reqObj != nullptr )
    {
        QString res = username + password;
        reqObj->loginUserResponse(res);
    }
}
void OAIUserApiHandler::logoutUser(QJsonValue username) {
    auto reqObj = qobject_cast<OAIUserApiRequest*>(sender());
    if( reqObj != nullptr )
    {
        
        reqObj->logoutUserResponse();
    }
}
void OAIUserApiHandler::updateUser(QString username, OAIUser body) {
    auto iter = m_users.constFind(username);
    auto reqObj = qobject_cast<OAIUserApiRequest*>(sender());
    if( reqObj != nullptr )
    {
        OAIUser res;
        bool updated = (iter != m_users.cend());
        if (updated) {
            m_users.insert(username, body);
            res = body;
        }
        reqObj->updateUserResponse(updated, res);
    }
}


}
